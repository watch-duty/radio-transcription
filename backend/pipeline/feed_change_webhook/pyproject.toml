[project]
name = "feed-change-webhook"
version = "0.1.0"
requires-python = ">=3.13, <3.14"

dependencies = [
    "fastapi>=0.110.0",
    "httpx>=0.28.1",
    "tenacity>=9.1.4",
    "uvicorn[standard]>=0.27.0",
    "radio-transcription-common",
]

[tool.setuptools]
packages = []
