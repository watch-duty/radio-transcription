{
  "event": "call_download_failed",
  "expected_keys": [
    "event_type",
    "feed_id",
    "source_type"
  ],
  "_note": "LOG-02: emitted through the shared telemetry.emit_call_download_failed helper. Collectors call the helper instead of owning event payload construction. Keys alphabetized; tests assert the helper payload keys match expected_keys."
}
