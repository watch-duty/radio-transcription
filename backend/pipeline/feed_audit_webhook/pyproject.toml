[project]
name = "feed-audit-webhook"
version = "0.1.0"
requires-python = ">=3.13, <3.14"

dependencies = [
    "fastapi>=0.110.0",
    "uvicorn[standard]>=0.27.0",
    "urllib3>=2.7.0",
    "radio-transcription-common",
]

[tool.setuptools]
packages = []

