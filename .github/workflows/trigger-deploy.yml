name: Trigger Private Deployment

on:
  push:
    branches: [main]

jobs:
  signal-private:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2 # Required to see what changed

      - name: Check for Module Changes
        id: filter
        run: |
          # Check if any files in the terraform/modules directory changed
          MOD_CHANGED=$(git diff --name-only HEAD^ HEAD | grep 'terraform/modules/' || true)
          if [ -n "$MOD_CHANGED" ]; then
            echo "infra_change=true" >> $GITHUB_OUTPUT
          else
            echo "infra_change=false" >> $GITHUB_OUTPUT
          fi

      - name: Notify Private Repo
        env:
          GH_TOKEN: ${{ secrets.PRIVATE_REPO_PAT }}
        run: |
          # Decide which workflow to trigger based on what changed
          if [ "${{ steps.filter.outputs.infra_change }}" == "true" ]; then
            echo "Module changed! Triggering Infrastructure Update..."
            gh workflow run terraform.yml --repo ${{ secrets.PRIVATE_REPO_TARGET }} -f public_sha=${{ github.sha }}
          else
            echo "Only code changed. Triggering App Deployment..."
            gh workflow run app-deploy.yml --repo ${{ secrets.PRIVATE_REPO_TARGET }} -f public_sha=${{ github.sha }}
          fi