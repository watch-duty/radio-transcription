name: Integration Tests

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  run-integration-tests:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run integration tests
        run: docker compose run --rm integration-tests

      # for testing only
      - name: Print transcripts-api logs on failure
        if: failure()
        run: docker compose logs transcripts-api
