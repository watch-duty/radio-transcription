# Install the common library with all extras needed for the SFT pipeline.
# Run from model/scripts/sft/: pip install -e "../../.[scoring,hf,audio,vertex]"
# Or install into a venv: python -m venv .venv && .venv/bin/pip install -e "../../.[scoring,hf,audio,vertex]"
-e ../../.[scoring,hf,audio,vertex]
