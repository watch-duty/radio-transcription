---
phase: 05-retention-and-verification-hardening
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pipeline/storage/tests/test_feed_audit_v1_verification_gate.py
autonomous: true
requirements: [VER-01, VER-03, VER-05]
must_haves:
  truths:
    - "Automated tests explicitly cover feed.created, feed.updated, feed.deactivated, feed.reset, feed.deleted, feed.failure_reported, feed.quarantined, and feed.recovered per VER-01."
    - "Automated tests explicitly cover status_reason_detail lifecycle, public API migration away from quarantine_reason, and secret/detail bounding per VER-03."
    - "Automated tests explicitly cover clean heartbeat, clean progress, clean source-observation, and runtime/Echo no-default-audit paths per VER-05."
    - "Verification grouping preserves D-12 by covering retention-adjacent contract behavior without redefining event semantics."
    - "Verification grouping preserves D-13 by using existing low-resource contract/unit/service lanes for local checks."
  artifacts:
    - path: "backend/pipeline/storage/tests/test_feed_audit_v1_verification_gate.py"
      provides: "Static v1 verification gate over existing behavioral test lanes"
      contains: "test_v1_audit_event_behavior_tests_are_registered"
  key_links:
    - from: "backend/pipeline/storage/tests/test_feed_audit_v1_verification_gate.py"
      to: "backend/pipeline/storage/tests/test_feed_store.py"
      via: "required async storage event test names"
      pattern: "test_writes_feed_created_audit_event"
    - from: "backend/pipeline/storage/tests/test_feed_audit_v1_verification_gate.py"
      to: "frontend/api/src/feeds/feedsController.test.ts"
      via: "required BFF statusReasonDetail compatibility tokens"
      pattern: "statusReasonDetail"
---

<objective>
Create a low-resource v1 verification gate for the already-built audit contract.

Purpose: Phase 5 must prove the v1 behavioral contract without adding broad
local E2E or Docker-heavy test runs by default. This plan adds a focused static
gate that keeps the existing storage, sync, service, API, BFF, diagnostic, and
no-noise test lanes explicitly tied to the Phase 5 requirements.

Output: a new Python test file that verifies the required behavior tests and
public compatibility tests remain registered under exact names/tokens.
</objective>

<execution_context>
@/home/shuojing/.codex/get-shit-done/workflows/execute-plan.md
@/home/shuojing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-retention-and-verification-hardening/05-CONTEXT.md
@.planning/phases/05-retention-and-verification-hardening/05-RESEARCH.md
@.planning/phases/05-retention-and-verification-hardening/05-PATTERNS.md
@AGENTS.md
@.agents/instructions.md
@backend/pipeline/storage/tests/test_feed_store.py
@backend/pipeline/storage/tests/test_sync_feed_store.py
@backend/pipeline/storage/tests/test_feed_query_contracts.py
@backend/pipeline/storage/tests/test_feed_lifecycle.py
@backend/services/feeds/tests/test_api.py
@backend/services/feeds/tests/test_service.py
@frontend/api/src/feeds/feedsController.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add v1 audit verification gate test</name>
  <read_first>
@backend/pipeline/storage/tests/test_feed_audit_v1_verification_gate.py
@backend/pipeline/storage/tests/test_feed_store.py
@backend/pipeline/storage/tests/test_sync_feed_store.py
@backend/pipeline/storage/tests/test_feed_query_contracts.py
@backend/pipeline/storage/tests/test_feed_lifecycle.py
@backend/services/feeds/tests/test_api.py
@backend/services/feeds/tests/test_service.py
@frontend/api/src/feeds/feedsController.test.ts
@AGENTS.md
@.agents/instructions.md
  </read_first>
  <files>backend/pipeline/storage/tests/test_feed_audit_v1_verification_gate.py</files>
  <action>
    Create `backend/pipeline/storage/tests/test_feed_audit_v1_verification_gate.py`. Use `from __future__ import annotations`, `pathlib`, and pure pytest-style `assert` checks. Define `_REPO_ROOT = pathlib.Path(__file__).resolve().parents[4]` and helper `_read(path: str) -> str`.

    Add `test_v1_audit_event_behavior_tests_are_registered()` that reads `backend/pipeline/storage/tests/test_feed_store.py` and asserts the following exact test names and action strings are present:
    - `test_writes_feed_created_audit_event` with `feed.created`
    - `test_meaningful_update_writes_feed_updated_audit_event` with `feed.updated`
    - `test_success_writes_feed_deactivated_audit_event` with `feed.deactivated`
    - `test_success_writes_feed_reset_audit_event` with `feed.reset`
    - `test_success_writes_feed_deleted_before_delete` and `test_feed_deleted_uses_full_before_snapshot_and_empty_after` with `feed.deleted`
    - `test_first_abnormal_failure_emits_failure_reported` and `test_reason_change_emits_failure_reported` with `feed.failure_reported`
    - `test_threshold_crossing_emits_only_quarantined` with `feed.quarantined`
    - `test_successful_progress_from_failing_emits_recovered` and `test_successful_progress_from_quarantined_emits_recovered` with `feed.recovered`

    Add `test_v1_sync_echo_runtime_parity_tests_are_registered()` that reads `backend/pipeline/storage/tests/test_sync_feed_store.py` and asserts these exact names/tokens: `test_reason_change_emits_failure_reported`, `feed.failure_reported`, `test_threshold_crossing_emits_only_quarantined`, `feed.quarantined`, `test_recovery_heartbeat_from_failing_emits_recovered`, `feed.recovered`, `test_clean_heartbeat_emits_no_event`, `test_detail_only_heartbeat_clear_from_normal_emits_no_event`, and `test_no_row_mutation_emits_no_event`.

    Add `test_v1_diagnostic_detail_and_public_api_tests_are_registered()` that reads `backend/pipeline/storage/tests/test_feed_lifecycle.py`, `backend/services/feeds/tests/test_api.py`, `backend/services/feeds/tests/test_service.py`, and `frontend/api/src/feeds/feedsController.test.ts`. Assert these exact strings exist: `test_status_reason_detail_storage_value_redacts_credentials`, `test_status_reason_detail_storage_value_caps_reason`, `test_feed_model_exposes_status_reason_detail_not_quarantine_reason`, `test_get_feed_with_status_reason_detail`, `self.assertNotIn("quarantine_reason", data)`, `test_admin_mutation_methods_require_keyword_only_actor_id`, `status_reason_detail`, and `statusReasonDetail`.

    Add `test_v1_no_noise_and_storage_ownership_tests_are_registered()` that reads `backend/pipeline/storage/tests/test_feed_store.py` and `backend/pipeline/storage/tests/test_feed_query_contracts.py`. Assert these exact strings exist: `test_clean_progress_emits_no_event`, `test_detail_only_progress_clear_from_normal_emits_no_event`, `test_clean_source_observation_emits_no_event`, `test_lease_style_noop_emits_no_event`, `test_runtime_and_echo_sources_do_not_reference_audit_table`, `feed_audit_events`, and `INSERT INTO feed_audit_events`.

    Do not add Docker, Testcontainers, or integration test execution to this file. It is a low-resource registration gate for VER-01, VER-03, and VER-05.
  </action>
  <verify>
    <automated>safe-run -- uv run python -m pytest backend/pipeline/storage/tests/test_feed_audit_v1_verification_gate.py backend/pipeline/storage/tests/test_feed_lifecycle.py backend/pipeline/storage/tests/test_feed_query_contracts.py backend/pipeline/storage/tests/test_feed_store.py backend/pipeline/storage/tests/test_sync_feed_store.py backend/services/feeds/tests/test_api.py backend/services/feeds/tests/test_service.py -q</automated>
    <automated>safe-run -- yarn --cwd frontend/api test --run src/feeds/feedsController.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `test -f backend/pipeline/storage/tests/test_feed_audit_v1_verification_gate.py` exits 0.
    `rg -n "test_v1_audit_event_behavior_tests_are_registered|test_v1_sync_echo_runtime_parity_tests_are_registered|test_v1_diagnostic_detail_and_public_api_tests_are_registered|test_v1_no_noise_and_storage_ownership_tests_are_registered" backend/pipeline/storage/tests/test_feed_audit_v1_verification_gate.py` finds all four gate tests.
    `rg -n "feed\\.created|feed\\.updated|feed\\.deactivated|feed\\.reset|feed\\.deleted|feed\\.failure_reported|feed\\.quarantined|feed\\.recovered|statusReasonDetail|quarantine_reason|test_clean_heartbeat_emits_no_event" backend/pipeline/storage/tests/test_feed_audit_v1_verification_gate.py` finds the required coverage tokens.
    The Python pytest command in `<verify>` exits 0.
    The frontend API Vitest command in `<verify>` exits 0 or the summary records the exact dependency/setup failure if frontend dependencies are not installed.
  </acceptance_criteria>
  <done>The low-resource gate proves Phase 5's required v1 behavior tests are registered and runnable without Docker/Testcontainers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test gate -> behavioral test files | The new gate reads existing tests and fails if required v1 coverage disappears. |
| Backend service/BFF -> public feed consumers | Public compatibility depends on canonical `status_reason_detail` and no public `quarantine_reason` alias. |
| Runtime/Echo -> storage audit insertion | Runtime and Echo paths must keep audit row construction inside storage boundaries. |
| Local executor -> host resources | Verification must stay in low-resource unit/contract lanes by default. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Repudiation | Audit event coverage | mitigate | Gate exact behavior-test names and action tokens for all eight v1 audit actions. |
| T-05-02-02 | Information Disclosure | Diagnostic detail tests | mitigate | Gate secret redaction and length cap tests for `status_reason_detail`. |
| T-05-02-03 | Tampering | Public API compatibility | mitigate | Gate backend and BFF tests that expose `status_reason_detail` and reject public `quarantine_reason` in responses. |
| T-05-02-04 | Denial of Service | Test execution | mitigate | Use targeted `safe-run -- uv run python -m pytest ...` and targeted Vitest commands, not broad E2E/API/component/Docker lanes. |
| T-05-02-05 | Elevation of Privilege | Actor propagation tests | mitigate | Gate service tests requiring keyword-only actor IDs and trusted actor propagation, without accepting actor IDs from request bodies. |
| T-05-02-06 | Tampering | Runtime/Echo audit ownership | mitigate | Gate existing static test that runtime and Echo implementation files do not reference `feed_audit_events` directly. |
</threat_model>

<verification>
Run the low-resource v1 verification command set:

```bash
safe-run -- uv run python -m pytest \
  backend/pipeline/storage/tests/test_feed_audit_v1_verification_gate.py \
  backend/pipeline/storage/tests/test_feed_lifecycle.py \
  backend/pipeline/storage/tests/test_feed_query_contracts.py \
  backend/pipeline/storage/tests/test_feed_store.py \
  backend/pipeline/storage/tests/test_sync_feed_store.py \
  backend/services/feeds/tests/test_api.py \
  backend/services/feeds/tests/test_service.py \
  -q
safe-run -- yarn --cwd frontend/api test --run src/feeds/feedsController.test.ts
git diff --check
```

Do not run Testcontainers or full integration stacks for this plan.
</verification>

<success_criteria>
- The new gate file exists and names all eight v1 audit actions.
- The gate proves diagnostic-detail sanitizer, public API/BFF compatibility, no-noise runtime/sync paths, and storage-owned audit insertion tests remain registered.
- Targeted Python and frontend API commands pass or any environment-only frontend dependency issue is recorded in the summary with exact output.
</success_criteria>

<output>
After completion, create `.planning/phases/05-retention-and-verification-hardening/05-02-SUMMARY.md`.
</output>
