---
phase: 04-durable-eval
plan: "04"
type: execute
wave: 4
depends_on: ["04-03"]
files_modified: [model/scripts/sft/run_config.example.toml, model/scripts/sft/README.md, model/scripts/sft/score_gemini_sft_checkpoints_online.py, model/tests/gemini_sft/test_checkpoint_scorer.py, model/tests/common/tests/test_drift_guard.py]
autonomous: true
requirements: [EXEC-05, DATA-03, DATA-04, DATA-05]
user_setup: []
must_haves:
  truths:
    - "D-01/D-02/D-03: Docs and examples must show [eval.model]/eval_model only and must not show plural [[eval.models]] as supported packaged eval config."
    - "D-06/D-16/D-17: Internal multi-target parallelism, dataset breakdowns, and multiple eval manifests are deferred; docs must direct operators to separate configs/wrappers for multi-model comparisons and follow-up work for dataset breakdowns."
    - "D-12/D-13/D-14/D-15: Docs must name stable GCS evals/wer_summary.json and evals/wer_summary.md, overwrite behavior, and raw output/online prediction/normalized manifest links."
  artifacts:
    - path: "model/scripts/sft/run_config.example.toml"
      provides: "Operator example for singular eval model"
      contains: "[eval.model]"
    - path: "model/scripts/sft/README.md"
      provides: "Operator docs for singular eval, durable summaries, and deferred breakdowns"
      contains: "evals/wer_summary.json"
    - path: "model/scripts/sft/score_gemini_sft_checkpoints_online.py"
      provides: "Legacy checkpoint sweep compatibility after config API changes"
      contains: "load_eval_run_config"
    - path: "model/tests/common/tests/test_drift_guard.py"
      provides: "Drift guard for singular config docs and shared helpers"
      contains: "eval.model"
  key_links:
    - from: "model/scripts/sft/README.md"
      to: ".planning/phases/04-durable-eval/04-CONTEXT.md"
      via: "superseded multi-target and dataset-breakdown decisions"
      pattern: "dataset breakdown"
---

<objective>
Update operator-facing docs and compatibility tests for the narrowed durable
eval contract.

Purpose: the repo should no longer teach plural packaged eval targets or
dataset breakdown behavior for Phase 4, while checkpoint sweep tooling remains
usable as a separate legacy/ranking path.

Output: singular example config, README artifact/reuse semantics, explicit
deferred multi-target/dataset-breakdown guidance, and drift/checkpoint tests
that stay aligned with the new config API.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-durable-eval/04-CONTEXT.md
@.planning/phases/04-durable-eval/04-RESEARCH.md
@.planning/phases/04-durable-eval/04-PATTERNS.md
@.planning/phases/04-durable-eval/04-03-PLAN.md
@.planning/codebase/CONVENTIONS.md
@AGENTS.md
@.github/instructions/PYTHON_STYLE.instructions.md
@model/scripts/sft/run_config.example.toml
@model/scripts/sft/README.md
@model/scripts/sft/score_gemini_sft_checkpoints_online.py
@model/tests/gemini_sft/test_checkpoint_scorer.py
@model/tests/common/tests/test_drift_guard.py
</context>

<threat_model>
Trust boundaries:
- README and example TOML are copied into real paid experiments.
- The legacy checkpoint scorer still performs online inference and ranking.
- Requirement IDs `EXEC-05`, `DATA-03`, and `DATA-04` remain in ROADMAP even
  though Phase 4 context defers their original behavior.

STRIDE register:
- DUR-T10 documentation spoofing: examples can suggest plural target support
  still exists. Mitigation: remove plural packaged eval examples and show one
  `[eval.model]`.
- DUR-T11 scope creep replay: deferred dataset breakdowns can be mistaken as
  implemented. Mitigation: README states dataset breakdowns and multiple eval
  manifests are follow-up work.
- DUR-T12 checkpoint path confusion: users can conflate packaged eval with the
  checkpoint sweep script. Mitigation: README says packaged eval uses one
  explicit model, while the sweep script remains for tuning-job checkpoint
  discovery/ranking.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Update example config to singular eval.model</name>
  <files>model/scripts/sft/run_config.example.toml</files>
  <read_first>model/scripts/sft/run_config.example.toml, .planning/phases/04-durable-eval/04-CONTEXT.md, model/src/gemini_sft/config.py</read_first>
  <action>Replace the `[eval]` plus `[[eval.models]]` example with a singular `[eval.model]` table containing `label = "base"` and `model = "gemini-3.1-flash-lite"`. Replace the commented checkpoint example with commented lines under the same `[eval.model]` table format: `# label = "checkpoint_6"` and `# model = "projects/PROJECT/locations/us-central1/endpoints/ENDPOINT_ID"`. Add comments that `gemini-sft eval` supports one model per config, and that operators should run separate config files in parallel externally when comparing base, tuned, or checkpoint models. Keep `[eval.execution]` showing only optional `backend`, optional `limit`, `concurrency`, and `max_retries`.</action>
  <verify>rg -n "\\[eval\\.model\\]|\\[\\[eval\\.models\\]\\]|one model per config|concurrency|max_retries" model/scripts/sft/run_config.example.toml</verify>
  <acceptance_criteria>
    - `model/scripts/sft/run_config.example.toml` contains `[eval.model]`
    - `model/scripts/sft/run_config.example.toml` contains `one model per config`
    - `model/scripts/sft/run_config.example.toml` does not contain `[[eval.models]]`
    - `model/scripts/sft/run_config.example.toml` contains `[eval.execution]`
    - `model/scripts/sft/run_config.example.toml` contains `max_retries = 3`
  </acceptance_criteria>
  <done>The example config teaches the singular eval model contract only.</done>
</task>

<task type="auto">
  <name>Task 2: Update README durable eval semantics</name>
  <files>model/scripts/sft/README.md</files>
  <read_first>model/scripts/sft/README.md, .planning/phases/04-durable-eval/04-CONTEXT.md, model/src/gemini_sft/reporting.py, model/src/common/gemini/batch.py</read_first>
  <action>Update `model/scripts/sft/README.md` so every packaged eval section says eval requires one `[eval.model]` with `label` and `model`, durable `config.json` stores `eval_model`, and plural `[[eval.models]]` is unsupported. In the execution section, state that multiple model comparisons should use separate config files and an external wrapper, not internal target parallelism. In the records/artifacts section, add `evals/<label>/batch_predictions.meta.json`, `evals/wer_summary.json`, and `evals/wer_summary.md` to the run prefix layout. State that summary files are overwritten after each successful eval, batch/online outputs are reused only when request-identity metadata matches, local `results/<round-id>/` remains a mirror/cache, and dataset breakdowns/multiple eval manifests are follow-up work not implemented in Phase 4.</action>
  <verify>rg -n "\\[eval\\.model\\]|eval_model|\\[\\[eval\\.models\\]\\]|batch_predictions.meta.json|evals/wer_summary.json|dataset breakdowns|external wrapper" model/scripts/sft/README.md</verify>
  <acceptance_criteria>
    - `model/scripts/sft/README.md` contains `[eval.model]`
    - `model/scripts/sft/README.md` contains `eval_model`
    - `model/scripts/sft/README.md` contains `plural [[eval.models]] is unsupported`
    - `model/scripts/sft/README.md` contains `batch_predictions.meta.json`
    - `model/scripts/sft/README.md` contains `evals/wer_summary.json`
    - `model/scripts/sft/README.md` contains `dataset breakdowns`
  </acceptance_criteria>
  <done>Operator docs match the Phase 4 durable eval contract and defer dataset breakdowns explicitly.</done>
</task>

<task type="auto">
  <name>Task 3: Keep checkpoint scorer and drift guards compatible</name>
  <files>model/scripts/sft/score_gemini_sft_checkpoints_online.py, model/tests/gemini_sft/test_checkpoint_scorer.py, model/tests/common/tests/test_drift_guard.py</files>
  <read_first>model/scripts/sft/score_gemini_sft_checkpoints_online.py, model/tests/gemini_sft/test_checkpoint_scorer.py, model/tests/common/tests/test_drift_guard.py, model/src/gemini_sft/config.py, model/src/gemini_sft/target_execution.py</read_first>
  <action>Audit the checkpoint scorer for config API fallout from replacing `eval_models` with `eval_model`. Keep checkpoint discovery/ranking behavior intact. If the scorer only relies on `load_eval_run_config`, `canonical_eval_uri`, prompt/context fields, and package online executor, update tests and helpers to provide singular `[eval.model]` config fixtures. Update drift guards to assert `model/scripts/sft/run_config.example.toml` contains `[eval.model]`, does not contain `[[eval.models]]`, and that `score_gemini_sft_checkpoints_online.py` still imports `run_online_target_inference`. Do not add dataset breakdown or internal multi-target behavior to the scorer.</action>
  <verify>safe-run -- bash -lc 'cd model && PYTHONPATH=src:tests:scripts/sft python3 -m pytest tests/gemini_sft/test_checkpoint_scorer.py tests/common/tests/test_drift_guard.py -q'</verify>
  <acceptance_criteria>
    - `model/tests/common/tests/test_drift_guard.py` contains `[eval.model]`
    - `model/tests/common/tests/test_drift_guard.py` contains `[[eval.models]]`
    - `model/tests/common/tests/test_drift_guard.py` contains `run_online_target_inference`
    - `model/tests/gemini_sft/test_checkpoint_scorer.py` passes with singular eval config fixtures where config fixtures are needed
    - The targeted pytest command exits 0.
  </acceptance_criteria>
  <done>Legacy checkpoint ranking remains compatible, while docs/examples are guarded against plural eval target drift.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m py_compile model/scripts/sft/score_gemini_sft_checkpoints_online.py`
- [ ] `safe-run -- bash -lc 'cd model && PYTHONPATH=src:tests:scripts/sft python3 -m pytest tests/gemini_sft/test_checkpoint_scorer.py tests/common/tests/test_drift_guard.py -q'`
- [ ] `rg -n "\\[eval\\.model\\]|\\[\\[eval\\.models\\]\\]|eval_model|batch_predictions.meta.json|evals/wer_summary.json|dataset breakdowns|run_online_target_inference" model/scripts/sft model/tests/common/tests/test_drift_guard.py model/tests/gemini_sft/test_checkpoint_scorer.py`
</verification>

<success_criteria>
- Example config uses only `[eval.model]` for packaged eval.
- README documents one-model eval, external multi-model wrappers, strict reuse metadata, stable summary GCS paths, and deferred dataset breakdowns.
- Checkpoint scorer remains usable as a separate tuning-job checkpoint sweep path.
- Drift tests catch accidental reintroduction of plural eval model examples.
- Requirement IDs `EXEC-05`, `DATA-03`, and `DATA-04` are addressed as explicitly deferred or superseded by Phase 4 context.
</success_criteria>

<output>
After completion, create `.planning/phases/04-durable-eval/04-04-SUMMARY.md`.
</output>
