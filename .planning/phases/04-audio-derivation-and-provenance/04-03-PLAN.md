---
phase: 04-audio-derivation-and-provenance
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - model/scripts/sft/dataset_split/leakage.py
  - model/scripts/sft/dataset_split/canonical.py
  - model/scripts/sft/tests/test_dataset_split_leakage.py
  - model/scripts/sft/tests/test_dataset_canonical.py
autonomous: true
requirements: [AUD-06]
user_setup: []
must_haves:
  truths:
    - "D-11/D-12/D-23/AUD-06: canonical publication requires every SFT example to have non-empty gs:// model_ready_audio_uri."
    - "D-14: canonical rows keep both original/source URI fields and model-ready URI fields."
    - "D-24: derived_audio_uri is populated only for actual derived clipped spans."
    - "D-25: transformation_metadata.action is one of reused, copied, derived, or transcoded."
    - "D-26: transformation_metadata contains every required provenance field before canonical JSONL serialization."
    - "D-29: split integrity and model-ready validation run after audio enrichment so model_ready_audio_uri leakage is checked."
  artifacts:
    - path: "model/scripts/sft/dataset_split/leakage.py"
      provides: "Post-Phase-4 model-ready audio and provenance validator"
      contains: "def validate_model_ready_audio("
    - path: "model/scripts/sft/dataset_split/canonical.py"
      provides: "Canonical JSONL hard gate for enriched model-ready rows"
      contains: "validate_model_ready_audio"
    - path: "model/scripts/sft/tests/test_dataset_split_leakage.py"
      provides: "Validation tests for model-ready URI, action vocabulary, derived URI, and metadata keys"
      contains: "test_model_ready_audio_uri_is_required"
    - path: "model/scripts/sft/tests/test_dataset_canonical.py"
      provides: "Canonical tests for populated Phase 4 provenance"
      contains: "test_canonical_manifests_require_model_ready_audio"
  key_links:
    - from: "model/scripts/sft/dataset_split/canonical.py"
      to: "model/scripts/sft/dataset_split/leakage.py"
      via: "validate_split_integrity() followed by validate_model_ready_audio()"
      pattern: "validate_split_integrity.*validate_model_ready_audio"
    - from: "model/scripts/sft/dataset_split/leakage.py"
      to: "model/scripts/sft/dataset_split/types.py"
      via: "LabeledSegment model_ready_audio_uri and transformation_metadata fields"
      pattern: "transformation_metadata"
---

<objective>
Add the hard post-audio validation contract for canonical dataset rows.

Purpose: Phase 2 leakage gates already compare model-ready URIs, but they
ignore empty values. Phase 4 needs an explicit boundary that rejects rows
whose model-ready URI or transformation provenance is missing before JSONL
artifacts are written.
Output: `validate_model_ready_audio()`, canonical manifest integration, and
tests covering required `gs://` model-ready audio and provenance metadata.
</objective>

<execution_context>
@/home/shuojing/.codex/get-shit-done/workflows/execute-plan.md
@/home/shuojing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-audio-derivation-and-provenance/04-CONTEXT.md
@.planning/phases/04-audio-derivation-and-provenance/04-RESEARCH.md
@.planning/phases/04-audio-derivation-and-provenance/04-VALIDATION.md
@.planning/phases/04-audio-derivation-and-provenance/04-PATTERNS.md
@.planning/phases/04-audio-derivation-and-provenance/04-01-SUMMARY.md
@.planning/phases/04-audio-derivation-and-provenance/04-02-SUMMARY.md
@model/scripts/sft/dataset_split/types.py
@model/scripts/sft/dataset_split/leakage.py
@model/scripts/sft/dataset_split/canonical.py
@model/scripts/sft/tests/test_dataset_split_leakage.py
@model/scripts/sft/tests/test_dataset_canonical.py

<interfaces>
Use existing leakage API:
```python
class SplitLeakageError(ValueError): ...
def validate_split_leakage(segments: tuple[LabeledSegment, ...]) -> None: ...
def validate_no_duplicate_audio_spans(segments: tuple[LabeledSegment, ...]) -> None: ...
def validate_split_integrity(segments: tuple[LabeledSegment, ...]) -> None: ...
```

Add this validator:
```python
AUDIO_ACTIONS = frozenset({"reused", "copied", "derived", "transcoded"})
TRANSFORMATION_METADATA_KEYS = frozenset({
    "original_audio_uri",
    "source_audio_uri",
    "offset",
    "duration",
    "source_duration",
    "output_duration",
    "source_format",
    "output_format",
    "source_channels",
    "output_channels",
    "mixed_to_mono",
    "resampled",
    "padded",
    "split",
    "source_group",
})

def validate_model_ready_audio(
    segments: tuple[LabeledSegment, ...],
) -> None: ...
```

`validate_model_ready_audio()` raises `SplitLeakageError` with row context.
It does not replace `validate_split_integrity()`; canonical manifest builders
must call both.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add post-audio validation and canonical tests</name>
  <files>model/scripts/sft/tests/test_dataset_split_leakage.py, model/scripts/sft/tests/test_dataset_canonical.py</files>
  <read_first>.planning/phases/04-audio-derivation-and-provenance/04-CONTEXT.md, .planning/phases/04-audio-derivation-and-provenance/04-VALIDATION.md, .planning/phases/04-audio-derivation-and-provenance/04-PATTERNS.md, model/scripts/sft/tests/test_dataset_split_leakage.py, model/scripts/sft/tests/test_dataset_canonical.py, model/scripts/sft/dataset_split/leakage.py, model/scripts/sft/dataset_split/canonical.py, model/scripts/sft/dataset_split/types.py</read_first>
  <action>Extend `test_dataset_split_leakage.py` with tests for `validate_model_ready_audio()`: `test_model_ready_audio_uri_is_required`, `test_model_ready_audio_uri_must_be_gcs`, `test_transformation_metadata_action_is_required`, `test_unknown_transformation_action_fails`, `test_derived_audio_uri_required_only_for_derived_action`, `test_transformation_metadata_requires_all_minimum_keys`, and `test_model_ready_uri_overlap_still_fails_after_enrichment`. Extend canonical tests so positive fixtures include the D-26 metadata keys and model-ready GCS URI, and add `test_canonical_manifests_require_model_ready_audio` plus `test_canonical_rows_preserve_original_and_model_ready_audio` for D-14/D-23/D-24/D-25/D-26.</action>
  <verify>
    <automated>PYTHONPATH=model/scripts/sft:model/colabs uv run --project model python -m py_compile model/scripts/sft/tests/test_dataset_split_leakage.py model/scripts/sft/tests/test_dataset_canonical.py</automated>
    <automated>rg -n "test_model_ready_audio_uri_is_required" model/scripts/sft/tests/test_dataset_split_leakage.py</automated>
    <automated>rg -n "test_canonical_manifests_require_model_ready_audio" model/scripts/sft/tests/test_dataset_canonical.py</automated>
  </verify>
  <acceptance_criteria>
    - `model/scripts/sft/tests/test_dataset_split_leakage.py` contains `test_model_ready_audio_uri_is_required`
    - `model/scripts/sft/tests/test_dataset_split_leakage.py` contains `test_model_ready_audio_uri_must_be_gcs`
    - `model/scripts/sft/tests/test_dataset_split_leakage.py` contains `test_unknown_transformation_action_fails`
    - `model/scripts/sft/tests/test_dataset_split_leakage.py` contains `test_derived_audio_uri_required_only_for_derived_action`
    - `model/scripts/sft/tests/test_dataset_split_leakage.py` contains all D-26 metadata key strings
    - `model/scripts/sft/tests/test_dataset_canonical.py` contains `test_canonical_manifests_require_model_ready_audio`
    - `model/scripts/sft/tests/test_dataset_canonical.py` contains `test_canonical_rows_preserve_original_and_model_ready_audio`
  </acceptance_criteria>
  <done>Tests define the hard post-audio validation boundary and canonical provenance expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement model-ready audio validation and canonical integration</name>
  <files>model/scripts/sft/dataset_split/leakage.py, model/scripts/sft/dataset_split/canonical.py, model/scripts/sft/tests/test_dataset_split_leakage.py, model/scripts/sft/tests/test_dataset_canonical.py</files>
  <read_first>model/scripts/sft/dataset_split/leakage.py, model/scripts/sft/dataset_split/canonical.py, model/scripts/sft/dataset_split/types.py, model/scripts/sft/tests/test_dataset_split_leakage.py, model/scripts/sft/tests/test_dataset_canonical.py, .planning/phases/04-audio-derivation-and-provenance/04-CONTEXT.md, .planning/phases/04-audio-derivation-and-provenance/04-PATTERNS.md</read_first>
  <action>In `leakage.py`, add `AUDIO_ACTIONS`, `TRANSFORMATION_METADATA_KEYS`, and `validate_model_ready_audio(segments)`. For each row, require `segment.model_ready_audio_uri.strip().startswith("gs://")` per D-11/D-12/D-23; require `segment.transformation_metadata` to be a mapping; require `action` in `AUDIO_ACTIONS` per D-25; require every D-26 key; require `metadata["split"] == segment.split` and `metadata["source_group"] == segment.source_group`; require `segment.derived_audio_uri` to be a non-empty `gs://` URI iff action is `derived`, and require it to be `None` or blank for `reused`, `copied`, and `transcoded` per D-24. Error messages must include row_index, action when available, and the missing/invalid field. In `canonical.py`, import `validate_model_ready_audio` and call it after `validate_split_integrity()` in `canonical_rows()`, `canonical_manifests()`, and `per_dataset_manifests()` so canonical artifacts are never serialized with incomplete Phase 4 provenance per D-14/D-29.</action>
  <verify>
    <automated>PYTHONPATH=model/scripts/sft:model/colabs uv run --project model python -m unittest model.scripts.sft.tests.test_dataset_split_leakage model.scripts.sft.tests.test_dataset_canonical</automated>
  </verify>
  <acceptance_criteria>
    - `model/scripts/sft/dataset_split/leakage.py` contains `def validate_model_ready_audio(`
    - `model/scripts/sft/dataset_split/leakage.py` contains `AUDIO_ACTIONS`
    - `model/scripts/sft/dataset_split/leakage.py` contains `TRANSFORMATION_METADATA_KEYS`
    - `model/scripts/sft/dataset_split/leakage.py` contains `startswith("gs://")`
    - `model/scripts/sft/dataset_split/leakage.py` contains `derived_audio_uri`
    - `model/scripts/sft/dataset_split/canonical.py` imports `validate_model_ready_audio`
    - `model/scripts/sft/dataset_split/canonical.py` calls `validate_model_ready_audio(` in canonical rows/manifests paths
    - `PYTHONPATH=model/scripts/sft:model/colabs uv run --project model python -m unittest model.scripts.sft.tests.test_dataset_split_leakage model.scripts.sft.tests.test_dataset_canonical` passes
  </acceptance_criteria>
  <done>Canonical JSONL generation hard-fails unless rows are split-safe, model-ready, and auditable after audio enrichment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Enriched segment -> canonical JSONL | Model-ready URI and transformation metadata become durable generated artifacts. |
| Model-ready URI set -> leakage gate | Cross-split overlap checks only work after non-empty model-ready values exist. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering/Elevation | high | subprocess helpers | transfer/mitigate | Plan 04-01 owns subprocess execution; this plan must not add subprocess calls. |
| T-04-02 | Tampering | high | generated GCS names | transfer/mitigate | Plan 04-01 owns generated object names; this plan validates the resulting `gs://` model-ready URIs before serialization. |
| T-04-03 | Tampering | high | GCS overwrite/race | transfer/mitigate | Plan 04-01/04-02 own GCS writes; this plan only validates generated row fields before text artifact creation. |
| T-04-04 | Denial of Service | high | external URL download | transfer/mitigate | Plan 04-01 owns bounded downloads; canonical validation has no network behavior. |
| T-04-05 | Information Disclosure | high | model-ready audio leakage | mitigate/block | `validate_model_ready_audio()` requires non-empty `gs://` model-ready URIs, validates provenance, then canonical builders call existing split integrity checks so model-ready overlaps are hard failures. |
| T-04-06 | Repudiation | medium | provenance metadata | mitigate | Require D-26 metadata keys and action vocabulary before canonical JSONL generation. |
</threat_model>

<verification>
Focused:

```bash
PYTHONPATH=model/scripts/sft:model/colabs uv run --project model python -m unittest model.scripts.sft.tests.test_dataset_split_leakage model.scripts.sft.tests.test_dataset_canonical
```

Wave gate:

```bash
PYTHONPATH=model/scripts/sft:model/colabs uv run --project model python -m unittest discover model/scripts/sft/tests
```
</verification>

<success_criteria>
- `validate_model_ready_audio()` rejects missing, blank, non-GCS, or overlapping model-ready audio rows.
- Canonical row/manifests preserve original audio, model-ready audio, derived URI, and transformation metadata.
- Canonical JSONL cannot be generated from rows missing the Phase 4 provenance contract.
</success_criteria>

<output>
After completion, create `.planning/phases/04-audio-derivation-and-provenance/04-03-SUMMARY.md`.
</output>
