---
phase: 06-compatibility-and-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pipeline/ingestion/collectors/README.md
autonomous: true
requirements: [DOC-11]
user_setup: []
must_haves:
  truths:
    - "D-05: update only the stale collector guide plus Phase 6 planning summaries/verification docs."
    - "D-06: the collector guide must no longer say pipeline_publish_after_bookmark_failed must not quarantine the feed."
    - "D-07: keep the known data-gap wording; v1.1 still has no durable replay/outbox worker."
    - "D-08: do not touch frontend, OpenAPI, generated API metadata, database migration, or schema files."
  artifacts:
    - path: backend/pipeline/ingestion/collectors/README.md
      provides: "Collector authoring guide reflects final v1.1 status reason and pipeline failure semantics."
  key_links:
    - from: backend/pipeline/ingestion/collectors/README.md
      to: backend/pipeline/ingestion/failure_policy.py
      via: "documented budgeted/non-budgeted rows match policy table semantics"
      pattern: "pipeline_publish_after_bookmark_failed|system_runtime_configuration_invalid|system_credential_access_failed|system_source_payload_invalid"
---

<objective>
Update the backend collector authoring guide so it describes final v1.1
quarantine policy semantics instead of the older non-budgeted Pub/Sub
post-bookmark behavior.

Purpose: downstream contributors should not read stale guidance saying
`pipeline_publish_after_bookmark_failed` cannot quarantine a feed.
Output: one documentation update in `backend/pipeline/ingestion/collectors/README.md`.
</objective>

<execution_context>
@/home/shuojing/.codex/get-shit-done/workflows/execute-plan.md
@/home/shuojing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/06-compatibility-and-verification/06-CONTEXT.md
@.planning/phases/05-producer-and-runtime-routing-merge/05-CONTEXT.md
@.planning/phases/05-producer-and-runtime-routing-merge/05-VERIFICATION.md
@backend/pipeline/ingestion/collectors/README.md
@backend/pipeline/ingestion/failure_policy.py
@backend/pipeline/ingestion/collector_runtime.py
</context>

<must_haves>
<truth>DOC-11: collector authoring documentation reflects final v1.1 Pub/Sub post-bookmark publish semantics.</truth>
<truth>Do not edit `frontend/api/openapi.yaml`, `frontend/api/src/generated/routes.ts`, `frontend/common/src/types/feeds.ts`, `frontend/common/src/utils/statusUtils.ts`, `frontend/transcription-ui/src/components/common/FeedStatusIndicator.tsx`, or any DB migration/schema file.</truth>
<truth>`pipeline_publish_after_bookmark_failed` should be documented as a known post-bookmark publish gap that can consume feed quarantine budget after the existing threshold.</truth>
</must_haves>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Correct pipeline failure routing prose</name>
  <files>backend/pipeline/ingestion/collectors/README.md</files>
  <read_first>
    - backend/pipeline/ingestion/collectors/README.md
    - backend/pipeline/ingestion/failure_policy.py
    - backend/pipeline/ingestion/collector_runtime.py
    - .planning/phases/06-compatibility-and-verification/06-CONTEXT.md
  </read_first>
  <behavior>
    - Runtime-side `_PipelineFailure` is still separate from collector `FeedFailure`.
    - GCS upload and bookmark-write failures remain `system_pipeline_error` and non-budgeted.
    - Pub/Sub publish failure after successful bookmark uses `pipeline_publish_after_bookmark_failed`, records a known post-bookmark gap, and can quarantine after the existing feed failure threshold.
  </behavior>
  <action>
    In `backend/pipeline/ingestion/collectors/README.md`, update the paragraph around runtime-side `_PipelineFailure` so it states this exact target behavior:
    - runtime classifies post-capture failures through `failure_policy`
    - GCS upload and bookmark write failures retain `system_pipeline_error` and remain non-budgeted
    - Pub/Sub publish failures after a successful bookmark use `pipeline_publish_after_bookmark_failed`
    - the Pub/Sub case records `replay_missing=true` and `data_gap_known=true`
    - the Pub/Sub case can consume feed quarantine budget after the existing threshold because retry cannot repair the already-advanced bookmark/publish gap
  </action>
  <verify>
    <automated>rg -n "pipeline_publish_after_bookmark_failed|replay_missing|data_gap_known|existing threshold|system_pipeline_error" backend/pipeline/ingestion/collectors/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "must not quarantine the feed" backend/pipeline/ingestion/collectors/README.md` returns no matches.
    - `rg -n "pipeline_publish_after_bookmark_failed.*threshold|threshold.*pipeline_publish_after_bookmark_failed" backend/pipeline/ingestion/collectors/README.md` finds the updated Pub/Sub threshold wording.
    - `rg -n "replay_missing=true.*data_gap_known=true|data_gap_known=true.*replay_missing=true" backend/pipeline/ingestion/collectors/README.md` finds the known-gap telemetry wording.
    - `rg -n "system_pipeline_error" backend/pipeline/ingestion/collectors/README.md` still finds GCS/bookmark pipeline guidance.
  </acceptance_criteria>
  <done>The stale non-quarantine Pub/Sub wording is removed and replaced with final v1.1 semantics.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Document split system status reasons</name>
  <files>backend/pipeline/ingestion/collectors/README.md</files>
  <read_first>
    - backend/pipeline/ingestion/collectors/README.md
    - backend/pipeline/storage/feed_store.py
    - backend/pipeline/ingestion/failure_policy.py
    - .planning/phases/05-producer-and-runtime-routing-merge/05-CONTEXT.md
  </read_first>
  <behavior>
    - The collector guide's status reason table includes the three v1.1 split backend status reasons.
    - The wording preserves the Phase 4/5 distinctions: feed-row config, runtime config, credential access, upstream auth rejection, source payload invalid, and broad collector error.
  </behavior>
  <action>
    In the "Use system-owned reasons" table, add or update these exact reason rows:
    - `system_runtime_configuration_invalid` — shared runtime, deployment, environment, source-class, or transport configuration is invalid and retry is not expected to repair it.
    - `system_credential_access_failed` — Watch Duty could not retrieve or access internal credentials, such as Secret Manager access failure; this is not the same as upstream provider credential rejection.
    - `system_source_payload_invalid` — a successful source response violates the collector payload contract and repeating the same request is not expected to help.
    Preserve the existing `system_authentication_failed`, `system_configuration_invalid`, `system_collector_error`, `system_pipeline_error`, and `system_unexpected_error` rows with wording that does not conflict with the new split rows.
  </action>
  <verify>
    <automated>rg -n "system_runtime_configuration_invalid|system_credential_access_failed|system_source_payload_invalid" backend/pipeline/ingestion/collectors/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "system_runtime_configuration_invalid" backend/pipeline/ingestion/collectors/README.md` finds one system table row.
    - `rg -n "system_credential_access_failed" backend/pipeline/ingestion/collectors/README.md` finds one system table row.
    - `rg -n "system_source_payload_invalid" backend/pipeline/ingestion/collectors/README.md` finds one system table row.
    - `rg -n "upstream provider credential rejection|provider credential rejection|credentials, tokens, or partner auth are rejected" backend/pipeline/ingestion/collectors/README.md` confirms upstream auth rejection remains distinct from credential access failure.
  </acceptance_criteria>
  <done>The collector guide documents all current backend status reasons needed for v1.1 routing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer documentation -> future collector changes | Stale docs can cause future contributors to reintroduce incorrect routing behavior. |
| backend docs -> operator expectations | Incorrect quarantine wording can misrepresent when a feed may be quarantined. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Repudiation | collector guide | mitigate | Replace stale "must not quarantine" wording with thresholded budget semantics and known-gap telemetry fields. |
| T-06-01-02 | Tampering | status reason table | mitigate | Add explicit rows for the three split system statuses so broad `system_collector_error` guidance is not reused incorrectly. |
| T-06-01-03 | Information Disclosure | documentation examples | accept | Documentation update must not include raw credentials, signed URLs, payload bodies, or incident-private data. |
</threat_model>

<verification>
Run:

```bash
rg -n "must not quarantine the feed" backend/pipeline/ingestion/collectors/README.md
rg -n "pipeline_publish_after_bookmark_failed|replay_missing|data_gap_known|existing threshold|system_runtime_configuration_invalid|system_credential_access_failed|system_source_payload_invalid" backend/pipeline/ingestion/collectors/README.md
git diff --check
```

The first `rg` should return no matches. The second `rg` should find the final v1.1 documentation terms.
</verification>

<success_criteria>
- `pipeline_publish_after_bookmark_failed` no longer has stale "must not quarantine" guidance.
- The README documents thresholded budget semantics and known-gap telemetry for Pub/Sub post-bookmark publish failures.
- The README documents `system_runtime_configuration_invalid`, `system_credential_access_failed`, and `system_source_payload_invalid`.
- No frontend, OpenAPI, generated API metadata, DB migration, or schema file is modified.
</success_criteria>

<output>
After execution, create `.planning/phases/06-compatibility-and-verification/06-01-SUMMARY.md`.
</output>
