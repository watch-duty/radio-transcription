---
phase: 05-producer-and-runtime-routing-merge
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pipeline/ingestion/collector_runtime.py
  - backend/pipeline/ingestion/tests/test_collector_runtime.py
autonomous: true
requirements: [RUN-11, RUN-12, RUN-13, RUN-15, RUN-16, TEST-13]
user_setup: []
must_haves:
  truths:
    - "D-01: _PipelineFailure must use the same classify-and-execute policy branch shape as collector FeedFailure."
    - "D-02: runtime must not duplicate status-specific routing; the Phase 4 policy table remains routing authority."
    - "D-03: pipeline_publish_after_bookmark_failed with Pub/Sub publish-stage evidence calls report_feed_failure(...) and does not call release_non_budgeted_failure(...)."
    - "D-04: GCS upload and bookmark-write system_pipeline_error failures remain non-budgeted."
    - "D-06/D-16/D-17: the legacy post_bookmark_publish_failure event is not required for v1.1 routing; policy-decision telemetry preserves replay_missing/data_gap_known."
    - "D-07: feed_quarantined telemetry remains threshold-driven by report_feed_failure(...) returning quarantined."
    - "D-18: no frontend/OpenAPI/generated compatibility changes are included in Phase 5 runtime routing."
  artifacts:
    - path: backend/pipeline/ingestion/collector_runtime.py
      provides: "Shared policy execution for collector FeedFailure and runtime _PipelineFailure."
    - path: backend/pipeline/ingestion/tests/test_collector_runtime.py
      provides: "Runtime proof for Pub/Sub budgeted path, GCS/bookmark non-budgeted path, and thresholded quarantine telemetry."
  key_links:
    - from: backend/pipeline/ingestion/tests/test_collector_runtime.py
      to: backend/pipeline/ingestion/collector_runtime.py
      via: "report_feed_failure vs release_non_budgeted_failure assertions"
      pattern: "PIPELINE_PUBLISH_AFTER_BOOKMARK_FAILED|report_feed_failure|release_non_budgeted_failure"
---

<objective>
Route runtime `_PipelineFailure` through the Phase 4 policy decision table and update runtime tests for the v1.1 Pub/Sub post-bookmark behavior.

Purpose: post-capture runtime failures must no longer bypass policy decisions; only the Pub/Sub post-bookmark publish route should consume the feed failure budget, while GCS/bookmark pipeline failures remain non-budgeted.
Output: a shared `_PipelineFailure` execution branch and focused runtime tests proving budgeted and non-budgeted store calls.
</objective>

<execution_context>
@/home/shuojing/.codex/get-shit-done/workflows/execute-plan.md
@/home/shuojing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-producer-and-runtime-routing-merge/05-CONTEXT.md
@.planning/phases/05-producer-and-runtime-routing-merge/05-RESEARCH.md
@.planning/phases/05-producer-and-runtime-routing-merge/05-PATTERNS.md
@.planning/phases/04-strict-policy-table-and-status-vocabulary/04-VERIFICATION.md
@backend/pipeline/ingestion/failure_policy.py
@backend/pipeline/ingestion/collector_runtime.py
@backend/pipeline/ingestion/tests/test_collector_runtime.py
@backend/pipeline/storage/feed_store.py
</context>

<must_haves>
<truth>RUN-11: `_PipelineFailure` uses the same policy branch as collector `FeedFailure`.</truth>
<truth>RUN-12/TEST-13: Pub/Sub publish-after-bookmark failure calls `report_feed_failure(...)` through the budgeted policy route.</truth>
<truth>RUN-13: GCS upload and bookmark-write failures remain non-budgeted through `release_non_budgeted_failure(...)`.</truth>
<truth>RUN-15: Budgeted Pub/Sub failures still use `feed_failure_threshold`; no immediate quarantine occurs before the store returns `quarantined`.</truth>
<truth>RUN-16: The old special `post_bookmark_publish_failure` event is not required for v1.1 behavior.</truth>
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing runtime tests for _PipelineFailure policy execution</name>
  <files>backend/pipeline/ingestion/tests/test_collector_runtime.py</files>
  <read_first>
    - .planning/phases/05-producer-and-runtime-routing-merge/05-CONTEXT.md
    - .planning/phases/05-producer-and-runtime-routing-merge/05-RESEARCH.md
    - backend/pipeline/ingestion/collector_runtime.py
    - backend/pipeline/ingestion/tests/test_collector_runtime.py
    - backend/pipeline/ingestion/failure_policy.py
  </read_first>
  <behavior>
    - Existing Pub/Sub post-bookmark publish failure tests now expect `rt._store.report_feed_failure.assert_awaited_once()`.
    - Pub/Sub post-bookmark publish failure tests expect `rt._store.release_non_budgeted_failure.assert_not_awaited()`.
    - Pub/Sub post-bookmark publish failure tests assert `report_feed_failure` receives `threshold=rt._collector_settings.feed_failure_threshold`, `status_reason=FeedStatusReason.PIPELINE_PUBLISH_AFTER_BOOKMARK_FAILED`, and the original reason from `pubsub.publish_failure_reason(exc)`.
    - When `report_feed_failure.return_value = "failing"`, `quarantine_telemetry.emit_quarantine_event` is not awaited.
    - When `report_feed_failure.return_value = "quarantined"`, `quarantine_telemetry.emit_quarantine_event` is awaited once.
    - Runtime log assertions check the canonical `feed_failure_policy_decision` event contains `replay_missing=True`, `data_gap_known=True`, `policy_intent="quarantine_feed"`, `executed_action="increment_feed_failure_budget"`, and Pub/Sub publish evidence fields.
    - Tests no longer require the legacy `post_bookmark_publish_failure` event for v1.1 acceptance.
    - Existing GCS upload and bookmark-write tests continue to expect non-budgeted release.
  </behavior>
  <action>
    Update existing runtime tests around `pipeline_publish_after_bookmark_failed` rather than adding duplicate old/new behavior tests. Add threshold and quarantine telemetry assertions so budgeted does not mean immediate quarantine before the store threshold is crossed.
  </action>
  <verify>
    <automated>safe-run -- uv run python -m pytest backend/pipeline/ingestion/tests/test_collector_runtime.py -q -n 0</automated>
    <expected_before_fix>The updated Pub/Sub post-bookmark tests fail before runtime implementation because `_PipelineFailure` still unconditionally calls `release_non_budgeted_failure(...)`.</expected_before_fix>
  </verify>
  <acceptance_criteria>
    - `rg -n "PIPELINE_PUBLISH_AFTER_BOOKMARK_FAILED|report_feed_failure|release_non_budgeted_failure|replay_missing|data_gap_known" backend/pipeline/ingestion/tests/test_collector_runtime.py` finds updated runtime assertions.
    - `rg -n "post_bookmark_publish_failure" backend/pipeline/ingestion/tests/test_collector_runtime.py` does not appear in a required assertion for Pub/Sub v1.1 routing.
    - The targeted runtime pytest command fails before implementation for the Pub/Sub budget route.
  </acceptance_criteria>
  <done>Runtime tests define v1.1 policy execution for `_PipelineFailure`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement shared _PipelineFailure policy execution</name>
  <files>backend/pipeline/ingestion/collector_runtime.py</files>
  <read_first>
    - backend/pipeline/ingestion/collector_runtime.py
    - backend/pipeline/ingestion/failure_policy.py
    - backend/pipeline/ingestion/tests/test_collector_runtime.py
  </read_first>
  <behavior>
    - `_PipelineFailure` catch arm calls `failure_policy.classify_failure_policy(e.status_reason, e.policy_evidence)`.
    - If `self._is_feed_quarantine_decision(decision)` is true, runtime calls `_record_feed_failure(...)`.
    - If the decision is not feed quarantine, runtime calls `_record_non_budgeted_failure(...)`.
    - Pub/Sub post-bookmark publish evidence sets `replay_missing=True` and `data_gap_known=True` on the canonical policy decision log even when using `_record_feed_failure(...)`.
    - `post_bookmark_publish_failure` may remain as supplemental code only if no tests require it for v1.1 routing; do not use it as routing authority.
    - No storage API signature changes are required.
  </behavior>
  <action>
    Change the `_PipelineFailure` catch arm to mirror the collector `FeedFailure` branch. Add optional `replay_missing: bool = False` and `data_gap_known: bool = False` parameters to `_record_feed_failure(...)`, pass them to `_emit_policy_decision(...)`, and set them from the decision/evidence for `FeedStatusReason.PIPELINE_PUBLISH_AFTER_BOOKMARK_FAILED` with Pub/Sub publish-stage evidence. Keep `_record_non_budgeted_failure(...)` behavior unchanged for GCS/bookmark and telemetry-gap routes.
  </action>
  <verify>
    <automated>safe-run -- uv run python -m pytest backend/pipeline/ingestion/tests/test_collector_runtime.py -q -n 0</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "except _PipelineFailure|_is_feed_quarantine_decision|_record_feed_failure|_record_non_budgeted_failure" backend/pipeline/ingestion/collector_runtime.py` shows the pipeline catch arm branches by policy decision.
    - `rg -n "def _record_feed_failure\\(|replay_missing|data_gap_known" backend/pipeline/ingestion/collector_runtime.py` shows `_record_feed_failure(...)` can preserve data-gap fields in policy telemetry.
    - `rg -n "status_reason == FeedStatusReason.PIPELINE_PUBLISH_AFTER_BOOKMARK_FAILED|if e.status_reason is FeedStatusReason.PIPELINE_PUBLISH_AFTER_BOOKMARK_FAILED" backend/pipeline/ingestion/collector_runtime.py` does not find status-specific runtime routing outside small replay/data-gap flag calculation.
    - The targeted runtime pytest command passes.
  </acceptance_criteria>
  <done>`_PipelineFailure` executes the same policy branch as collector `FeedFailure`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify thresholded quarantine telemetry for budgeted pipeline failures</name>
  <files>backend/pipeline/ingestion/tests/test_collector_runtime.py</files>
  <read_first>
    - backend/pipeline/ingestion/collector_runtime.py
    - backend/pipeline/ingestion/tests/test_collector_runtime.py
    - backend/pipeline/ingestion/failure_policy.py
  </read_first>
  <behavior>
    - A Pub/Sub post-bookmark failure whose `report_feed_failure(...)` result is `"failing"` does not emit `feed_quarantined`.
    - A Pub/Sub post-bookmark failure whose `report_feed_failure(...)` result is `"quarantined"` emits `feed_quarantined` exactly once with `status_reason="pipeline_publish_after_bookmark_failed"`.
    - The policy decision event is emitted for both cases.
  </behavior>
  <action>
    Add or update runtime tests to cover both store return values for the budgeted Pub/Sub route. Keep assertions centered on store method calls and quarantine telemetry, not the legacy special publish-gap event.
  </action>
  <verify>
    <automated>safe-run -- uv run python -m pytest backend/pipeline/ingestion/tests/test_collector_runtime.py -q -n 0</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "return_value = \"failing\"|return_value = \"quarantined\"|emit_quarantine_event" backend/pipeline/ingestion/tests/test_collector_runtime.py` finds both threshold cases.
    - The targeted runtime pytest command passes.
  </acceptance_criteria>
  <done>Runtime tests prove budgeted pipeline failures respect the existing threshold path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime side-effect failure -> feed store | `_PipelineFailure` can now consume feed budget only through policy decisions. |
| publish after bookmark -> operational telemetry | A known data gap must remain visible even though v1.1 routes through feed budget. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | `_PipelineFailure` routing | mitigate | Route via `classify_failure_policy(...)` and test store calls for budgeted/non-budgeted decisions. |
| T-05-02-02 | Repudiation | post-bookmark data gap | mitigate | Preserve `replay_missing` and `data_gap_known` in canonical policy telemetry. |
| T-05-02-03 | Denial of Service | immediate mass quarantine | mitigate | Keep `report_feed_failure(...)` threshold behavior and emit quarantine telemetry only on `"quarantined"`. |
| T-05-02-04 | Information Disclosure | publish failure reasons | mitigate | Preserve bounded runtime reason strings; do not log payload bodies or credentials. |
</threat_model>

<verification>
Run:

```bash
safe-run -- uv run python -m pytest backend/pipeline/ingestion/tests/test_collector_runtime.py -q -n 0
```

Do not run broad local E2E, Docker, testcontainers, OpenAPI generation, frontend builds, or generated API checks for this plan.
</verification>

<success_criteria>
- RUN-11, RUN-12, RUN-13, RUN-15, RUN-16, and TEST-13 are satisfied by runtime tests.
- Pub/Sub post-bookmark publish failures call `report_feed_failure(...)` with thresholded behavior.
- GCS upload and bookmark-write failures remain non-budgeted.
- Runtime routing authority remains `failure_policy.classify_failure_policy(...)`, not telemetry or raw reason strings.
</success_criteria>

<output>
After execution, create `.planning/phases/05-producer-and-runtime-routing-merge/05-02-SUMMARY.md`.
</output>
