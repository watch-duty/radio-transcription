---
phase: 05-producer-and-runtime-routing-merge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pipeline/ingestion/collectors/bcfy_calls/bcfy_calls_collector.py
  - backend/pipeline/ingestion/collectors/tests/test_bcfy_calls_collector.py
  - backend/pipeline/ingestion/collectors/fire_notifications/collector.py
  - backend/pipeline/ingestion/collectors/tests/test_fire_notifications_collector.py
  - backend/pipeline/ingestion/collectors/icecast/icecast_collector.py
  - backend/pipeline/ingestion/collectors/tests/test_icecast_collector.py
  - backend/pipeline/ingestion/collectors/openmhz/collector.py
  - backend/pipeline/ingestion/collectors/tests/test_openmhz_collector.py
  - backend/pipeline/ingestion/collectors/payloads.py
  - backend/pipeline/ingestion/collectors/tests/test_aiohttp_requests.py
autonomous: true
requirements: [TEST-15]
user_setup: []
must_haves:
  truths:
    - "D-08: system_configuration_invalid remains feed-row or source-specific feed configuration invalid, including missing source_feed_id and invalid selected OpenMHz transport."
    - "D-09: shared runtime/deploy/env config failures produce system_runtime_configuration_invalid."
    - "D-10: internal credential retrieval/access failures produce system_credential_access_failed."
    - "D-11: upstream/provider auth rejection remains system_authentication_failed."
    - "D-12: malformed successful source payloads produce system_source_payload_invalid."
    - "D-13: ambiguous mixed item failures and ffmpeg process fallback remain system_collector_error."
    - "D-14/D-15/D-18: use direct substitutions or narrow helper defaults, with focused backend tests and no frontend/OpenAPI/generated changes."
  artifacts:
    - path: backend/pipeline/ingestion/collectors/bcfy_calls/bcfy_calls_collector.py
      provides: "Broadcastify Calls split producer statuses for JWT config, credential access, and malformed source payloads."
    - path: backend/pipeline/ingestion/collectors/fire_notifications/collector.py
      provides: "Fire Notifications split producer statuses for shared runtime config and malformed source payloads."
    - path: backend/pipeline/ingestion/collectors/icecast/icecast_collector.py
      provides: "Icecast split producer status for shared Broadcastify credential config."
    - path: backend/pipeline/ingestion/collectors/openmhz/collector.py
      provides: "OpenMHz split producer status for invalid source-provided media URL payloads."
    - path: backend/pipeline/ingestion/collectors/payloads.py
      provides: "Shared successful-payload shape failures route to system_source_payload_invalid."
  key_links:
    - from: backend/pipeline/ingestion/collectors/tests/test_bcfy_calls_collector.py
      to: backend/pipeline/ingestion/collectors/bcfy_calls/bcfy_calls_collector.py
      via: "JWT config/access and Calls API payload assertions"
      pattern: "calls_jwt_config_missing|calls_jwt_secret_access_failed|calls_api_response_invalid"
    - from: backend/pipeline/ingestion/collectors/tests/test_fire_notifications_collector.py
      to: backend/pipeline/ingestion/collectors/fire_notifications/collector.py
      via: "Fire env/auth config and payload assertions"
      pattern: "missing_fire_notifications_s3_base|missing_fire_notifications_auth_config|fn_api_payload_malformed"
    - from: backend/pipeline/ingestion/collectors/tests/test_icecast_collector.py
      to: backend/pipeline/ingestion/collectors/icecast/icecast_collector.py
      via: "missing Broadcastify credential config assertion"
      pattern: "missing_broadcastify_credentials"
    - from: backend/pipeline/ingestion/collectors/tests/test_openmhz_collector.py
      to: backend/pipeline/ingestion/collectors/openmhz/collector.py
      via: "invalid media URL source payload assertion"
      pattern: "invalid_openmhz_media_url|_INVALID_OPENMHZ_MEDIA_URL_REASON"
---

<objective>
Split current collector producer mappings so Calls, Fire Notifications, Icecast, and OpenMHz emit the precise Phase 4 backend status reasons for agreed root causes.

Purpose: Phase 5 must stop broad producer labels from hiding clear routing evidence before runtime executes the policy table.
Output: focused collector/helper tests plus producer code changes for `system_runtime_configuration_invalid`, `system_credential_access_failed`, and `system_source_payload_invalid`, while preserving intentionally broad fallback cases.
</objective>

<execution_context>
@/home/shuojing/.codex/get-shit-done/workflows/execute-plan.md
@/home/shuojing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-producer-and-runtime-routing-merge/05-CONTEXT.md
@.planning/phases/05-producer-and-runtime-routing-merge/05-RESEARCH.md
@.planning/phases/05-producer-and-runtime-routing-merge/05-PATTERNS.md
@backend/pipeline/ingestion/collectors/failure_classification.py
@backend/pipeline/ingestion/failure_policy.py
@backend/pipeline/storage/feed_store.py

<interfaces>
Use existing producer helpers:

```python
collector_failure(status_reason, reason, policy_evidence=...)
policy_evidence_for_status_reason(status_reason, failure_scope=..., endpoint_kind=...)
```

The exact status targets for this plan are:

```python
FeedStatusReason.SYSTEM_RUNTIME_CONFIGURATION_INVALID
FeedStatusReason.SYSTEM_CREDENTIAL_ACCESS_FAILED
FeedStatusReason.SYSTEM_SOURCE_PAYLOAD_INVALID
```
</interfaces>
</context>

<must_haves>
<truth>TEST-15: Calls, Fire Notifications, Icecast, and OpenMHz tests must prove the new split enum values are produced for currently agreed root causes.</truth>
<truth>D-08: Missing feed `source_feed_id` and invalid feed-selected transport remain `system_configuration_invalid`.</truth>
<truth>D-09: Missing shared runtime/env/auth config produces `system_runtime_configuration_invalid`.</truth>
<truth>D-10: Internal credential retrieval/access failure produces `system_credential_access_failed` and remains non-budgeted by policy.</truth>
<truth>D-11: Upstream/provider 401/403 rejection remains `system_authentication_failed`.</truth>
<truth>D-12: Malformed successful source payloads and source-provided invalid media URLs produce `system_source_payload_invalid`.</truth>
<truth>D-13: Mixed item failures, duration probe failures, and ffmpeg process fallback remain `system_collector_error`.</truth>
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing producer split tests</name>
  <files>backend/pipeline/ingestion/collectors/tests/test_bcfy_calls_collector.py, backend/pipeline/ingestion/collectors/tests/test_fire_notifications_collector.py, backend/pipeline/ingestion/collectors/tests/test_icecast_collector.py, backend/pipeline/ingestion/collectors/tests/test_openmhz_collector.py, backend/pipeline/ingestion/collectors/tests/test_aiohttp_requests.py</files>
  <read_first>
    - .planning/phases/05-producer-and-runtime-routing-merge/05-CONTEXT.md
    - .planning/phases/05-producer-and-runtime-routing-merge/05-PATTERNS.md
    - backend/pipeline/ingestion/collectors/tests/test_bcfy_calls_collector.py
    - backend/pipeline/ingestion/collectors/tests/test_fire_notifications_collector.py
    - backend/pipeline/ingestion/collectors/tests/test_icecast_collector.py
    - backend/pipeline/ingestion/collectors/tests/test_openmhz_collector.py
    - backend/pipeline/ingestion/collectors/tests/test_aiohttp_requests.py
    - backend/pipeline/storage/feed_store.py
  </read_first>
  <behavior>
    - Broadcastify Calls `_get_jwt_token()` missing `GOOGLE_CLOUD_PROJECT` or `BROADCASTIFY_JWT_SECRET_ID` raises `FeedStatusReason.SYSTEM_RUNTIME_CONFIGURATION_INVALID` with reason `calls_jwt_config_missing`.
    - Broadcastify Calls Secret Manager access error raises `FeedStatusReason.SYSTEM_CREDENTIAL_ACCESS_FAILED` with reason `calls_jwt_secret_access_failed`.
    - Broadcastify Calls invalid successful API payload raises `FeedStatusReason.SYSTEM_SOURCE_PAYLOAD_INVALID` with reason prefix `calls_api_response_invalid`.
    - Fire Notifications missing `FIRE_NOTIFICATIONS_S3_BASE`, `FIRE_NOTIFICATIONS_API_KEY`, or `FIRE_NOTIFICATIONS_API_SECRET` raises `FeedStatusReason.SYSTEM_RUNTIME_CONFIGURATION_INVALID`.
    - Fire Notifications malformed successful JSON/payload raises `FeedStatusReason.SYSTEM_SOURCE_PAYLOAD_INVALID` with reason prefix `fn_api_payload_malformed`.
    - Icecast missing `BROADCASTIFY_USERNAME` or `BROADCASTIFY_PASSWORD` raises `FeedStatusReason.SYSTEM_RUNTIME_CONFIGURATION_INVALID` with reason `missing_broadcastify_credentials`.
    - OpenMHz invalid source-provided media URL returns or promotes `FeedStatusReason.SYSTEM_SOURCE_PAYLOAD_INVALID` with reason `invalid_openmhz_media_url`.
    - Existing assertions for `missing_source_feed_id`, invalid OpenMHz transport, mixed item failures, and ffmpeg fallback continue to expect `SYSTEM_CONFIGURATION_INVALID` or `SYSTEM_COLLECTOR_ERROR` as currently appropriate.
  </behavior>
  <action>
    Update existing collector tests before changing producer code. Replace old expected statuses only for the exact cases listed above. Add evidence assertions where helpers already expose `policy_evidence`: `SYSTEM_RUNTIME_CONFIGURATION_INVALID` should map to `OwnerScope.SOURCE_CLASS`, `SYSTEM_CREDENTIAL_ACCESS_FAILED` to `OwnerScope.CREDENTIAL_SCOPE`, and `SYSTEM_SOURCE_PAYLOAD_INVALID` to `OwnerScope.SOURCE_CLASS`.
  </action>
  <verify>
    <automated>safe-run -- uv run python -m pytest backend/pipeline/ingestion/collectors/tests/test_bcfy_calls_collector.py backend/pipeline/ingestion/collectors/tests/test_fire_notifications_collector.py backend/pipeline/ingestion/collectors/tests/test_icecast_collector.py backend/pipeline/ingestion/collectors/tests/test_openmhz_collector.py backend/pipeline/ingestion/collectors/tests/test_aiohttp_requests.py -q -n 0</automated>
    <expected_before_fix>The updated expectations fail before producer code changes.</expected_before_fix>
  </verify>
  <acceptance_criteria>
    - `rg -n "SYSTEM_RUNTIME_CONFIGURATION_INVALID|SYSTEM_CREDENTIAL_ACCESS_FAILED|SYSTEM_SOURCE_PAYLOAD_INVALID" backend/pipeline/ingestion/collectors/tests/test_bcfy_calls_collector.py backend/pipeline/ingestion/collectors/tests/test_fire_notifications_collector.py backend/pipeline/ingestion/collectors/tests/test_icecast_collector.py backend/pipeline/ingestion/collectors/tests/test_openmhz_collector.py backend/pipeline/ingestion/collectors/tests/test_aiohttp_requests.py` finds split-status test expectations.
    - `rg -n "missing_source_feed_id|invalid_openmhz_transport|mixed_item_failures|ffmpeg" backend/pipeline/ingestion/collectors/tests/test_icecast_collector.py backend/pipeline/ingestion/collectors/tests/test_openmhz_collector.py backend/pipeline/ingestion/collectors/tests/test_fire_notifications_collector.py backend/pipeline/ingestion/collectors/tests/test_bcfy_calls_collector.py` still finds guard coverage for intentionally unchanged broad/config cases.
    - The targeted pytest command fails before implementation for at least one changed expectation.
  </acceptance_criteria>
  <done>Tests define every producer split required by TEST-15.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement collector and helper status splits</name>
  <files>backend/pipeline/ingestion/collectors/bcfy_calls/bcfy_calls_collector.py, backend/pipeline/ingestion/collectors/fire_notifications/collector.py, backend/pipeline/ingestion/collectors/icecast/icecast_collector.py, backend/pipeline/ingestion/collectors/openmhz/collector.py, backend/pipeline/ingestion/collectors/payloads.py</files>
  <read_first>
    - backend/pipeline/ingestion/collectors/bcfy_calls/bcfy_calls_collector.py
    - backend/pipeline/ingestion/collectors/fire_notifications/collector.py
    - backend/pipeline/ingestion/collectors/icecast/icecast_collector.py
    - backend/pipeline/ingestion/collectors/openmhz/collector.py
    - backend/pipeline/ingestion/collectors/payloads.py
    - backend/pipeline/ingestion/collectors/failure_classification.py
    - backend/pipeline/ingestion/failure_policy.py
  </read_first>
  <behavior>
    - Calls JWT env config uses `SYSTEM_RUNTIME_CONFIGURATION_INVALID`.
    - Calls Secret Manager access uses `SYSTEM_CREDENTIAL_ACCESS_FAILED`.
    - Calls API invalid payload uses `SYSTEM_SOURCE_PAYLOAD_INVALID`.
    - Fire shared env/auth config uses `SYSTEM_RUNTIME_CONFIGURATION_INVALID`.
    - Fire malformed successful payload uses `SYSTEM_SOURCE_PAYLOAD_INVALID`.
    - Icecast shared Broadcastify credential config uses `SYSTEM_RUNTIME_CONFIGURATION_INVALID`.
    - OpenMHz invalid source-provided media URL uses `SYSTEM_SOURCE_PAYLOAD_INVALID`.
    - `missing_source_feed_id_failure()` remains unchanged.
    - Provider/upstream 401/403 status mappings remain `SYSTEM_AUTHENTICATION_FAILED`.
    - Broad ambiguous fallbacks remain `SYSTEM_COLLECTOR_ERROR`.
  </behavior>
  <action>
    Make direct status substitutions at the producer sites. In `payloads.extract_optional_item_list(...)`, change malformed source payload failures from `FeedStatusReason.SYSTEM_COLLECTOR_ERROR` to `FeedStatusReason.SYSTEM_SOURCE_PAYLOAD_INVALID` while preserving caller-supplied `failure_scope`, `endpoint_kind`, and `malformed_reason`. Do not add new enum values, new evidence fields, or `reason_family`.
  </action>
  <verify>
    <automated>safe-run -- uv run python -m pytest backend/pipeline/ingestion/collectors/tests/test_bcfy_calls_collector.py backend/pipeline/ingestion/collectors/tests/test_fire_notifications_collector.py backend/pipeline/ingestion/collectors/tests/test_icecast_collector.py backend/pipeline/ingestion/collectors/tests/test_openmhz_collector.py backend/pipeline/ingestion/collectors/tests/test_aiohttp_requests.py -q -n 0</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "SYSTEM_RUNTIME_CONFIGURATION_INVALID" backend/pipeline/ingestion/collectors/bcfy_calls/bcfy_calls_collector.py backend/pipeline/ingestion/collectors/fire_notifications/collector.py backend/pipeline/ingestion/collectors/icecast/icecast_collector.py` finds runtime config producers.
    - `rg -n "SYSTEM_CREDENTIAL_ACCESS_FAILED" backend/pipeline/ingestion/collectors/bcfy_calls/bcfy_calls_collector.py` finds the Secret Manager access producer.
    - `rg -n "SYSTEM_SOURCE_PAYLOAD_INVALID" backend/pipeline/ingestion/collectors/bcfy_calls/bcfy_calls_collector.py backend/pipeline/ingestion/collectors/fire_notifications/collector.py backend/pipeline/ingestion/collectors/openmhz/collector.py backend/pipeline/ingestion/collectors/payloads.py` finds source-payload producers/helpers.
    - The targeted pytest command passes.
  </acceptance_criteria>
  <done>Producer code emits split status reasons for all agreed current root causes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify producer split boundaries</name>
  <files>backend/pipeline/ingestion/collectors/tests/test_bcfy_calls_collector.py, backend/pipeline/ingestion/collectors/tests/test_fire_notifications_collector.py, backend/pipeline/ingestion/collectors/tests/test_icecast_collector.py, backend/pipeline/ingestion/collectors/tests/test_openmhz_collector.py, backend/pipeline/ingestion/collectors/tests/test_aiohttp_requests.py</files>
  <read_first>
    - .planning/phases/05-producer-and-runtime-routing-merge/05-CONTEXT.md
    - .planning/phases/05-producer-and-runtime-routing-merge/05-RESEARCH.md
    - backend/pipeline/ingestion/collectors/tests/test_bcfy_calls_collector.py
    - backend/pipeline/ingestion/collectors/tests/test_fire_notifications_collector.py
    - backend/pipeline/ingestion/collectors/tests/test_icecast_collector.py
    - backend/pipeline/ingestion/collectors/tests/test_openmhz_collector.py
  </read_first>
  <behavior>
    - The producer split does not touch runtime routing, storage SQL, API/OpenAPI, frontend, generated files, replay, breaker state, or database migrations.
    - Focused tests prove both changed and intentionally unchanged producer cases.
  </behavior>
  <action>
    Run the producer-focused tests and inspect the diff. If a split would require changing the Phase 4 policy table, stop and document the mismatch instead of broadening producer evidence ad hoc.
  </action>
  <verify>
    <automated>safe-run -- uv run python -m pytest backend/pipeline/ingestion/collectors/tests/test_bcfy_calls_collector.py backend/pipeline/ingestion/collectors/tests/test_fire_notifications_collector.py backend/pipeline/ingestion/collectors/tests/test_icecast_collector.py backend/pipeline/ingestion/collectors/tests/test_openmhz_collector.py backend/pipeline/ingestion/collectors/tests/test_aiohttp_requests.py backend/pipeline/ingestion/collectors/tests/test_failure_classification.py backend/pipeline/ingestion/tests/test_failure_policy.py -q -n 0</automated>
  </verify>
  <acceptance_criteria>
    - Producer-focused pytest command passes.
    - `git diff --name-only` for this plan contains only files listed in this plan's `files_modified`.
    - No OpenAPI/frontend/generated/DB migration files are modified.
  </acceptance_criteria>
  <done>Plan 05-01 proves producer split mappings without broadening Phase 5 scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source evidence -> collector status | Collector labels determine which policy route runtime can execute later. |
| internal config/credential access -> persisted status_reason | Internal failures may surface to operators through feed status fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | producer status mappings | mitigate | Add RED tests for exact status values before changing producer code. |
| T-05-01-02 | Repudiation | root-cause attribution | mitigate | Preserve stable reason strings such as `calls_jwt_secret_access_failed`, `fn_api_payload_malformed`, and `missing_broadcastify_credentials`. |
| T-05-01-03 | Denial of Service | accidental mass quarantine from over-splitting | mitigate | Keep ambiguous mixed item/ffmpeg fallbacks as `system_collector_error` and verify unchanged cases. |
| T-05-01-04 | Information Disclosure | credential/config failures | mitigate | Tests assert enum/reason only; do not log secret values or raw payload bodies. |
</threat_model>

<verification>
Run:

```bash
safe-run -- uv run python -m pytest backend/pipeline/ingestion/collectors/tests/test_bcfy_calls_collector.py backend/pipeline/ingestion/collectors/tests/test_fire_notifications_collector.py backend/pipeline/ingestion/collectors/tests/test_icecast_collector.py backend/pipeline/ingestion/collectors/tests/test_openmhz_collector.py backend/pipeline/ingestion/collectors/tests/test_aiohttp_requests.py backend/pipeline/ingestion/collectors/tests/test_failure_classification.py backend/pipeline/ingestion/tests/test_failure_policy.py -q -n 0
```

Do not run broad local E2E, Docker, testcontainers, OpenAPI generation, frontend builds, or generated API checks for this plan.
</verification>

<success_criteria>
- TEST-15 is satisfied for Calls, Fire Notifications, Icecast, and OpenMHz.
- New split producer values are emitted only for currently agreed root causes.
- Existing feed-row configuration, provider auth rejection, mixed item, and ffmpeg fallback semantics remain intact.
- No runtime `_PipelineFailure` routing changes are made by this plan.
</success_criteria>

<output>
After execution, create `.planning/phases/05-producer-and-runtime-routing-merge/05-01-SUMMARY.md`.
</output>
