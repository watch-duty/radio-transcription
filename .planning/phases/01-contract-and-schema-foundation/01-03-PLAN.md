---
phase: 01-contract-and-schema-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - backend/pipeline/storage/tests/test_feed_audit_contract.py
autonomous: true
requirements: [AUD-02, AUD-03, DIAG-01, ACT-01, DOC-01, DOC-02, DOC-03]
user_setup: []
must_haves:
  truths:
    - "AUD-02/D-01/D-02/D-03/D-04/D-05: local tests prove deleted-feed audit history is documented and the SQL has no cascade to feeds."
    - "AUD-03: local tests prove the SQL names occurred_at, feed_sequence, and feed_audit_events_feed_sequence_unique."
    - "DIAG-01/D-14/D-15/D-16/D-17: local tests prove status_reason_detail is documented, bounded to 2048, and HOT-guarded."
    - "ACT-01/D-06/D-07/D-08/D-09/D-10/D-11/D-12/D-13: local tests prove the actor_id vocabulary is documented, constrained in SQL, rejects empty namespaced suffixes, and accepts unknown:unknown."
    - "DOC-01/DOC-02/DOC-03/D-18/D-19/D-20: local tests prove the documentation includes action vocabulary, consumer derivation, and terminology."
  artifacts:
    - path: "backend/pipeline/storage/tests/test_feed_audit_contract.py"
      provides: "Text-level contract tests for documentation and SQL artifacts"
  key_links:
    - from: "backend/pipeline/storage/tests/test_feed_audit_contract.py"
      to: "documentation/feed-audit-events.md"
      via: "pathlib text assertions"
      pattern: "documentation/feed-audit-events.md"
    - from: "backend/pipeline/storage/tests/test_feed_audit_contract.py"
      to: "terraform/modules/alloydb/sql/ingestion/029_feed_audit_events.sql"
      via: "pathlib SQL assertions without a live database"
      pattern: "029_feed_audit_events.sql"
    - from: "backend/pipeline/storage/tests/test_feed_audit_contract.py"
      to: "terraform/modules/alloydb/sql/ci/hot_protection_check.sql"
      via: "HOT guard text assertion"
      pattern: "status_reason_detail"
---

<objective>
Add focused local verification for the Phase 1 contract and schema artifacts.

Purpose: Executors and reviewers need automated checks that catch contract drift
without starting AlloyDB, Docker, component tests, API tests, or E2E lanes.

Output: a narrow text-level pytest module that validates the documentation,
migration, and HOT guard.
</objective>

<execution_context>
@/home/shuojing/.codex/get-shit-done/workflows/execute-plan.md
@/home/shuojing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-contract-and-schema-foundation/01-CONTEXT.md
@.planning/phases/01-contract-and-schema-foundation/01-RESEARCH.md
@.planning/phases/01-contract-and-schema-foundation/01-PATTERNS.md
@documentation/feed-audit-events.md
@CONTEXT.md
@terraform/modules/alloydb/sql/ingestion/029_feed_audit_events.sql
@terraform/modules/alloydb/sql/ci/hot_protection_check.sql
@backend/pipeline/storage/tests/test_feed_store.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create text-level contract tests</name>
  <files>backend/pipeline/storage/tests/test_feed_audit_contract.py</files>
  <read_first>
    - `documentation/feed-audit-events.md`
    - `CONTEXT.md`
    - `terraform/modules/alloydb/sql/ingestion/029_feed_audit_events.sql`
    - `terraform/modules/alloydb/sql/ci/hot_protection_check.sql`
    - `backend/pipeline/storage/tests/test_feed_store.py`
    - `.planning/phases/01-contract-and-schema-foundation/01-PATTERNS.md`
    - `.agents/instructions.md`
  </read_first>
  <action>
    Create `backend/pipeline/storage/tests/test_feed_audit_contract.py` as a
    narrow Python pytest module using only standard-library `pathlib` and `re`.
    Do not import asyncpg, psycopg, Docker/testcontainers, application stores,
    or service modules. Define helper functions `_read(path: str) -> str` and
    `_sql_without_comments(text: str) -> str` so banned SQL checks ignore
    comment-only prose.

    Add `test_documentation_defines_feed_audit_event_contract()` asserting
    `documentation/feed-audit-events.md` contains all action strings
    `feed.created`, `feed.updated`, `feed.deactivated`, `feed.reset`,
    `feed.deleted`, `feed.failure_reported`, `feed.quarantined`,
    `feed.recovered`; all actor strings `user:google:`, `user-email:`,
    `service:`, `system:`, `job:`, `gcp-sa:`, `unknown:unknown`; and the exact
    terms `feed_audit_events`, `before_values`, `after_values`, `occurred_at`,
    `feed_sequence`, `status_reason_detail`, `quarantine_reason`, `18 months`,
    `Watch Duty`, and `admin timeline`.

    Add `test_repository_glossary_defines_audit_terms()` asserting `CONTEXT.md`
    contains the headings `### Current Feed State`, `### Audit History`,
    `### Feed Audit Event`, `### Status Reason Detail`, and `### Actor ID`.

    Add `test_migration_defines_delete_safe_audit_schema()` asserting
    `029_feed_audit_events.sql` contains `CREATE TABLE IF NOT EXISTS
    feed_audit_events`, `feed_id UUID NOT NULL`, `feed_name VARCHAR(255) NOT
    NULL`, `source_type TEXT NOT NULL REFERENCES source_types(slug)`,
    `before_values JSONB NOT NULL DEFAULT '{}'::jsonb`, `after_values JSONB NOT
    NULL DEFAULT '{}'::jsonb`, `feed_sequence BIGINT NOT NULL`,
    `occurred_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW()`, and
    `feed_audit_events_feed_sequence_unique`. Assert the comment-stripped SQL
    does not match `REFERENCES\s+feeds`, `ON\s+DELETE\s+CASCADE`, `pg_cron`,
    `dispatcher`, `webhook`, or `DROP\s+COLUMN\s+quarantine_reason`.

    Add `test_migration_defines_actor_and_action_constraints()` asserting the
    migration contains `feed_audit_events_action_check`,
    `feed_audit_events_actor_id_check`, every action string, and every actor
    prefix string listed above.

    Add `test_migration_rejects_empty_actor_id_suffixes()` asserting the
    migration accepts `unknown:unknown` exactly and rejects empty suffix
    namespaced actor IDs. The test must explicitly list these rejected
    examples: `user:google:`, `service:`, `system:`, `job:`, `gcp-sa:`, and
    `user-email:`. Because this is a text-level test, prove rejection by
    asserting the `feed_audit_events_actor_id_check` constraint pairs each
    namespaced `LIKE` prefix with a non-empty suffix check using
    `char_length(actor_id) > char_length('<prefix>')`; do not accept a bare
    `LIKE '<prefix>%'` branch as sufficient.

    Add `test_migration_defines_status_reason_detail_and_hot_guard()` asserting
    the migration contains `ADD COLUMN IF NOT EXISTS status_reason_detail TEXT`,
    `feeds_status_reason_detail_length`, `feed_audit_events_detail_length`, and
    `2048`; asserting `hot_protection_check.sql` contains
    `'status_reason_detail'`; and asserting the comment-stripped migration does
    not contain `CREATE INDEX` followed by `status_reason_detail` on `feeds`.
  </action>
  <verify>
    <automated>safe-run -- uv run python -m pytest backend/pipeline/storage/tests/test_feed_audit_contract.py -q</automated>
    <automated>git diff --check -- backend/pipeline/storage/tests/test_feed_audit_contract.py</automated>
  </verify>
  <acceptance_criteria>
    - `backend/pipeline/storage/tests/test_feed_audit_contract.py` exists.
    - The test file contains the function names
      `test_documentation_defines_feed_audit_event_contract`,
      `test_repository_glossary_defines_audit_terms`,
      `test_migration_defines_delete_safe_audit_schema`,
      `test_migration_defines_actor_and_action_constraints`,
      `test_migration_rejects_empty_actor_id_suffixes`, and
      `test_migration_defines_status_reason_detail_and_hot_guard`.
    - The test file explicitly asserts `user:google:`, `service:`, `system:`,
      `job:`, `gcp-sa:`, and `user-email:` are rejected and `unknown:unknown`
      is accepted.
    - The test file asserts every actor namespace prefix has a paired
      `char_length(actor_id) > char_length('<prefix>')` suffix check in the
      SQL constraint.
    - The test file contains `pathlib` and does not contain `asyncpg`,
      `psycopg`, `testcontainers`, `docker`, or `FeedStore`.
    - `safe-run -- uv run python -m pytest backend/pipeline/storage/tests/test_feed_audit_contract.py -q`
      exits 0.
    - `git diff --check -- backend/pipeline/storage/tests/test_feed_audit_contract.py`
      exits 0.
  </acceptance_criteria>
  <done>Local text-level tests verify the Phase 1 documentation, SQL migration, and HOT guard without a live database or broad local test lane.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test assertions -> contract artifacts | Text-level tests enforce required documentation and SQL semantics. |
| SQL comments -> SQL gate checks | Banned SQL patterns must be checked outside comments to avoid false failures. |
| Local validation -> developer host | Verification must avoid resource-heavy Docker, component, API, and E2E lanes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Repudiation | contract tests for delete survival | mitigate | Test that comment-stripped SQL contains no `REFERENCES feeds` or `ON DELETE CASCADE` and that docs define `before_values` deletion snapshots. |
| T-01-03-02 | Spoofing | actor_id tests | mitigate | Test that documentation and SQL include every canonical actor namespace, reject empty namespaced suffixes, accept `unknown:unknown`, and include the actor constraint name. |
| T-01-03-03 | Information Disclosure | diagnostic-detail tests | mitigate | Test that docs and SQL name `status_reason_detail`, `2048`, and the HOT guard; Phase 1 intentionally does not add redaction beyond the documented cap. |
| T-01-03-04 | Tampering | action vocabulary tests | mitigate | Test that documentation and SQL include the exact action vocabulary and constraint name. |
| T-01-03-05 | Denial of Service | local test execution | mitigate | Keep tests text-only and run only `safe-run -- uv run python -m pytest backend/pipeline/storage/tests/test_feed_audit_contract.py -q`. |
| T-01-03-06 | Information Disclosure | future payload exposure | mitigate | Test that documentation names Watch Duty and admin timeline derivation from the contract while this phase adds no read API or delivery state. |
</threat_model>

<verification>
Run:

```bash
safe-run -- uv run python -m pytest backend/pipeline/storage/tests/test_feed_audit_contract.py -q
git diff --check
```
</verification>

<success_criteria>
- Focused pytest coverage exists for the domain documentation, repository glossary, SQL migration, actor/action constraints, empty actor suffix rejection, diagnostic detail bounds, HOT guard, and delete-survival invariants.
- Verification uses no live DB, Docker, testcontainers, API tests, component tests, or E2E tests.
- Requirements AUD-02, AUD-03, DIAG-01, ACT-01, DOC-01, DOC-02, and DOC-03 are covered by executable local checks.
</success_criteria>

<output>
After completion, create `.planning/phases/01-contract-and-schema-foundation/01-03-SUMMARY.md`.
</output>
