---
phase: 03-service-and-compatibility-surface
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pipeline/storage/feed_queries.py
  - backend/pipeline/storage/feed_store.py
  - backend/pipeline/storage/tests/test_feed_store.py
  - backend/services/feeds/models.py
  - backend/services/feeds/tests/test_api.py
  - backend/services/feeds/tests/test_service.py
autonomous: true
requirements: [DIAG-04, COMP-01, COMP-02]
must_haves:
  truths:
    - "Public FastAPI feed responses expose status_reason_detail as the canonical diagnostic detail field."
    - "Public FastAPI feed responses no longer expose quarantine_reason after this compatibility migration."
    - "Storage public feed projections return status_reason_detail consistently for create, get, list, update, and reset paths."
    - "Audit snapshots may continue to include quarantine_reason internally; this plan changes the public service contract, not historical audit snapshot content."
  artifacts:
    - path: "backend/pipeline/storage/feed_queries.py"
      provides: "Public feed projection SQL includes status_reason_detail"
    - path: "backend/pipeline/storage/feed_store.py"
      provides: "Feed TypedDict and row mapper include status_reason_detail"
    - path: "backend/services/feeds/models.py"
      provides: "FastAPI Feed response model exposes status_reason_detail and omits quarantine_reason"
    - path: "backend/services/feeds/tests/test_api.py"
      provides: "Service response compatibility tests"
    - path: "backend/services/feeds/tests/test_service.py"
      provides: "Service model validation fixtures"
  key_links:
    - from: "backend/pipeline/storage/feed_queries.py"
      to: "backend/pipeline/storage/feed_store.py"
      via: "_row_to_feed consumes projected columns"
      pattern: "status_reason_detail"
    - from: "backend/services/feeds/models.py"
      to: "frontend/api/src/feeds/feedsController.ts"
      via: "downstream BFF response shape in Plan 03-02"
      pattern: "status_reason_detail"
---

<objective>
Expose canonical diagnostic detail from the Python feed service.

Purpose: Phase 3 needs the backend service contract to return
`status_reason_detail` consistently before the BFF and UI migrate their public
TypeScript contract.

Output: Storage projections, storage feed mapping, FastAPI feed response model,
and focused backend tests updated for canonical diagnostic detail.
</objective>

<execution_context>
@/home/shuojing/.codex/get-shit-done/workflows/execute-plan.md
@/home/shuojing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-service-and-compatibility-surface/03-CONTEXT.md
@.planning/phases/03-service-and-compatibility-surface/03-RESEARCH.md
@.planning/phases/03-service-and-compatibility-surface/03-PATTERNS.md
@documentation/feed-audit-events.md
@backend/pipeline/storage/feed_queries.py
@backend/pipeline/storage/feed_store.py
@backend/pipeline/storage/tests/test_feed_store.py
@backend/services/feeds/models.py
@backend/services/feeds/tests/test_api.py
@backend/services/feeds/tests/test_service.py
</context>

<decision_coverage>
- D-07: Public feed responses expose canonical `status_reason_detail`.
- D-08: Compatibility preserves existing feed API flow, not a legacy public
  `quarantine_reason` alias.
- D-09: Service contracts consume `status_reason_detail`; runtime cleanup of
  `quarantine_reason` writes remains Phase 4.
- DIAG-04/COMP-02: Feed API consumers get the new canonical diagnostic detail.
- COMP-01: Existing status/status_reason/name/tags/source fields continue to
  be returned.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Add status_reason_detail to storage public feed projections</name>
  <files>backend/pipeline/storage/feed_queries.py, backend/pipeline/storage/feed_store.py, backend/pipeline/storage/tests/test_feed_store.py</files>
  <action>
    Add `status_reason_detail` to the normal feed projection SQL used by
    `CREATE_FEED_SQL`, `GET_FEED_SQL`, `LIST_FEEDS_DESC_SQL`,
    `LIST_FEEDS_ASC_SQL`, and `UPDATE_FEED_SQL`. `RESET_FEED_SQL` already
    returns the column; keep that behavior. Add `status_reason_detail` to the
    `Feed` `TypedDict` in `feed_store.py` and to `_row_to_feed`.

    Update focused storage tests so `TestStatusReasonRowMapping` and
    `TestStatusReasonSqlProjection` assert `status_reason_detail` is returned
    everywhere `_row_to_feed` may consume it. Keep existing audit snapshot tests
    unchanged except where they need import or fixture maintenance. Do not
    remove `quarantine_reason` from audit snapshot allowlists in this task.
  </action>
  <verify>
    <automated>safe-run -- uv run python -m pytest backend/pipeline/storage/tests/test_feed_store.py::TestStatusReasonRowMapping backend/pipeline/storage/tests/test_feed_store.py::TestStatusReasonSqlProjection backend/pipeline/storage/tests/test_feed_store.py::TestFeedAuditSql -q</automated>
    <automated>safe-run -- uv run python -m py_compile backend/pipeline/storage/feed_queries.py backend/pipeline/storage/feed_store.py</automated>
  </verify>
  <done>`_row_to_feed` receives and returns `status_reason_detail` consistently across create/get/list/update/reset feed service paths.</done>
</task>

<task type="auto">
  <name>Task 2: Move FastAPI Feed responses to status_reason_detail</name>
  <files>backend/services/feeds/models.py, backend/services/feeds/tests/test_api.py</files>
  <action>
    Replace public `Feed.quarantine_reason` with
    `Feed.status_reason_detail: str | None = None` in the FastAPI models.
    Preserve `status_reason`, `last_heartbeat`, tags, source fields, and
    `last_speech_segment_timestamp`. Do not add `actor_id` to request or
    response models.

    Add or update API tests proving a feed response includes
    `status_reason_detail` when present and omits `quarantine_reason`.
    Update the existing create/update contract tests so they still assert
    request bodies and responses are actor-field-free while tolerating the new
    response field.
  </action>
  <verify>
    <automated>safe-run -- uv run python -m pytest backend/services/feeds/tests/test_api.py -q</automated>
  </verify>
  <done>FastAPI feed responses expose canonical diagnostic detail and no public quarantine_reason field.</done>
</task>

<task type="auto">
  <name>Task 3: Update service fixtures for canonical detail</name>
  <files>backend/services/feeds/tests/test_service.py</files>
  <action>
    Update `_store_feed` and any service-layer expectations to include
    `status_reason_detail` as the public diagnostic detail fixture field.
    Keep storage dict fixtures allowed to contain internal `quarantine_reason`
    only if needed to reflect current storage rows, but service-level public
    assertions should use `status_reason_detail`. Do not change actor behavior
    in this plan; Plan 03-03 replaces the Phase 2 `service:feeds-service`
    fallback with trusted admin actor propagation.
  </action>
  <verify>
    <automated>safe-run -- uv run python -m pytest backend/services/feeds/tests/test_service.py backend/services/feeds/tests/test_api.py -q</automated>
  </verify>
  <done>Backend service tests pass with `status_reason_detail` as the public diagnostic field and actor behavior still deferred to Plan 03-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AlloyDB -> FeedStore | SQL projections provide the fields mapped into public service models. |
| FeedStore -> FastAPI service | Storage rows may still include internal fields; Pydantic response model defines public contract. |
| FastAPI service -> BFF | Service JSON becomes the source for TypeScript migration in Plan 03-02. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Information Disclosure | FastAPI `Feed` model | mitigate | Do not expose deprecated raw `quarantine_reason` in public feed responses; expose only canonical bounded `status_reason_detail`. |
| T-03-01-02 | Tampering | Service request models | mitigate | Keep feed request models actor-field-free; actor propagation is handled in Plan 03-03 through trusted headers. |
| T-03-01-03 | Repudiation | Diagnostic detail migration | mitigate | Tests assert the canonical detail field is present so operators do not lose current abnormal-state explanation during BFF/UI migration. |
| T-03-01-04 | Denial of Service | Storage projections | accept | Adding one unindexed text column to existing projections is low volume and does not add indexes or query predicates. |
</threat_model>

<risks_dependencies>
- Depends on Phase 1 adding `feeds.status_reason_detail` and Phase 2 keeping
  audit snapshots internal to storage.
- Risk: generated TypeScript/OpenAPI still exposes `quarantineReason` until
  Plan 03-02 runs. Keep this plan backend-only and execute Plan 03-02 next.
- Risk: runtime still writes `quarantine_reason` during failure/quarantine
  paths. That write cleanup is explicitly deferred to Phase 4 and should not be
  mixed into this service compatibility plan.
</risks_dependencies>

<verification>
Run focused backend checks:

```bash
safe-run -- uv run python -m pytest backend/pipeline/storage/tests/test_feed_store.py::TestStatusReasonRowMapping backend/pipeline/storage/tests/test_feed_store.py::TestStatusReasonSqlProjection backend/pipeline/storage/tests/test_feed_store.py::TestFeedAuditSql -q
safe-run -- uv run python -m pytest backend/services/feeds/tests/test_api.py backend/services/feeds/tests/test_service.py -q
git diff --check -- backend/pipeline/storage/feed_queries.py backend/pipeline/storage/feed_store.py backend/pipeline/storage/tests/test_feed_store.py backend/services/feeds/models.py backend/services/feeds/tests/test_api.py backend/services/feeds/tests/test_service.py .planning/phases/03-service-and-compatibility-surface/03-01-PLAN.md
```
</verification>

<success_criteria>
- Storage public feed projections include `status_reason_detail` wherever
  `_row_to_feed` maps feed rows.
- `backend.services.feeds.models.Feed` exposes `status_reason_detail` and does
  not expose `quarantine_reason`.
- Backend API/service tests prove canonical diagnostic detail response behavior
  without adding actor fields to request bodies.
</success_criteria>

<output>
After completion, create `.planning/phases/03-service-and-compatibility-surface/03-01-SUMMARY.md`.
</output>
