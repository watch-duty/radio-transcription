---
phase: 05-operator-docs
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - model/scripts/sft/README.md
  - model/scripts/sft/docs/index.md
  - model/scripts/sft/docs/runbook.md
autonomous: true
requirements:
  - DOC-01
  - DOC-04
  - DOC-05
must_haves:
  truths:
    - "D-01: Human-facing SFT docs use OKF-compatible Markdown."
    - "D-02/D-03: docs/runbook.md is the canonical operator runbook and README.md is only a thin entrypoint."
    - "D-04: docs live as a small bundle under model/scripts/sft/docs/ with runbook.md as the primary workflow doc."
    - "D-05: OKF frontmatter uses lightweight YAML metadata and no new documentation build tool is added."
    - "D-06: the runbook is optimized for a first-time operator running the standard path."
    - "D-07/D-08: the runbook lists prepare, tune, eval, report inspection, optional checkpoint scoring, and hygiene commands, with paid Vertex commands marked before use."
    - "D-09: docs teach one [eval.model] per config/run and separate configs or external wrappers for comparisons."
    - "D-20/D-21/D-22: the runbook distinguishes durable GCS state from local cache output and ends with an artifact hygiene check."
  artifacts:
    - path: model/scripts/sft/docs/index.md
      provides: "OKF navigation for the operator docs bundle"
    - path: model/scripts/sft/docs/runbook.md
      provides: "Canonical OKF operator runbook"
    - path: model/scripts/sft/README.md
      provides: "Thin entrypoint to the OKF runbook"
  key_links:
    - from: model/scripts/sft/README.md
      to: model/scripts/sft/docs/runbook.md
      via: "relative Markdown link"
      pattern: "\\[Operator runbook\\]\\(docs/runbook\\.md\\)"
    - from: model/scripts/sft/docs/runbook.md
      to: model/scripts/sft/docs/hygiene.md
      via: "final hygiene section"
      pattern: "docs/hygiene\\.md"
---

<objective>
Create the canonical OKF operator runbook and make the existing SFT README a
thin entrypoint into that runbook.

Purpose: A new operator needs one command-oriented path through prepare, tune,
eval, report inspection, optional checkpoint scoring, masked/unmasked evals,
and pre-commit artifact hygiene without reading historical notes.

Output: `model/scripts/sft/docs/index.md`,
`model/scripts/sft/docs/runbook.md`, and a shortened
`model/scripts/sft/README.md`.
</objective>

<execution_context>
@/home/shuojing/.codex/get-shit-done/workflows/execute-plan.md
@/home/shuojing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-operator-docs/05-CONTEXT.md
@.planning/phases/05-operator-docs/05-PATTERNS.md
@.planning/phases/04-durable-eval/04-CONTEXT.md
@.planning/phases/04-durable-eval/04-04-SUMMARY.md
@model/scripts/sft/README.md
@model/scripts/sft/run_config.example.toml
@model/src/gemini_sft/evaluate.py
@model/src/gemini_sft/reporting.py
@.gitignore

<interfaces>
Current durable eval contract:
```toml
[eval.model]
label = "base"
model = "gemini-3.1-flash-lite"
```

Current command surface:
```bash
gemini-sft prepare --config /path/to/run.toml
gemini-sft tune --config /path/to/run.toml --confirm
gemini-sft eval --config /path/to/run.toml
```

Current stable GCS report paths:
```text
gs://BUCKET/sft/runs/ROUND_ID/evals/wer_summary.json
gs://BUCKET/sft/runs/ROUND_ID/evals/wer_summary.md
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OKF index and canonical runbook</name>
  <files>model/scripts/sft/docs/index.md, model/scripts/sft/docs/runbook.md</files>
  <read_first>
    - .planning/phases/05-operator-docs/05-CONTEXT.md
    - .planning/phases/05-operator-docs/05-PATTERNS.md
    - .planning/phases/04-durable-eval/04-CONTEXT.md
    - model/scripts/sft/README.md
    - model/scripts/sft/run_config.example.toml
    - model/src/gemini_sft/evaluate.py
    - .gitignore
  </read_first>
  <action>
    Create `model/scripts/sft/docs/` if it does not exist. D-01: use
    OKF-compatible Markdown for human-facing SFT docs. D-04: use a small bundle
    under `model/scripts/sft/docs/` with `runbook.md` as the primary workflow
    doc and focused companion docs. D-05: use lightweight YAML metadata only;
    do not add a new required documentation build tool.

    Create `model/scripts/sft/docs/index.md` with lightweight OKF frontmatter
    per D-01, D-04, and D-05:
    `type: index`, `title: Gemini SFT Operator Docs`,
    `description: Entry point for the Gemini SFT operator documentation.`,
    and `tags: [gemini-sft, operator-docs]`. The body must have `# Gemini SFT
    Operator Docs` and direct links to `runbook.md`, `configs.md`,
    `metrics.md`, `artifacts.md`, and `hygiene.md`. Keep it a navigation
    file, not a duplicate runbook, per D-02 and D-03.

    Create `model/scripts/sft/docs/runbook.md` as the canonical OKF runbook per
    D-02. Use frontmatter exactly shaped as:
    `type: runbook`, `title: Gemini SFT Operator Runbook`,
    `description: End-to-end prepare, tune, eval, and report workflow.`, and
    `tags: [gemini-sft, operator-docs]`.

    D-06: optimize the runbook for a first-time operator running the standard
    path, not exhaustive historical experiment variants. The runbook body must
    use
    these section headings in this order: `## Scope`, `## Before You Start`,
    `## 1. Prepare A Placeholder Config`, `## 2. Build Gemini SFT Inputs`,
    `## 3. Submit Or Resume Vertex Tuning`, `## 4. Run Eval`,
    `## 5. Read Reports`, `## 6. Optional Checkpoint Sweep`,
    `## 7. Masked And Unmasked Eval Runs`, and
    `## 8. Artifact Hygiene Before Commit`.

    Include the exact commands from D-07:
    `gemini-sft prepare --config /path/to/run.toml`,
    `gemini-sft tune --config /path/to/run.toml --confirm`, and
    `gemini-sft eval --config /path/to/run.toml`. Mark `tune`, `eval`, and
    checkpoint scoring as paid or potentially paid Vertex operations per D-08.
    Before each paid command, state the expected output prefix:
    `gs://BUCKET/sft/runs/ROUND_ID/` for tuning and run state,
    `gs://BUCKET/sft/runs/ROUND_ID/evals/LABEL/` for target prediction
    artifacts, and `gs://BUCKET/sft/runs/ROUND_ID/evals/wer_summary.{json,md}`
    for report summaries.

    D-09: document the Phase 4 eval contract: one `[eval.model]` per
    config/run; base, tuned endpoint, and checkpoint endpoint comparisons use
    separate configs or an external wrapper; internal multi-model orchestration
    and dataset-breakdown features remain out of scope.

    Add a checkpoint scorer example as an optional ranking path, not the main
    packaged eval path: `python model/scripts/sft/score_gemini_sft_checkpoints_online.py --config /path/to/run.toml --limit 100`.

    Distinguish durable GCS state from local cache per D-20 and D-21. Name
    durable inspection points: `config.json`, `status.json`, canonical
    manifests, `model_inputs/gemini/*.jsonl`, `evals/LABEL/input.jsonl`,
    `evals/LABEL/output/`, `evals/LABEL/batch_predictions.meta.json`,
    `evals/LABEL/online_predictions.jsonl`,
    `evals/LABEL/online_predictions.meta.json`, normalized inference manifests,
    and `evals/wer_summary.{json,md}`.

    End the runbook with an explicit artifact hygiene check per D-22:
    `git status --short --ignored`, then
    `git diff --cached --name-only | rg '(^results/|^model/scripts/sft/results/|^model/data/inference_manifests/|\\.local\\.toml$|online_predictions\\.jsonl$|batch_predictions.*\\.jsonl$)'`.
    Explain that any match must be unstaged unless the user explicitly asked to
    commit that generated artifact. Link the detailed explanation to
    `docs/hygiene.md`.
  </action>
  <verify>
    <automated>test -f model/scripts/sft/docs/index.md && test -f model/scripts/sft/docs/runbook.md</automated>
    <automated>rg -n "^type: runbook$|^title: Gemini SFT Operator Runbook$|^## 8\\. Artifact Hygiene Before Commit$|gemini-sft tune --config /path/to/run.toml --confirm|git diff --cached --name-only|evals/wer_summary\\.\\{json,md\\}|docs/hygiene\\.md" model/scripts/sft/docs/runbook.md</automated>
    <automated>rg -n "\\[runbook\\.md\\]\\(runbook\\.md\\)|\\[configs\\.md\\]\\(configs\\.md\\)|\\[metrics\\.md\\]\\(metrics\\.md\\)|\\[artifacts\\.md\\]\\(artifacts\\.md\\)|\\[hygiene\\.md\\]\\(hygiene\\.md\\)" model/scripts/sft/docs/index.md</automated>
  </verify>
  <acceptance_criteria>
    - `model/scripts/sft/docs/runbook.md` contains the canonical OKF frontmatter
      fields `type`, `title`, `description`, and `tags`.
    - `runbook.md` contains the prepare, tune, eval, checkpoint scoring, masked
      and unmasked eval, report inspection, and artifact hygiene sections.
    - `runbook.md` says one `[eval.model]` is supported per config/run and
      states that model comparisons use separate configs or an external wrapper.
    - `runbook.md` names local `results/` as cache/mirror only and includes the
      explicit staged-file hygiene command from D-22.
  </acceptance_criteria>
  <done>The canonical runbook and OKF index exist with the required sections, commands, GCS prefixes, singular eval contract, and final hygiene check.</done>
</task>

<task type="auto">
  <name>Task 2: Make README a thin entrypoint</name>
  <files>model/scripts/sft/README.md</files>
  <read_first>
    - model/scripts/sft/README.md
    - model/scripts/sft/docs/index.md
    - model/scripts/sft/docs/runbook.md
    - .planning/phases/05-operator-docs/05-CONTEXT.md
  </read_first>
  <action>
    Replace the long `model/scripts/sft/README.md` with a concise entrypoint
    per D-03. Keep `# Watch Duty Gemini SFT CLI`, a short `## Runtime`
    section with the existing lightweight ASR Docker command, an
    `## Operator Docs` section linking to `[Operator runbook](docs/runbook.md)`,
    `[Config examples](docs/configs.md)`, `[Metric glossary](docs/metrics.md)`,
    `[Artifact reference](docs/artifacts.md)`, and
    `[Artifact hygiene](docs/hygiene.md)`, a `## Command Summary` section with
    the three CLI commands, and a `## Verification Boundary` section stating
    that unit tests mock GCS and Vertex and must not submit paid Vertex tuning,
    run Vertex batch inference, execute notebooks, or run end-to-end evals.

    Do not duplicate the runbook journey, config field table, metric glossary,
    artifact layout, or hygiene checklist in README. The README may summarize
    that `tune` and `eval` can spend money, but detailed command sequencing
    belongs in `docs/runbook.md` per D-02 and D-03.
  </action>
  <verify>
    <automated>rg -n "\\[Operator runbook\\]\\(docs/runbook\\.md\\)|\\[Config examples\\]\\(docs/configs\\.md\\)|\\[Metric glossary\\]\\(docs/metrics\\.md\\)|\\[Artifact reference\\]\\(docs/artifacts\\.md\\)|\\[Artifact hygiene\\]\\(docs/hygiene\\.md\\)" model/scripts/sft/README.md</automated>
    <automated>test "$(wc -l model/scripts/sft/README.md | awk '{print $1}')" -le 120</automated>
    <automated>rg -n "gemini-sft prepare --config /path/to/run.toml|gemini-sft tune --config /path/to/run.toml --confirm|gemini-sft eval --config /path/to/run.toml|must not submit paid Vertex" model/scripts/sft/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` is at most 120 lines.
    - `README.md` links to the five OKF docs and does not duplicate the full
      runbook.
    - `README.md` preserves the lightweight ASR runtime guidance and the
      no-paid-Vertex unit test boundary.
  </acceptance_criteria>
  <done>README is a thin entrypoint into the OKF runbook and companion docs, with no duplicate long-form operator workflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator docs -> paid Vertex CLI | Human follows commands that can submit tuning or inference jobs. |
| local workspace -> git index | Local generated experiment artifacts may be staged accidentally. |
| README -> runbook | Duplicate docs can drift and send operators to stale commands. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | I | docs/runbook.md | mitigate | Per D-08, mark `tune`, `eval`, and checkpoint scoring as paid or potentially paid and show expected GCS prefixes before each command. |
| T-05-01-02 | T | model/scripts/sft/README.md | mitigate | Per D-02/D-03, keep README under 120 lines and link to the canonical runbook instead of duplicating workflow details. |
| T-05-01-03 | I | docs/runbook.md | mitigate | Per D-20/D-22, name local `results/` as cache only and include a final `git diff --cached --name-only` hygiene check. |
| T-05-01-04 | R | docs/runbook.md | accept | Docs-only plan changes no runtime authorization, audit log, or data mutation behavior; verification is text and link based. |
</threat_model>

<verification>
Run these checks after all tasks in this plan:

```bash
git diff --check -- model/scripts/sft/README.md model/scripts/sft/docs/index.md model/scripts/sft/docs/runbook.md
rg -n "\\[eval\\.model\\]|external wrapper|results/|git diff --cached --name-only|paid Vertex" model/scripts/sft/docs/runbook.md model/scripts/sft/README.md
```
</verification>

<success_criteria>
- A new operator can open `model/scripts/sft/README.md` and immediately find the canonical runbook.
- The canonical runbook contains executable command order, paid-operation warnings, GCS output prefixes, report inspection paths, masked/unmasked guidance, and a final artifact hygiene check.
- The README no longer duplicates long runbook, config, metric, or artifact sections.
</success_criteria>

<output>
After completion, create `.planning/phases/05-operator-docs/05-01-SUMMARY.md`.
</output>
