---
phase: 05-operator-docs
plan: "03"
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - .gitignore
  - model/tests/common/tests/test_drift_guard.py
autonomous: true
requirements:
  - DOC-03
  - DOC-04
  - DOC-05
must_haves:
  truths:
    - "D-22/D-24: common local experiment artifacts are ignored or caught by a lightweight guard before commit."
    - "D-15/D-18: text-only drift guard keeps metric docs aligned with canonical report columns and away from legacy terms."
    - "D-23: verification stays lightweight and does not add a heavy CI policy or paid Vertex validation."
  artifacts:
    - path: .gitignore
      provides: "Narrow local SFT/operator artifact ignore coverage"
    - path: model/tests/common/tests/test_drift_guard.py
      provides: "Text-only docs/config drift guard"
  key_links:
    - from: model/tests/common/tests/test_drift_guard.py
      to: model/scripts/sft/docs/metrics.md
      via: "REPORT_COLUMNS text guard"
      pattern: "REPORT_COLUMNS"
    - from: model/tests/common/tests/test_drift_guard.py
      to: model/scripts/sft/docs/hygiene.md
      via: "artifact hygiene text guard"
      pattern: "git diff --cached --name-only"
---

<objective>
Add lightweight guardrails that keep Phase 5 docs and ignore rules from
drifting away from the operator artifact hygiene and metric terminology
contract.

Purpose: The docs alone teach the workflow, but the repo also needs narrow
ignore coverage and a cheap text-only test so accidental local artifact commits
and stale metric names are caught before execution is considered complete.

Output: Updated `.gitignore` and focused additions to
`model/tests/common/tests/test_drift_guard.py`.
</objective>

<execution_context>
@/home/shuojing/.codex/get-shit-done/workflows/execute-plan.md
@/home/shuojing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-operator-docs/05-CONTEXT.md
@.planning/phases/05-operator-docs/05-PATTERNS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/TESTING.md
@model/scripts/sft/docs/metrics.md
@model/scripts/sft/docs/hygiene.md
@model/src/gemini_sft/reporting.py
@model/tests/common/tests/test_drift_guard.py
@.gitignore

<interfaces>
Existing drift-guard style:
```python
class TestDriftGuard(unittest.TestCase):
    def test_sft_example_config_uses_singular_eval_model(self) -> None:
        text = (_SCRIPTS_DIR / "sft" / "run_config.example.toml").read_text(
            encoding="utf-8"
        )
```

Add only text/file-read guards in this plan. Do not submit Vertex jobs, run
batch inference, execute notebooks, traverse `results/`, or inspect live GCS.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add narrow ignore coverage for local SFT artifacts</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore
    - model/scripts/sft/docs/hygiene.md
    - .planning/codebase/STRUCTURE.md
    - .planning/phases/05-operator-docs/05-CONTEXT.md
  </read_first>
  <action>
    Update `.gitignore` per D-24 because the current dirty worktree pattern
    includes local `results/`, local `*.local.toml`, and generated
    `model/data/inference_manifests/*.jsonl` artifacts that are not fully
    covered.

    Add this narrow block near the existing SFT pipeline build-output rules:

    ```gitignore
    # Local SFT/operator experiment artifacts.
    results/
    *.local.toml
    model/data/inference_manifests/*.jsonl
    model/data/inference_manifests/*.jsonl.gz
    ```

    Keep the existing `model/scripts/sft/results/**/*.jsonl` and
    `model/scripts/sft/results/**/*.jsonl.gz` lines. Do not add broad patterns
    that hide committed placeholder docs or `model/scripts/sft/run_config.example.toml`.
    Do not remove any existing ignore rules.
  </action>
  <verify>
    <automated>rg -n "^results/$|^\\*\\.local\\.toml$|^model/data/inference_manifests/\\*\\.jsonl$|^model/data/inference_manifests/\\*\\.jsonl\\.gz$|model/scripts/sft/results/\\*\\*/\\*\\.jsonl" .gitignore</automated>
    <automated>! rg -n "run_config\\.example\\.toml" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` ignores root `results/`, `*.local.toml`, and generated
      local inference-manifest JSONL files.
    - `.gitignore` still contains the existing SFT result JSONL ignore lines.
    - `.gitignore` does not ignore `run_config.example.toml`.
  </acceptance_criteria>
  <done>Common local experiment artifacts are ignored narrowly without hiding committed placeholder docs or configs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add text-only docs drift guards</name>
  <files>model/tests/common/tests/test_drift_guard.py</files>
  <read_first>
    - model/tests/common/tests/test_drift_guard.py
    - model/scripts/sft/docs/metrics.md
    - model/scripts/sft/docs/hygiene.md
    - model/scripts/sft/docs/artifacts.md
    - model/src/gemini_sft/reporting.py
    - .gitignore
  </read_first>
  <behavior>
    - Test 1: every string in `gemini_sft.reporting.REPORT_COLUMNS` appears in `model/scripts/sft/docs/metrics.md`.
    - Test 2: `metrics.md` does not contain `empty_rate`, `hallucination_rate`, `hits`, or `correct_words`.
    - Test 3: `docs/hygiene.md` and `.gitignore` mention the local artifact classes `.local.toml`, `results/`, `model/data/inference_manifests/*.jsonl`, and `online_predictions.jsonl` or batch prediction JSONL.
  </behavior>
  <action>
    Extend `model/tests/common/tests/test_drift_guard.py` with text-only guards
    per D-15 through D-19 and D-22 through D-24. Reuse the existing `Path`
    style; add `_REPO_ROOT = _MODEL_DIR.parent` if needed.

    Add method `test_sft_operator_metric_docs_track_report_columns`. It should
    import `REPORT_COLUMNS` from `gemini_sft.reporting`, read
    `_SCRIPTS_DIR / "sft" / "docs" / "metrics.md"`, assert every column in
    `REPORT_COLUMNS` is present, and assert none of these legacy terms are
    present as raw text: `empty_rate`, `hallucination_rate`, `hits`,
    `correct_words`.

    Add method `test_sft_operator_hygiene_docs_and_gitignore_cover_local_artifacts`.
    It should read `_SCRIPTS_DIR / "sft" / "docs" / "hygiene.md"` and
    `_REPO_ROOT / ".gitignore"`. Assert `hygiene.md` contains `.local.toml`,
    `results/`, `model/data/inference_manifests/*.jsonl`,
    `online_predictions.jsonl`, `git status --short --ignored`, and
    `git diff --cached --name-only`. Assert `.gitignore` contains
    `*.local.toml`, `results/`, and
    `model/data/inference_manifests/*.jsonl`.

    Keep these tests free of GCS clients, Vertex SDK calls, Docker, notebook
    execution, broad filesystem traversal, and local `results/` inspection per
    D-23 and repo test-safety guidance.
  </action>
  <verify>
    <automated>rg -n "test_sft_operator_metric_docs_track_report_columns|REPORT_COLUMNS|test_sft_operator_hygiene_docs_and_gitignore_cover_local_artifacts|git diff --cached --name-only|model/data/inference_manifests" model/tests/common/tests/test_drift_guard.py</automated>
    <automated>safe-run -- bash -lc 'cd model && PYTHONPATH=src:tests python3 -m pytest tests/common/tests/test_drift_guard.py -q'</automated>
  </verify>
  <acceptance_criteria>
    - `test_drift_guard.py` asserts metric docs include all `REPORT_COLUMNS`
      values.
    - `test_drift_guard.py` asserts metric docs exclude the legacy terms
      `empty_rate`, `hallucination_rate`, `hits`, and `correct_words`.
    - `test_drift_guard.py` asserts hygiene docs and `.gitignore` cover the
      local artifact classes required by D-22 and D-24.
    - Targeted drift guard pytest passes without paid Vertex, Docker, notebook,
      or broad test execution.
  </acceptance_criteria>
  <done>Lightweight tests guard canonical metric terminology and artifact hygiene documentation without adding a heavy CI policy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local experiment artifacts -> git index | Sensitive or misleading generated artifacts may be staged. |
| docs -> metric decisions | Stale docs can cause incorrect interpretation of eval quality. |
| ignore rules -> repository visibility | Overbroad ignore rules can hide intended committed examples. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | I | .gitignore | mitigate | Per D-24, add narrow ignore rules for `results/`, `*.local.toml`, and generated inference manifest JSONL files. |
| T-05-03-02 | T | test_drift_guard.py | mitigate | Per D-15/D-18, assert metrics docs include `REPORT_COLUMNS` and exclude legacy terms. |
| T-05-03-03 | D | test_drift_guard.py | accept | Targeted text-only pytest is intentionally small and does not traverse large result trees or call external services. |
| T-05-03-04 | I | .gitignore | mitigate | Negative check ensures `run_config.example.toml` is not ignored by the new hygiene rules. |
</threat_model>

<verification>
Run these checks after all tasks in this plan:

```bash
git diff --check -- .gitignore model/tests/common/tests/test_drift_guard.py
rg -n "^results/$|^\\*\\.local\\.toml$|^model/data/inference_manifests/\\*\\.jsonl$" .gitignore
safe-run -- bash -lc 'cd model && PYTHONPATH=src:tests python3 -m pytest tests/common/tests/test_drift_guard.py -q'
```
</verification>

<success_criteria>
- `.gitignore` covers common local SFT/operator artifacts without hiding
  placeholder examples.
- Drift guards make metrics docs track `REPORT_COLUMNS`.
- Drift guards make hygiene docs and ignore rules cover the local artifact
  classes called out by the runbook.
- Verification remains text-only and no paid Vertex validation is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/05-operator-docs/05-03-SUMMARY.md`.
</output>
