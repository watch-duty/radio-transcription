---
phase: 05-operator-docs
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - model/scripts/sft/run_config.example.toml
  - model/scripts/sft/docs/configs.md
  - model/scripts/sft/docs/metrics.md
  - model/scripts/sft/docs/artifacts.md
  - model/scripts/sft/docs/hygiene.md
autonomous: true
requirements:
  - DOC-02
  - DOC-03
  - DOC-04
  - DOC-05
must_haves:
  truths:
    - "D-10/D-12: the repo has one full placeholder config and at most one masked-eval variant snippet."
    - "D-11: docs do not create a broad gallery of separate config files for every target/backend/masked combination."
    - "D-13: base, tuned endpoint, and checkpoint endpoint examples use the same [eval.model] shape with short snippets."
    - "D-14: examples contain placeholders only and no real local credentials, live run IDs, local .local.toml values, or generated paths."
    - "D-15/D-19: metric docs define the canonical report columns and explain missing predictions separately from exact empty responses."
    - "D-16: metric docs use empty_or_unintelligible_rate for empty or exact [UNINTELLIGIBLE] hypotheses."
    - "D-17: metric docs use empty_response_rate only for exact empty model output after stripping whitespace."
    - "D-20/D-24: artifact and hygiene docs distinguish durable GCS state from local cache and name files that must not be committed."
  artifacts:
    - path: model/scripts/sft/docs/configs.md
      provides: "Placeholder config reference and snippets"
    - path: model/scripts/sft/docs/metrics.md
      provides: "Canonical report metric glossary"
    - path: model/scripts/sft/docs/artifacts.md
      provides: "Durable GCS and local cache artifact reference"
    - path: model/scripts/sft/docs/hygiene.md
      provides: "Operator pre-commit hygiene checklist"
    - path: model/scripts/sft/run_config.example.toml
      provides: "Canonical full placeholder TOML"
  key_links:
    - from: model/scripts/sft/docs/metrics.md
      to: model/src/gemini_sft/reporting.py
      via: "REPORT_COLUMNS vocabulary"
      pattern: "target_label.*missing_prediction_count.*artifacts"
    - from: model/scripts/sft/docs/artifacts.md
      to: model/src/gemini_sft/evaluate.py
      via: "stable summary upload paths"
      pattern: "evals/wer_summary"
---

<objective>
Create the focused OKF companion docs for placeholder configs, metric
interpretation, durable artifacts, and local artifact hygiene.

Purpose: The runbook needs concise references so operators can configure common
base/tuned/checkpoint/masked/unmasked runs, interpret current report columns,
and avoid treating local experiment output as source-controlled state.

Output: `docs/configs.md`, `docs/metrics.md`, `docs/artifacts.md`,
`docs/hygiene.md`, and any small comment cleanup needed in the canonical
placeholder TOML.
</objective>

<execution_context>
@/home/shuojing/.codex/get-shit-done/workflows/execute-plan.md
@/home/shuojing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-operator-docs/05-CONTEXT.md
@.planning/phases/05-operator-docs/05-PATTERNS.md
@.planning/phases/01-reporting-contract/01-01-SUMMARY.md
@.planning/phases/04-durable-eval/04-03-SUMMARY.md
@.planning/phases/04-durable-eval/04-04-SUMMARY.md
@model/scripts/sft/run_config.example.toml
@model/src/gemini_sft/config.py
@model/src/gemini_sft/reporting.py
@model/src/gemini_sft/evaluate.py
@.gitignore

<interfaces>
Canonical report columns from `model/src/gemini_sft/reporting.py`:
```python
REPORT_COLUMNS = (
    "target_label",
    "model",
    "wer",
    "cer",
    "keyword_accuracy",
    "empty_or_unintelligible_rate",
    "empty_response_rate",
    "insertions",
    "deletions",
    "substitutions",
    "total_reference_words",
    "missing_prediction_count",
    "artifacts",
)
```

Supported eval config keys from `model/src/gemini_sft/config.py`:
```text
[eval.model] requires exactly: label, model
[eval.execution] supports: backend, concurrency, limit, max_retries
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document placeholder config shapes</name>
  <files>model/scripts/sft/run_config.example.toml, model/scripts/sft/docs/configs.md</files>
  <read_first>
    - model/scripts/sft/run_config.example.toml
    - model/src/gemini_sft/config.py
    - .planning/phases/05-operator-docs/05-CONTEXT.md
    - .planning/phases/05-operator-docs/05-PATTERNS.md
  </read_first>
  <action>
    Create `model/scripts/sft/docs/configs.md` with OKF frontmatter:
    `type: reference`, `title: Gemini SFT Config Examples`,
    `description: Placeholder config shapes for config-driven Gemini SFT runs.`,
    and `tags: [gemini-sft, configs]`.

    Keep `model/scripts/sft/run_config.example.toml` as the one canonical full
    placeholder config per D-10 and D-12. D-11: do not create a broad gallery
    of separate config files for every base, tuned, checkpoint, masked,
    unmasked, batch, and online combination. Update only comments if necessary
    so the example states that values are placeholders, `gcp.bucket` is a bucket
    name not a `gs://` URI, prompt files are unsupported, and `[eval.model]` is
    singular.

    In `configs.md`, include these headings: `## Full Placeholder Config`,
    `## Eval Target Snippets`, `## Masked Eval Variant`,
    `## Unsupported Shapes`, and `## Placeholder Safety`.
    The full-placeholder section must link to `../run_config.example.toml`.

    Under `## Eval Target Snippets`, D-13: document base-only, tuned endpoint,
    and checkpoint endpoint targets as the same `[eval.model]` shape:
    base snippet with `label = "base"` and `model = "gemini-3.1-flash-lite"`;
    tuned snippet with `label = "tuned"` and
    `model = "projects/PROJECT/locations/us-central1/endpoints/TUNED_ENDPOINT_ID"`;
    checkpoint snippet with `label = "checkpoint_6"` and
    `model = "projects/PROJECT/locations/us-central1/endpoints/CHECKPOINT_ENDPOINT_ID"`.

    Under `## Masked Eval Variant`, add exactly one compact masked-eval snippet
    per D-12. It must change only:
    `round_id = "YYYY-MM-DD-short-description-masked"`,
    `inference_dataset_slug = "echo/masked_v2/eval"`, and
    `eval_manifest_uri = "gs://your-bucket/path/manifests/echo/masked_v2/eval.jsonl"`.
    State that unmasked eval uses the normal full placeholder config with its
    own `round_id`, `inference_dataset_slug`, and `eval_manifest_uri`.

    Under `## Unsupported Shapes`, state that plural `[[eval.models]]`,
    `eval_models`, `eval_label`, `masked`, prompt file fields, and local
    `.local.toml` committed examples are unsupported. This documents the
    singular contract per D-09. D-14: keep examples placeholder-only, with no
    real local credentials, live run IDs, local `.local.toml` values, or
    generated result artifact paths.
  </action>
  <verify>
    <automated>test -f model/scripts/sft/docs/configs.md</automated>
    <automated>rg -n "Full Placeholder Config|Eval Target Snippets|Masked Eval Variant|Unsupported Shapes|label = \"base\"|label = \"tuned\"|label = \"checkpoint_6\"|YYYY-MM-DD-short-description-masked" model/scripts/sft/docs/configs.md</automated>
    <automated>rg -n "^\\[eval\\.model\\]$|one model per config|max_retries = 3" model/scripts/sft/run_config.example.toml</automated>
    <automated>test "$(rg -n "^round_id = " model/scripts/sft/run_config.example.toml | wc -l)" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - There is one full placeholder TOML: `model/scripts/sft/run_config.example.toml`.
    - `configs.md` contains base, tuned endpoint, checkpoint endpoint, masked
      eval, and unmasked eval guidance without adding full duplicate config
      files.
    - Examples use placeholder values only: `PROJECT`, `TUNED_ENDPOINT_ID`,
      `CHECKPOINT_ENDPOINT_ID`, `YYYY-MM-DD-short-description`, and
      `gs://your-bucket/...`.
    - `run_config.example.toml` contains `[eval.model]` and no active
      `[[eval.models]]` table.
  </acceptance_criteria>
  <done>Config docs and the canonical placeholder TOML describe the supported singular eval model contract and the small masked variant without real run artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Document canonical report metrics</name>
  <files>model/scripts/sft/docs/metrics.md</files>
  <read_first>
    - model/src/gemini_sft/reporting.py
    - model/src/common/scoring.py
    - model/tests/gemini_sft/test_reporting.py
    - .planning/phases/05-operator-docs/05-CONTEXT.md
    - .planning/phases/05-operator-docs/05-PATTERNS.md
  </read_first>
  <action>
    Create `model/scripts/sft/docs/metrics.md` with OKF frontmatter:
    `type: reference`, `title: Gemini SFT Metric Glossary`,
    `description: Canonical report columns and metric semantics for Gemini SFT eval reports.`,
    and `tags: [gemini-sft, metrics, reports]`.

    Define every current public column from `REPORT_COLUMNS` per D-15:
    `target_label`, `model`, `wer`, `cer`, `keyword_accuracy`,
    `empty_or_unintelligible_rate`, `empty_response_rate`, `insertions`,
    `deletions`, `substitutions`, `total_reference_words`,
    `missing_prediction_count`, and `artifacts`.

    Include a `## Empty Output Metrics` section. D-16: define
    `empty_or_unintelligible_rate` exactly as the metric for hypotheses that
    are empty after stripping whitespace or exactly `[UNINTELLIGIBLE]`. D-17:
    define `empty_response_rate` exactly as the metric for exact empty model
    output after stripping whitespace.

    Include a `## Missing Predictions` section explaining D-19: missing provider
    predictions are scored as empty hypotheses and remain in the WER/CER
    denominator, while `missing_prediction_count` remains operationally
    separate from exact empty model responses.

    Include a `## Artifact URI Fields` section for `artifacts` keys:
    `raw_output_uri`, `online_predictions_uri`, `normalized_manifest_uri`,
    `summary_json_uri`, and `summary_markdown_uri`.

    Include row count guidance from D-15: current eval reports expose row count
    as report metadata `n_eval_examples`, not as a target-table column.

    Do not use `empty_rate`, `hallucination_rate`, `hits`, or `correct_words`
    anywhere in this new metrics doc. This stronger rule satisfies D-18 by
    avoiding legacy primary terminology completely.
  </action>
  <verify>
    <automated>test -f model/scripts/sft/docs/metrics.md</automated>
    <automated>rg -n "target_label|model|wer|cer|keyword_accuracy|empty_or_unintelligible_rate|empty_response_rate|insertions|deletions|substitutions|total_reference_words|missing_prediction_count|artifacts" model/scripts/sft/docs/metrics.md</automated>
    <automated>rg -n "raw_output_uri|online_predictions_uri|normalized_manifest_uri|summary_json_uri|summary_markdown_uri|n_eval_examples" model/scripts/sft/docs/metrics.md</automated>
    <automated>! rg -n "\\b(empty_rate|hallucination_rate|hits|correct_words)\\b" model/scripts/sft/docs/metrics.md</automated>
  </verify>
  <acceptance_criteria>
    - `metrics.md` defines every `REPORT_COLUMNS` value using the canonical
      public names.
    - `metrics.md` distinguishes `empty_or_unintelligible_rate` from
      `empty_response_rate`.
    - `metrics.md` explains that missing provider rows affect scoring as empty
      hypotheses but remain counted separately as `missing_prediction_count`.
    - `metrics.md` does not contain the legacy terms `empty_rate`,
      `hallucination_rate`, `hits`, or `correct_words`.
  </acceptance_criteria>
  <done>Metric docs match the canonical report contract and avoid legacy metric terminology as primary operator-facing names.</done>
</task>

<task type="auto">
  <name>Task 3: Document durable artifacts and hygiene checks</name>
  <files>model/scripts/sft/docs/artifacts.md, model/scripts/sft/docs/hygiene.md</files>
  <read_first>
    - model/src/gemini_sft/evaluate.py
    - model/src/gemini_sft/records.py
    - model/scripts/sft/README.md
    - .gitignore
    - .planning/phases/05-operator-docs/05-CONTEXT.md
    - .planning/phases/05-operator-docs/05-PATTERNS.md
  </read_first>
  <action>
    Create `model/scripts/sft/docs/artifacts.md` with OKF frontmatter:
    `type: reference`, `title: Gemini SFT Artifact Reference`,
    `description: Durable GCS state and local cache locations for Gemini SFT runs.`,
    and `tags: [gemini-sft, artifacts]`.

    Include headings `## Durable GCS State`, `## Eval Target Artifacts`,
    `## Normalized Inference Manifests`, and `## Local Cache Or Mirror`.
    Per D-20, list durable GCS paths:
    `gs://BUCKET/sft/runs/ROUND_ID/run_config.toml`,
    `gs://BUCKET/sft/runs/ROUND_ID/config.json`,
    `gs://BUCKET/sft/runs/ROUND_ID/status.json`,
    `gs://BUCKET/sft/runs/ROUND_ID/manifests/canonical/{train,validation,eval}.jsonl`,
    `gs://BUCKET/sft/runs/ROUND_ID/model_inputs/gemini/{train,validation}.jsonl`,
    `gs://BUCKET/sft/runs/ROUND_ID/evals/LABEL/input.jsonl`,
    `gs://BUCKET/sft/runs/ROUND_ID/evals/LABEL/output/`,
    `gs://BUCKET/sft/runs/ROUND_ID/evals/LABEL/batch_predictions.meta.json`,
    `gs://BUCKET/sft/runs/ROUND_ID/evals/LABEL/online_predictions.jsonl`,
    `gs://BUCKET/sft/runs/ROUND_ID/evals/LABEL/online_predictions.meta.json`,
    `gs://BUCKET/sft/runs/ROUND_ID/evals/wer_summary.json`, and
    `gs://BUCKET/sft/runs/ROUND_ID/evals/wer_summary.md`.
    For normalized manifests, include
    `gs://BUCKET/inference_manifests/INFERENCE_DATASET_SLUG/MODEL_FAMILY_SLUG/ROUND_ID/LABEL.jsonl`.
    Per D-21, state that local `results/ROUND_ID/` and downloaded/generated
    local mirrors are cache only and are not evidence that durable eval reuse
    will succeed.

    Create `model/scripts/sft/docs/hygiene.md` with OKF frontmatter:
    `type: reference`, `title: Gemini SFT Artifact Hygiene`,
    `description: Pre-commit checks for local Gemini SFT experiment artifacts.`,
    and `tags: [gemini-sft, git-hygiene]`.

    Include headings `## Never Commit By Default`, `## Pre-Commit Check`, and
    `## If A Check Matches`. The never-commit list must name `.local.toml`,
    root `results/`, `model/scripts/sft/results/`,
    `model/data/inference_manifests/*.jsonl`,
    `online_predictions.jsonl`, batch prediction JSONL, generated eval outputs,
    and local research experiment outputs per D-22.

    The pre-commit section must include these exact commands:
    `git status --short --ignored` and
    `git diff --cached --name-only | rg '(^results/|^model/scripts/sft/results/|^model/data/inference_manifests/|\\.local\\.toml$|online_predictions\\.jsonl$|batch_predictions.*\\.jsonl$)'`.
    Per D-23 and D-24, explain that this docs-level check is intentionally
    lightweight and that `.gitignore` should cover common local artifacts, but
    the staged-file check is the final source of truth before committing.
  </action>
  <verify>
    <automated>test -f model/scripts/sft/docs/artifacts.md && test -f model/scripts/sft/docs/hygiene.md</automated>
    <automated>rg -n "config\\.json|status\\.json|batch_predictions\\.meta\\.json|online_predictions\\.meta\\.json|evals/wer_summary\\.json|inference_manifests/INFERENCE_DATASET_SLUG|Local Cache Or Mirror" model/scripts/sft/docs/artifacts.md</automated>
    <automated>rg -n "\\.local\\.toml|results/|model/data/inference_manifests/\\*\\.jsonl|online_predictions\\.jsonl|git status --short --ignored|git diff --cached --name-only" model/scripts/sft/docs/hygiene.md</automated>
  </verify>
  <acceptance_criteria>
    - `artifacts.md` identifies durable GCS state and local cache/mirror output
      as separate categories.
    - `artifacts.md` lists stable `evals/wer_summary.json` and
      `evals/wer_summary.md` as durable report inspection points.
    - `hygiene.md` names every prohibited local artifact class from D-22.
    - `hygiene.md` contains the explicit staged-file check command that catches
      accidental local artifact commits.
  </acceptance_criteria>
  <done>Artifact and hygiene docs give operators exact durable paths, local-cache boundaries, and concrete git checks before committing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| placeholder docs -> operator config | Example values may be copied into a live paid run. |
| report code -> metric glossary | Stale terminology can lead to incorrect promotion decisions. |
| local experiment output -> git index | Generated artifacts may contain data, credentials, or misleading local state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | I | docs/configs.md | mitigate | Per D-14, use placeholders only: `PROJECT`, endpoint IDs, `YYYY-MM-DD-short-description`, and `gs://your-bucket/...`; do not include real run IDs or local paths. |
| T-05-02-02 | T | docs/metrics.md | mitigate | Per D-15 through D-18, define every `REPORT_COLUMNS` value and reject legacy names in the metrics doc with a negative grep check. |
| T-05-02-03 | I | docs/artifacts.md | mitigate | Per D-20/D-21, label GCS artifacts as durable and local `results/` as cache/mirror only. |
| T-05-02-04 | I | docs/hygiene.md | mitigate | Per D-22/D-24, include exact staged-file scan for `.local.toml`, `results/`, inference manifests, and raw prediction JSONL. |
</threat_model>

<verification>
Run these checks after all tasks in this plan:

```bash
git diff --check -- model/scripts/sft/run_config.example.toml model/scripts/sft/docs/configs.md model/scripts/sft/docs/metrics.md model/scripts/sft/docs/artifacts.md model/scripts/sft/docs/hygiene.md
rg -n "target_label|empty_or_unintelligible_rate|empty_response_rate|missing_prediction_count|summary_json_uri|summary_markdown_uri" model/scripts/sft/docs/metrics.md
! rg -n "\\b(empty_rate|hallucination_rate|hits|correct_words)\\b" model/scripts/sft/docs/metrics.md
```
</verification>

<success_criteria>
- Operators have one full placeholder config and compact snippets for tuned,
  checkpoint, masked, and unmasked variants.
- Metric docs use canonical report names from `REPORT_COLUMNS`.
- Artifact docs make GCS durable state and local cache output unambiguous.
- Hygiene docs include a concrete staged-file check before commit.
</success_criteria>

<output>
After completion, create `.planning/phases/05-operator-docs/05-02-SUMMARY.md`.
</output>
