---
phase: 01-policy-and-storage-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - backend/pipeline/storage/feed_queries.py
  - backend/pipeline/storage/tests/test_feed_store.py
autonomous: true
requirements:
  - STORE-05
  - STORE-06
  - STAT-01
must_haves:
  truths:
    - "D-09/D-14: failure_count means consecutive feed-budget-eligible failures only, and report_feed_failure(...) is the only incrementing path."
    - "D-15: Successful progress and SourceObservation still clear stale failure_count and status_reason state."
    - "D-17/D-19: pipeline_publish_after_bookmark_failed exists and cannot be treated as feed-budget-eligible storage behavior."
  artifacts:
    - path: backend/pipeline/storage/tests/test_feed_store.py
      provides: "Foundation storage tests for increment isolation and recovery clearing semantics."
    - path: backend/pipeline/storage/feed_queries.py
      provides: "Existing progress/source-observation SQL semantics preserved."
  key_links:
    - from: backend/pipeline/storage/tests/test_feed_store.py
      to: backend/pipeline/storage/feed_queries.py
      via: "SQL contract tests with comments stripped"
      pattern: "_sql_without_comments"
    - from: backend/pipeline/storage/feed_queries.py
      to: backend/pipeline/storage/feed_store.py
      via: "REPORT_FAILURE_SQL remains the only failure_count increment storage path"
      pattern: "failure_count = failure_count \\+ 1"
---

<objective>
Add the Phase 1 foundation storage tests and preserve recovery semantics.

Purpose: Prove the new non-budgeted primitive cannot consume quarantine budget
and does not regress stale failure-state clearing on successful progress or
SourceObservation.
Output: Focused storage tests and final targeted Phase 1 verification.
</objective>

<execution_context>
@/home/shuojing/.codex/get-shit-done/workflows/execute-plan.md
@/home/shuojing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-policy-and-storage-foundation/01-CONTEXT.md
@.planning/phases/01-policy-and-storage-foundation/01-RESEARCH.md
@.planning/phases/01-policy-and-storage-foundation/01-VALIDATION.md
@.planning/phases/01-policy-and-storage-foundation/01-PATTERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/STACK.md
@AGENTS.md
@.agents/instructions.md
@.github/instructions/PYTHON_STYLE.instructions.md

Important local state: this plan runs after 01-03, but the worktree
may still include earlier uncommitted edits. Read current files and diffs
before changing tests. Do not revert unrelated user/agent edits.

<interfaces>
Storage semantics to preserve:
```sql
-- Budgeted path:
failure_count = failure_count + 1

-- Non-budgeted path:
failure_count = 0

-- Successful progress and source observation:
failure_count = 0
status_reason = NULL
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove budget increment isolation</name>
  <files>backend/pipeline/storage/feed_queries.py, backend/pipeline/storage/tests/test_feed_store.py</files>
  <read_first>
  - Run `git status --short` and inspect current diffs in `feed_queries.py` and `test_feed_store.py`.
  - `.planning/phases/01-policy-and-storage-foundation/01-CONTEXT.md` decisions D-09, D-10, D-13, D-14, and D-19.
  - `.planning/phases/01-policy-and-storage-foundation/01-VALIDATION.md` row 01-04-01.
  - `backend/pipeline/storage/feed_queries.py`
  - `backend/pipeline/storage/tests/test_feed_store.py`
  </read_first>
  <behavior>
  - Test that stripped `REPORT_FAILURE_SQL` contains `failure_count = failure_count + 1`.
  - Test that stripped `RELEASE_NON_BUDGETED_FAILURE_SQL` contains `failure_count = 0`.
  - Test that stripped `RELEASE_NON_BUDGETED_FAILURE_SQL` contains no `failure_count + 1` and no `quarantine_reason`.
  - Test that no other feed lifecycle SQL constant added by this phase increments failure_count.
  </behavior>
  <action>Add or tighten storage SQL tests so STORE-05 is enforced by tests, not comments. Use `_sql_without_comments(...)` before checking SQL text. Keep `failure_count = failure_count + 1` only in `REPORT_FAILURE_SQL`; do not move that expression into the non-budgeted SQL. Do not add a new status, table, migration, or quarantine_reason assignment. If current dirty tests already cover part of this, extend them in place rather than adding duplicate classes.</action>
  <verify>
    <automated>safe-run -- uv run python -m pytest backend/pipeline/storage/tests/test_feed_store.py::TestNonBudgetedFailureSql backend/pipeline/storage/tests/test_feed_store.py::TestReportFailureSqlStatusReason -q -n 0</automated>
  </verify>
  <acceptance_criteria>
  - `safe-run -- uv run python -m pytest backend/pipeline/storage/tests/test_feed_store.py::TestNonBudgetedFailureSql backend/pipeline/storage/tests/test_feed_store.py::TestReportFailureSqlStatusReason -q -n 0` passes.
  - `rg -n "failure_count = failure_count \\+ 1" backend/pipeline/storage/feed_queries.py` shows the increment expression in `REPORT_FAILURE_SQL` only when inspected in context.
  - `rg -n "quarantine_reason" backend/pipeline/storage/tests/test_feed_store.py` shows an assertion that the non-budgeted SQL does not contain `quarantine_reason`.
  </acceptance_criteria>
  <done>Tests prove `report_feed_failure(...)` remains the only storage path that increments the feed quarantine budget.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve progress and SourceObservation stale-state clearing</name>
  <files>backend/pipeline/storage/feed_queries.py, backend/pipeline/storage/tests/test_feed_store.py</files>
  <read_first>
  - Run `git status --short` and inspect current diffs in `feed_queries.py` and `test_feed_store.py`.
  - `.planning/phases/01-policy-and-storage-foundation/01-CONTEXT.md` decision D-15.
  - `.planning/phases/01-policy-and-storage-foundation/01-VALIDATION.md` row 01-04-02.
  - `backend/pipeline/storage/feed_queries.py`
  - `backend/pipeline/storage/tests/test_feed_store.py`
  - `.planning/phases/01-policy-and-storage-foundation/01-PATTERNS.md` sections for `UPDATE_PROGRESS_SQL`, `RECORD_SOURCE_OBSERVATION_SQL`, and `TestStatusReasonClearSql`.
  </read_first>
  <behavior>
  - Test that stripped `UPDATE_PROGRESS_SQL` contains `failure_count = 0` and `status_reason = NULL`.
  - Test that stripped `UPDATE_PROGRESS_SQL` does not set lifecycle status.
  - Test that stripped `RECORD_SOURCE_OBSERVATION_SQL` contains `failure_count = 0`, `status_reason = NULL`, worker/fencing guards, and `current_state.status = 'active'::feed_status`.
  - Test that status reason timestamp updates only when a stale status reason is present.
  </behavior>
  <action>Preserve `UPDATE_PROGRESS_SQL` and `RECORD_SOURCE_OBSERVATION_SQL` stale-state clearing semantics per D-15. Tighten `TestStatusReasonClearSql` and `TestRecordSourceObservation` only as needed to prove successful progress and `SourceObservation` still clear stale `failure_count` and `status_reason` while keeping existing recovery/lifecycle behavior. Do not change the recovery claim query except to fix a failing test caused by the Phase 1 storage primitive.</action>
  <verify>
    <automated>safe-run -- uv run python -m pytest backend/pipeline/storage/tests/test_feed_store.py::TestStatusReasonClearSql backend/pipeline/storage/tests/test_feed_store.py::TestRecordSourceObservation -q -n 0</automated>
  </verify>
  <acceptance_criteria>
  - `safe-run -- uv run python -m pytest backend/pipeline/storage/tests/test_feed_store.py::TestStatusReasonClearSql backend/pipeline/storage/tests/test_feed_store.py::TestRecordSourceObservation -q -n 0` passes.
  - `rg -n "status_reason = NULL|failure_count = 0" backend/pipeline/storage/feed_queries.py backend/pipeline/storage/tests/test_feed_store.py` shows progress/source-observation clearing logic and tests.
  - `git diff -- backend/pipeline/storage/feed_queries.py` shows no removal of `current_state.worker_id = $2`, `current_state.fencing_token = $3`, or `current_state.status = 'active'::feed_status` from `RECORD_SOURCE_OBSERVATION_SQL`.
  </acceptance_criteria>
  <done>Existing successful progress and source-observation recovery semantics remain covered and intact.</done>
</task>

<task type="auto">
  <name>Task 3: Run the Phase 1 foundation verification gate</name>
  <files>backend/pipeline/ingestion/failure_policy.py, backend/pipeline/ingestion/models.py, backend/pipeline/ingestion/collectors/failure_classification.py, backend/pipeline/ingestion/collector_runtime.py, backend/pipeline/storage/feed_queries.py, backend/pipeline/storage/feed_store.py, backend/pipeline/ingestion/tests/test_failure_policy.py, backend/pipeline/ingestion/collectors/tests/test_failure_classification.py, backend/pipeline/ingestion/tests/test_collector_runtime.py, backend/pipeline/storage/tests/test_feed_store.py</files>
  <read_first>
  - Run `git status --short` to confirm which files are modified before the gate.
  - `.planning/phases/01-policy-and-storage-foundation/01-VALIDATION.md`
  - `.planning/phases/01-policy-and-storage-foundation/01-CONTEXT.md`
  - `backend/pipeline/ingestion/tests/test_failure_policy.py`
  - `backend/pipeline/storage/tests/test_feed_store.py`
  </read_first>
  <action>Run the targeted Phase 1 verification commands only. Use `safe-run --` and `-n 0`; do not run broad local E2E, API, Docker, component, integration, or unscoped pytest suites. If a targeted failure occurs, fix only the Phase 1 files listed in this plan and rerun the same targeted command.</action>
  <verify>
    <automated>safe-run -- uv run python -m pytest backend/pipeline/ingestion/tests/test_failure_policy.py backend/pipeline/ingestion/collectors/tests/test_failure_classification.py backend/pipeline/ingestion/tests/test_collector_runtime.py::TestFeedFailureContract backend/pipeline/storage/tests/test_feed_store.py::TestFeedStatusReason backend/pipeline/storage/tests/test_feed_store.py::TestNonBudgetedFailureSql backend/pipeline/storage/tests/test_feed_store.py::TestReleaseNonBudgetedFailure backend/pipeline/storage/tests/test_feed_store.py::TestReportFailureSqlStatusReason backend/pipeline/storage/tests/test_feed_store.py::TestStatusReasonClearSql backend/pipeline/storage/tests/test_feed_store.py::TestRecordSourceObservation -q -n 0</automated>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
  - The targeted pytest command exits 0.
  - `git diff --check` exits 0.
  - `rg -n "reason_family" backend/pipeline/ingestion backend/pipeline/storage` shows no matches.
  - `rg -n "class FailurePolicyDecision|class FailurePolicyEvidence|def classify_failure_policy" backend/pipeline/ingestion/failure_policy.py` shows the policy contract symbols.
  </acceptance_criteria>
  <done>All Phase 1 foundation tests pass with narrow local validation, and whitespace checks are clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| storage tests -> SQL constants | Tests verify state-transition SQL without a live database. |
| successful runtime event -> storage | Existing progress and source-observation calls clear stale failure state. |
| retryable failing state -> claim path | Suppressed retry rows remain schedulable through existing failing recovery behavior. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-01-09 | Denial of Service | HIGH | feed failure budget | mitigate | Tests must prove `failure_count = failure_count + 1` is isolated to `REPORT_FAILURE_SQL`; non-budgeted SQL resets to zero. |
| T-01-10 | Tampering | HIGH | recovery clearing SQL | mitigate | Tests must preserve worker/fencing/status guards on `RECORD_SOURCE_OBSERVATION_SQL` and fenced progress updates. |
| T-01-11 | Denial of Service | MEDIUM | stale quarantine debt | mitigate | Tests must prove progress and source observation clear stale `failure_count` and `status_reason` so old mixed debt cannot keep affecting future claims. |
| T-01-12 | Repudiation | MEDIUM | storage evidence | mitigate | SQL contract tests use stripped SQL text and exact assertions so behavior cannot hide in comments or broad prose. |
</threat_model>

<verification>
Run the Phase 1 foundation gate:

```bash
safe-run -- uv run python -m pytest backend/pipeline/ingestion/tests/test_failure_policy.py backend/pipeline/ingestion/collectors/tests/test_failure_classification.py backend/pipeline/ingestion/tests/test_collector_runtime.py::TestFeedFailureContract backend/pipeline/storage/tests/test_feed_store.py::TestFeedStatusReason backend/pipeline/storage/tests/test_feed_store.py::TestNonBudgetedFailureSql backend/pipeline/storage/tests/test_feed_store.py::TestReleaseNonBudgetedFailure backend/pipeline/storage/tests/test_feed_store.py::TestReportFailureSqlStatusReason backend/pipeline/storage/tests/test_feed_store.py::TestStatusReasonClearSql backend/pipeline/storage/tests/test_feed_store.py::TestRecordSourceObservation -q -n 0
git diff --check
```
</verification>

<success_criteria>
- Tests prove `report_feed_failure(...)` remains the only budget-incrementing storage path.
- Tests prove non-budgeted storage cannot write `quarantine_reason`.
- Tests prove successful progress and `SourceObservation` still clear stale failure count and status reason state.
- The focused Phase 1 verification gate passes without running broad local stacks.
</success_criteria>

<output>
After completion, create `.planning/phases/01-policy-and-storage-foundation/01-04-SUMMARY.md`.
</output>
