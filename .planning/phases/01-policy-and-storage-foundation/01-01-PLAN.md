---
phase: 01-policy-and-storage-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pipeline/ingestion/failure_policy.py
  - backend/pipeline/ingestion/models.py
  - backend/pipeline/ingestion/collector_runtime.py
  - backend/pipeline/ingestion/tests/test_failure_policy.py
  - backend/pipeline/ingestion/tests/test_collector_runtime.py
  - backend/pipeline/storage/feed_store.py
  - backend/pipeline/storage/tests/test_feed_store.py
autonomous: true
requirements:
  - POL-01
  - POL-02
  - POL-03
  - STAT-01
must_haves:
  truths:
    - "D-01/D-02: Policy vocabulary and pure classification live in backend/pipeline/ingestion/failure_policy.py."
    - "D-03: failure_policy.py has no DB writes, runtime routing side effects, Pub/Sub calls, telemetry, or alerts."
    - "D-04: FailurePolicyEvidence contains only fact fields; FailurePolicyDecision contains policy_intent, executed_action, feed_budget_eligible, and quarantine_feed."
    - "D-05/D-06: Typed FeedFailure requires policy_evidence; untyped runtime exceptions are the only fallback path."
    - "D-16/D-17/D-18/D-21: FeedStatusReason keeps its name and includes only pipeline_publish_after_bookmark_failed as the new Phase 1 value; pipeline_* means downstream post-capture consistency/replay semantics."
  artifacts:
    - path: backend/pipeline/ingestion/failure_policy.py
      provides: "OwnerScope, FailureScope, EndpointKind, PolicyIntent, ExecutedAction, PipelineStage, FailurePolicyEvidence, FailurePolicyDecision, classify_failure_policy, and pure predicates."
    - path: backend/pipeline/ingestion/tests/test_failure_policy.py
      provides: "Policy evidence, decision, and predicate contract tests."
    - path: backend/pipeline/ingestion/models.py
      provides: "Capture boundary models with strict FeedFailure policy_evidence."
    - path: backend/pipeline/storage/feed_store.py
      provides: "FeedStatusReason.PIPELINE_PUBLISH_AFTER_BOOKMARK_FAILED without renaming FeedStatusReason."
  key_links:
    - from: backend/pipeline/ingestion/models.py
      to: backend/pipeline/ingestion/failure_policy.py
      via: "module import for FailurePolicyEvidence annotation"
      pattern: "from backend\\.pipeline\\.ingestion import failure_policy"
    - from: backend/pipeline/ingestion/collector_runtime.py
      to: backend/pipeline/ingestion/failure_policy.py
      via: "policy module import/classification compatibility only"
      pattern: "failure_policy\\.(classify_failure_policy|FailurePolicyEvidence)"
    - from: backend/pipeline/storage/tests/test_feed_store.py
      to: backend/pipeline/storage/feed_store.py
      via: "FeedStatusReason enum value contract"
      pattern: "PIPELINE_PUBLISH_AFTER_BOOKMARK_FAILED"
---

<objective>
Add the policy evidence and status reason primitives that Phase 1 needs.

Purpose: The runtime-facing failure contract can represent quarantine policy
from structured evidence rather than raw reason text or quarantine_reason.
Output: A pure policy module, strict FeedFailure evidence wiring, and the
single required backend status reason value.
</objective>

<execution_context>
@/home/shuojing/.codex/get-shit-done/workflows/execute-plan.md
@/home/shuojing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-policy-and-storage-foundation/01-CONTEXT.md
@.planning/phases/01-policy-and-storage-foundation/01-RESEARCH.md
@.planning/phases/01-policy-and-storage-foundation/01-VALIDATION.md
@.planning/phases/01-policy-and-storage-foundation/01-PATTERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/STACK.md
@AGENTS.md
@.agents/instructions.md
@.github/instructions/PYTHON_STYLE.instructions.md

Important local state: the worktree may already contain uncommitted prior-agent
implementation edits in the files this plan touches. Read the current files
first, preserve useful existing work, and do not run destructive git commands
or revert unrelated user/agent edits.

<interfaces>
Current source state to reconcile:

From backend/pipeline/ingestion/models.py:
```python
class OwnerScope(StrEnum): ...
class FailureScope(StrEnum): ...
class EndpointKind(StrEnum): ...
class PolicyIntent(StrEnum): ...
class ExecutedAction(StrEnum): ...
class PipelineStage(StrEnum): ...

@dataclasses.dataclass(frozen=True)
class FailurePolicyEvidence:
    owner_scope: OwnerScope
    failure_scope: FailureScope
    endpoint_kind: EndpointKind
    policy_intent: PolicyIntent
    executed_action: ExecutedAction
    pipeline_stage: PipelineStage | None = None

class FeedFailure(Exception):
    policy_evidence: FailurePolicyEvidence | None
```

Required target contract:
```python
@dataclasses.dataclass(frozen=True)
class FailurePolicyEvidence:
    owner_scope: OwnerScope
    failure_scope: FailureScope
    endpoint_kind: EndpointKind
    pipeline_stage: PipelineStage | None = None

@dataclasses.dataclass(frozen=True)
class FailurePolicyDecision:
    status_reason: FeedStatusReason
    evidence: FailurePolicyEvidence
    policy_intent: PolicyIntent
    executed_action: ExecutedAction
    feed_budget_eligible: bool
    quarantine_feed: bool
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create pure failure policy contracts and tests</name>
  <files>backend/pipeline/ingestion/failure_policy.py, backend/pipeline/ingestion/tests/test_failure_policy.py</files>
  <read_first>
  - Run `git status --short` and treat existing uncommitted edits as prior work; do not revert them.
  - `.planning/phases/01-policy-and-storage-foundation/01-CONTEXT.md` decisions D-01, D-02, D-03, D-04, D-07, D-19, and D-20.
  - `.planning/phases/01-policy-and-storage-foundation/01-PATTERNS.md` sections for `failure_policy.py` and `test_failure_policy.py`.
  - `backend/pipeline/ingestion/models.py`
  - `backend/pipeline/ingestion/collectors/failure_classification.py`
  - `backend/pipeline/storage/feed_store.py`
  </read_first>
  <behavior>
  - Test that `FailurePolicyEvidence` exposes only `owner_scope`, `failure_scope`, `endpoint_kind`, and optional `pipeline_stage`.
  - Test that `FailurePolicyDecision` exposes `policy_intent`, `executed_action`, `feed_budget_eligible`, and `quarantine_feed`.
  - Test that feed-owned feed-configuration evidence classifies to `PolicyIntent.QUARANTINE_FEED`, `ExecutedAction.INCREMENT_FEED_FAILURE_BUDGET`, `feed_budget_eligible=True`, and `quarantine_feed=True`.
  - Test that `FeedStatusReason.PIPELINE_PUBLISH_AFTER_BOOKMARK_FAILED` with pipeline Pub/Sub evidence classifies to `PolicyIntent.HOLD_FOR_REPLAY`, `ExecutedAction.SUPPRESS_FEED_QUARANTINE_RECORD_PUBLISH_GAP`, `feed_budget_eligible=False`, and `quarantine_feed=False`.
  - Test predicate helpers `is_feed_quarantine`, `is_feed_budget_eligible`, `is_pipeline_hold`, and `is_source_class_breaker`.
  - Test that `reason_family` is absent from the public contract.
  </behavior>
  <action>Create `backend/pipeline/ingestion/failure_policy.py` per D-01/D-02. Move policy vocabulary ownership out of `models.py` into this module: `OwnerScope`, `FailureScope`, `EndpointKind`, `PolicyIntent`, `ExecutedAction`, `PipelineStage`, `FailurePolicyEvidence`, `FailurePolicyDecision`, `classify_failure_policy(status_reason, evidence)`, `is_feed_quarantine(decision)`, `is_feed_budget_eligible(decision)`, `is_pipeline_hold(decision)`, and `is_source_class_breaker(decision)`. Keep the module pure per D-03: no `FeedStore`, no DB calls, no Pub/Sub calls, no logging side effects, no telemetry calls, no alerting, no hold/replay writes, and no runtime lease mutation. Implement D-04 by keeping `policy_intent`, `executed_action`, `feed_budget_eligible`, and `quarantine_feed` off `FailurePolicyEvidence` and on `FailurePolicyDecision`. Do not add `reason_family` per D-07. Use stdlib `enum.StrEnum` and frozen dataclasses, full absolute imports, and the existing unittest style for `backend/pipeline/ingestion/tests/test_failure_policy.py`.</action>
  <verify>
    <automated>safe-run -- uv run python -m pytest backend/pipeline/ingestion/tests/test_failure_policy.py -q -n 0</automated>
  </verify>
  <acceptance_criteria>
  - `test -f backend/pipeline/ingestion/failure_policy.py` exits 0.
  - `test -f backend/pipeline/ingestion/tests/test_failure_policy.py` exits 0.
  - `rg -n "class FailurePolicyDecision|def classify_failure_policy|def is_feed_quarantine|def is_feed_budget_eligible|def is_pipeline_hold|def is_source_class_breaker" backend/pipeline/ingestion/failure_policy.py` shows all required symbols.
  - `rg -n "reason_family" backend/pipeline/ingestion/failure_policy.py backend/pipeline/ingestion/tests/test_failure_policy.py` shows no matches.
  - `safe-run -- uv run python -m pytest backend/pipeline/ingestion/tests/test_failure_policy.py -q -n 0` passes.
  </acceptance_criteria>
  <done>The pure policy module exists, tests prove the facts/verdict split, and pipeline Pub/Sub policy decisions are non-budgeted and non-quarantine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile strict FeedFailure evidence and imports</name>
  <files>backend/pipeline/ingestion/models.py, backend/pipeline/ingestion/collector_runtime.py, backend/pipeline/ingestion/tests/test_collector_runtime.py</files>
  <read_first>
  - Run `git status --short` and preserve existing uncommitted implementation edits that align with D-IDs.
  - `.planning/phases/01-policy-and-storage-foundation/01-CONTEXT.md` decisions D-01, D-04, D-05, D-06, and D-08.
  - `.github/instructions/PYTHON_STYLE.instructions.md` import rule: local imports should import packages/modules, not individual symbols; prefer `from backend.pipeline.ingestion import failure_policy` where policy symbols are needed.
  - `backend/pipeline/ingestion/failure_policy.py`
  - `backend/pipeline/ingestion/models.py`
  - `backend/pipeline/ingestion/collector_runtime.py`
  - `backend/pipeline/ingestion/tests/test_collector_runtime.py`
  </read_first>
  <behavior>
  - Test that `FeedFailure(...)` without `policy_evidence=` raises `TypeError` or `ValueError`.
  - Test that `FeedFailure` accepts and stores a `FailurePolicyEvidence` from `backend.pipeline.ingestion.failure_policy`.
  - Test that policy vocabulary is no longer defined in `models.py`.
  - Test that `collector_runtime.py` imports policy symbols through `from backend.pipeline.ingestion import failure_policy` where it needs policy types for compatibility.
  </behavior>
  <action>Remove policy enum/dataclass definitions from `backend/pipeline/ingestion/models.py` and use a module import (`from backend.pipeline.ingestion import failure_policy`) for `FeedFailure` annotations, e.g. `failure_policy.FailurePolicyEvidence`, per the Python style guide. Make typed `FeedFailure` strict per D-05: require the keyword-only `policy_evidence` argument and reject `None`; keep status reason normalization, raw reason validation, 200-character bounding, and Python exception runtime mutability. Update current `collector_runtime.py` imports so policy names come from the `failure_policy` module import. If current dirty runtime code reads `evidence.policy_intent` or `evidence.executed_action`, replace those reads with `failure_policy.classify_failure_policy(status_reason, evidence)` at that local compatibility point so the code compiles without moving runtime side effects into `failure_policy.py`. Do not update `failure_classification.py` or source collector call sites in this plan; 01-02 owns all collector evidence reconciliation per D-08.</action>
  <verify>
    <automated>safe-run -- uv run python -m pytest backend/pipeline/ingestion/tests/test_collector_runtime.py::TestFeedFailureContract -q -n 0</automated>
  </verify>
  <acceptance_criteria>
  - `rg -n "class OwnerScope|class FailureScope|class EndpointKind|class PolicyIntent|class ExecutedAction|class PipelineStage|class FailurePolicyEvidence" backend/pipeline/ingestion/models.py` shows no matches.
  - `rg -n "policy_evidence: FailurePolicyEvidence \\| None|policy_evidence: FailurePolicyEvidence \\| None = None" backend/pipeline/ingestion/models.py` shows no matches.
  - `rg -n "from backend\\.pipeline\\.ingestion import failure_policy" backend/pipeline/ingestion/models.py backend/pipeline/ingestion/collector_runtime.py` shows module imports where policy symbols are used.
  - `rg -n "from backend\\.pipeline\\.ingestion\\.failure_policy import" backend/pipeline/ingestion/models.py backend/pipeline/ingestion/collector_runtime.py backend/pipeline/ingestion/tests/test_collector_runtime.py` shows no matches unless an existing local-pattern exception is documented in the changed file.
  - `safe-run -- uv run python -m pytest backend/pipeline/ingestion/tests/test_collector_runtime.py::TestFeedFailureContract -q -n 0` passes.
  </acceptance_criteria>
  <done>Typed feed failures cannot intentionally omit policy evidence, policy vocabulary ownership is no longer in models.py, and runtime compatibility imports use the policy module without adding Phase 2 routing behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the Phase 1 status reason primitive</name>
  <files>backend/pipeline/storage/feed_store.py, backend/pipeline/storage/tests/test_feed_store.py</files>
  <read_first>
  - Run `git status --short` and preserve current status-reason edits if they already match this contract.
  - `.planning/phases/01-policy-and-storage-foundation/01-CONTEXT.md` decisions D-16, D-17, D-18, D-19, D-20, and D-21.
  - `backend/pipeline/storage/feed_store.py`
  - `backend/pipeline/storage/tests/test_feed_store.py`
  - `.planning/phases/01-policy-and-storage-foundation/01-PATTERNS.md` sections for `FeedStatusReason` and `TestFeedStatusReason`.
  </read_first>
  <behavior>
  - Test that `FeedStatusReason.PIPELINE_PUBLISH_AFTER_BOOKMARK_FAILED.value == "pipeline_publish_after_bookmark_failed"`.
  - Test that `FeedStatusReason("pipeline_publish_after_bookmark_failed") is FeedStatusReason.PIPELINE_PUBLISH_AFTER_BOOKMARK_FAILED`.
  - Test that no speculative status reasons are added with `reason_family` or extra `pipeline_*` enum members.
  </behavior>
  <action>Add or verify `FeedStatusReason.PIPELINE_PUBLISH_AFTER_BOOKMARK_FAILED = "pipeline_publish_after_bookmark_failed"` per D-17. Keep the enum named `FeedStatusReason` per D-21. Do not add other new status reason values per D-16, and do not treat the `pipeline_` prefix as routing authority per D-20; classification belongs to `FailurePolicyDecision`. Update `TestFeedStatusReason` in `backend/pipeline/storage/tests/test_feed_store.py` to include exactly the currently needed enum values, including `pipeline_publish_after_bookmark_failed`.</action>
  <verify>
    <automated>safe-run -- uv run python -m pytest backend/pipeline/storage/tests/test_feed_store.py::TestFeedStatusReason -q -n 0</automated>
  </verify>
  <acceptance_criteria>
  - `rg -n "PIPELINE_PUBLISH_AFTER_BOOKMARK_FAILED|pipeline_publish_after_bookmark_failed" backend/pipeline/storage/feed_store.py backend/pipeline/storage/tests/test_feed_store.py` shows the enum value and tests.
  - `rg -n "reason_family" backend/pipeline/storage/feed_store.py backend/pipeline/storage/tests/test_feed_store.py` shows no matches.
  - `safe-run -- uv run python -m pytest backend/pipeline/storage/tests/test_feed_store.py::TestFeedStatusReason -q -n 0` passes.
  </acceptance_criteria>
  <done>The backend status reason vocabulary includes the single required pipeline publish gap value without renaming or broadening the enum.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| collector -> runtime | Typed collector failures cross into runtime failure handling. |
| runtime -> policy | Runtime asks pure policy code for a decision from structured evidence. |
| policy -> storage caller | Policy output can influence whether a later caller uses budgeted or non-budgeted storage. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | HIGH | `failure_policy.py` | mitigate | Implement D-01 through D-04 and tests proving routing uses structured evidence and `FailurePolicyDecision`, not raw `reason`, `quarantine_reason`, or status prefix parsing. |
| T-01-02 | Denial of Service | HIGH | `FailurePolicyDecision` predicates | mitigate | Tests must prove `pipeline_publish_after_bookmark_failed` decisions have `feed_budget_eligible=False` and `quarantine_feed=False` per D-19. |
| T-01-03 | Information Disclosure | MEDIUM | `FeedFailure.reason` | mitigate | Preserve existing bounded raw reason behavior and keep raw reason forensic only; no policy parsing from raw text. |
| T-01-04 | Elevation of Privilege | LOW | Phase 1 surface | accept | No auth, API route, cryptography, or external authorization boundary is added in this plan. |
</threat_model>

<verification>
Run the focused plan checks:

```bash
safe-run -- uv run python -m pytest backend/pipeline/ingestion/tests/test_failure_policy.py backend/pipeline/ingestion/tests/test_collector_runtime.py::TestFeedFailureContract backend/pipeline/storage/tests/test_feed_store.py::TestFeedStatusReason -q -n 0
git diff --check
```
</verification>

<success_criteria>
- Policy vocabulary and pure classification are owned by `failure_policy.py`.
- `FailurePolicyEvidence` is facts-only and `FailurePolicyDecision` carries verdict/action/budget fields.
- Typed `FeedFailure` requires `policy_evidence`.
- `pipeline_publish_after_bookmark_failed` exists as the only new backend status reason value in this plan.
- `models.py` and `collector_runtime.py` import policy contracts from the `failure_policy` module where needed for compatibility.
- No task moved DB writes, Pub/Sub, telemetry, alerts, or runtime routing side effects into `failure_policy.py`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-policy-and-storage-foundation/01-01-SUMMARY.md`.
</output>
