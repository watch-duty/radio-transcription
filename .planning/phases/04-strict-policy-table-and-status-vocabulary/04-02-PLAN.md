---
phase: 04-strict-policy-table-and-status-vocabulary
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - backend/pipeline/storage/feed_store.py
  - backend/pipeline/storage/tests/test_feed_store.py
  - backend/pipeline/ingestion/collectors/failure_classification.py
  - backend/pipeline/ingestion/collectors/tests/test_failure_classification.py
  - backend/pipeline/ingestion/failure_policy.py
  - backend/pipeline/ingestion/tests/test_failure_policy.py
autonomous: true
requirements: [STAT-11, STAT-12, STAT-13, STAT-14, TEST-11, TEST-12]
user_setup: []
must_haves:
  truths:
    - "D-12: add only the backend enum values needed now: system_runtime_configuration_invalid, system_credential_access_failed, and system_source_payload_invalid."
    - "D-13/D-14/D-15/D-16/D-17: update backend owner/evidence mapping and policy rows for the split reasons."
    - "D-18/D-19/D-20: do not update frontend/OpenAPI/generated UI compatibility in Phase 4."
    - "D-05/D-06: policy remains pure and reason_family stays out."
  artifacts:
    - path: backend/pipeline/storage/feed_store.py
      provides: "Backend-only FeedStatusReason enum additions."
    - path: backend/pipeline/ingestion/collectors/failure_classification.py
      provides: "Owner-scope mappings for the new backend reasons."
    - path: backend/pipeline/ingestion/failure_policy.py
      provides: "Policy-table rows for the new backend reasons."
    - path: backend/pipeline/storage/tests/test_feed_store.py
      provides: "Backend enum vocabulary proof without frontend/OpenAPI parity updates."
    - path: backend/pipeline/ingestion/collectors/tests/test_failure_classification.py
      provides: "Status/evidence owner mapping proof."
    - path: backend/pipeline/ingestion/tests/test_failure_policy.py
      provides: "Split-status policy route proof."
  key_links:
    - from: backend/pipeline/ingestion/collectors/tests/test_failure_classification.py
      to: backend/pipeline/ingestion/collectors/failure_classification.py
      via: "owner_scope_for_status_reason(...) assertions"
      pattern: "owner_scope_for_status_reason|SYSTEM_RUNTIME_CONFIGURATION_INVALID|SYSTEM_CREDENTIAL_ACCESS_FAILED|SYSTEM_SOURCE_PAYLOAD_INVALID"
    - from: backend/pipeline/ingestion/tests/test_failure_policy.py
      to: backend/pipeline/ingestion/failure_policy.py
      via: "new enum policy route assertions"
      pattern: "SYSTEM_RUNTIME_CONFIGURATION_INVALID|SYSTEM_CREDENTIAL_ACCESS_FAILED|SYSTEM_SOURCE_PAYLOAD_INVALID"
---

<objective>
Add the backend-only status vocabulary needed by v1.1 and wire it into owner mapping plus policy routing.

Purpose: split broad backend reasons where the root cause has a clear v1 route, while avoiding frontend/OpenAPI/generated compatibility churn until the follow-up phase.
Output: three new backend `FeedStatusReason` values, owner mapping tests, and policy-table rows/tests for their routing.
</objective>

<execution_context>
@/home/shuojing/.codex/get-shit-done/workflows/execute-plan.md
@/home/shuojing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-strict-policy-table-and-status-vocabulary/04-CONTEXT.md
@.planning/phases/04-strict-policy-table-and-status-vocabulary/04-DISCUSSION-LOG.md
@backend/pipeline/storage/feed_store.py
@backend/pipeline/storage/tests/test_feed_store.py
@backend/pipeline/ingestion/collectors/failure_classification.py
@backend/pipeline/ingestion/collectors/tests/test_failure_classification.py
@backend/pipeline/ingestion/failure_policy.py
@backend/pipeline/ingestion/tests/test_failure_policy.py

<interfaces>
Add exactly these backend enum values:

```python
FeedStatusReason.SYSTEM_RUNTIME_CONFIGURATION_INVALID
FeedStatusReason.SYSTEM_CREDENTIAL_ACCESS_FAILED
FeedStatusReason.SYSTEM_SOURCE_PAYLOAD_INVALID
```

Map them through existing helpers:

```python
owner_scope_for_status_reason(...)
policy_evidence_for_status_reason(...)
classify_failure_policy(...)
```
</interfaces>
</context>

<must_haves>
<truth>STAT-11: Backend enum vocabulary includes only current-needed split reasons.</truth>
<truth>STAT-12: `system_runtime_configuration_invalid` is routeable as a code/deploy/config-owned failure rather than a generic collector error.</truth>
<truth>STAT-13: `system_credential_access_failed` is explicit and non-budgeted so retry-forever remains a one-row policy change later.</truth>
<truth>STAT-14: `system_source_payload_invalid` is routeable separately from generic unexpected errors.</truth>
<truth>TEST-11/TEST-12: New status/evidence combinations must be covered in both owner mapping and policy route tests.</truth>
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for backend enum and owner mapping splits</name>
  <files>backend/pipeline/storage/tests/test_feed_store.py, backend/pipeline/ingestion/collectors/tests/test_failure_classification.py</files>
  <read_first>
    - .planning/phases/04-strict-policy-table-and-status-vocabulary/04-CONTEXT.md
    - backend/pipeline/storage/feed_store.py
    - backend/pipeline/storage/tests/test_feed_store.py
    - backend/pipeline/ingestion/collectors/failure_classification.py
    - backend/pipeline/ingestion/collectors/tests/test_failure_classification.py
  </read_first>
  <behavior>
    - `system_runtime_configuration_invalid` exists in the backend enum and maps to `OwnerScope.SOURCE_CLASS`.
    - `system_credential_access_failed` exists in the backend enum and maps to `OwnerScope.CREDENTIAL_SCOPE`.
    - `system_source_payload_invalid` exists in the backend enum and maps to `OwnerScope.SOURCE_CLASS`.
    - Existing `system_authentication_failed`, `system_configuration_invalid`, `system_collector_error`, `system_pipeline_error`, and `system_unexpected_error` mappings remain stable unless a later producer split changes them intentionally.
  </behavior>
  <action>
    Add focused backend tests first. Update the backend canonical enum-value test or add a backend-only assertion for the three new values. Add parameterized owner-scope mapping tests in the collector classification test file. Do not update OpenAPI or frontend parity tests in this phase.
  </action>
  <verify>
    <automated>safe-run -- uv run python -m pytest backend/pipeline/storage/tests/test_feed_store.py::TestFeedStatusReason::test_canonical_reason_values backend/pipeline/ingestion/collectors/tests/test_failure_classification.py -q -n 0</automated>
    <expected_before_fix>The new enum and owner-mapping tests fail before implementation.</expected_before_fix>
  </verify>
  <acceptance_criteria>
    - `rg -n "system_runtime_configuration_invalid|system_credential_access_failed|system_source_payload_invalid" backend/pipeline/storage/tests/test_feed_store.py backend/pipeline/ingestion/collectors/tests/test_failure_classification.py` finds tests for all three values.
    - `rg -n "OwnerScope\\.SOURCE_CLASS|OwnerScope\\.CREDENTIAL_SCOPE" backend/pipeline/ingestion/collectors/tests/test_failure_classification.py` finds explicit owner assertions for the new reasons.
    - No OpenAPI/frontend/generated files are touched.
  </acceptance_criteria>
  <done>Tests describe the backend enum split and evidence owner mapping before code changes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add backend enum values and owner mapping</name>
  <files>backend/pipeline/storage/feed_store.py, backend/pipeline/ingestion/collectors/failure_classification.py</files>
  <read_first>
    - backend/pipeline/storage/feed_store.py
    - backend/pipeline/ingestion/collectors/failure_classification.py
    - backend/pipeline/storage/tests/test_feed_store.py
    - backend/pipeline/ingestion/collectors/tests/test_failure_classification.py
  </read_first>
  <behavior>
    - Add exactly the three enum values listed in D-12.
    - Add the new statuses to owner mapping without broadening existing generic statuses.
    - Keep `system_collector_error` and `system_unexpected_error` available as generic non-budgeted fallbacks.
  </behavior>
  <action>
    Update `FeedStatusReason` in `feed_store.py`. Update `_SOURCE_STATUS_REASONS` only if needed; the three new statuses are not source statuses. Extend `owner_scope_for_status_reason(...)` with narrow mappings for the three new reasons. Preserve `policy_evidence_for_status_reason(...)` behavior so callers can still override endpoint kind, failure scope, and pipeline stage with stronger evidence.
  </action>
  <verify>
    <automated>safe-run -- uv run python -m pytest backend/pipeline/storage/tests/test_feed_store.py::TestFeedStatusReason::test_canonical_reason_values backend/pipeline/ingestion/collectors/tests/test_failure_classification.py -q -n 0</automated>
  </verify>
  <acceptance_criteria>
    - Backend enum contains exactly `SYSTEM_RUNTIME_CONFIGURATION_INVALID`, `SYSTEM_CREDENTIAL_ACCESS_FAILED`, and `SYSTEM_SOURCE_PAYLOAD_INVALID` as new Phase 4 additions.
    - Focused backend enum and owner mapping tests pass.
    - Existing source status mappings are unchanged.
  </acceptance_criteria>
  <done>Backend vocabulary and owner mapping understand the new split reasons.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend policy-table tests and rows for split reasons</name>
  <files>backend/pipeline/ingestion/tests/test_failure_policy.py, backend/pipeline/ingestion/failure_policy.py</files>
  <read_first>
    - backend/pipeline/ingestion/failure_policy.py
    - backend/pipeline/ingestion/tests/test_failure_policy.py
    - backend/pipeline/storage/feed_store.py
    - backend/pipeline/ingestion/collectors/failure_classification.py
  </read_first>
  <behavior>
    - `system_runtime_configuration_invalid` is budgeted/quarantine-eligible for its source-class runtime configuration evidence in v1.1.
    - `system_source_payload_invalid` is budgeted/quarantine-eligible for source-class payload-invalid evidence in v1.1.
    - `system_credential_access_failed` is explicit and non-budgeted, with a single policy row that can be changed later if retry-forever proves wrong.
    - Mismatched owner/evidence for each new status fails closed to telemetry gap.
  </behavior>
  <action>
    Add policy tests for the three new statuses, then extend the explicit policy table from 04-01. Keep the rows narrow enough that changing `system_credential_access_failed` from non-budgeted to retry-forever or quarantine later is a one-row change. Do not add `reason_family`.
  </action>
  <verify>
    <automated>safe-run -- uv run python -m pytest backend/pipeline/ingestion/tests/test_failure_policy.py -q -n 0</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "SYSTEM_RUNTIME_CONFIGURATION_INVALID|SYSTEM_CREDENTIAL_ACCESS_FAILED|SYSTEM_SOURCE_PAYLOAD_INVALID" backend/pipeline/ingestion/tests/test_failure_policy.py backend/pipeline/ingestion/failure_policy.py` finds tests and table rows for all three statuses.
    - `system_credential_access_failed` route asserts `increments_feed_budget is False` and `quarantines_feed is False`.
    - Mismatch tests assert telemetry-gap fallback for the new statuses.
  </acceptance_criteria>
  <done>Policy routing has explicit split-status rows and remains fail-closed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Verify backend-only scope and known compatibility deferral</name>
  <files>backend/pipeline/storage/feed_store.py, backend/pipeline/ingestion/collectors/failure_classification.py, backend/pipeline/ingestion/failure_policy.py</files>
  <read_first>
    - .planning/phases/04-strict-policy-table-and-status-vocabulary/04-CONTEXT.md
    - .planning/phases/04-strict-policy-table-and-status-vocabulary/04-DISCUSSION-LOG.md
  </read_first>
  <behavior>
    - Focused backend tests pass.
    - Frontend/OpenAPI/generated compatibility remains explicitly deferred, so broad parity tests that require OpenAPI enum sync are not acceptance criteria for this phase.
    - The code diff contains no runtime routing, DB migration, replay, breaker, frontend, or generated API changes.
  </behavior>
  <action>
    Run the focused tests for backend enum, owner mapping, and policy. Inspect the diff for Phase 4 boundaries. Note the OpenAPI/frontend parity follow-up in the execution summary rather than weakening parity tests.
  </action>
  <verify>
    <automated>safe-run -- uv run python -m pytest backend/pipeline/ingestion/tests/test_failure_policy.py backend/pipeline/ingestion/collectors/tests/test_failure_classification.py backend/pipeline/storage/tests/test_feed_store.py::TestFeedStatusReason::test_canonical_reason_values -q -n 0</automated>
    <manual>git -C /home/shuojing/watch-duty-repo/worktrees/quarantine-failure-policy-redesign diff --stat</manual>
  </verify>
  <acceptance_criteria>
    - Focused backend tests pass.
    - No frontend/OpenAPI/generated compatibility files are modified.
    - Execution summary calls out that broad enum parity remains deferred to the follow-up compatibility phase.
  </acceptance_criteria>
  <done>Phase 4 status vocabulary changes are complete within the agreed backend-only boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend enum -> database/API consumers | New backend enum values can surface in persisted status_reason before frontend/OpenAPI compatibility is updated. |
| collector classification -> policy | Owner-scope mapping for split statuses determines whether policy can quarantine. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | `FeedStatusReason` vocabulary | mitigate | Add only D-12 enum values and test canonical backend values. |
| T-04-02-02 | Repudiation | owner mapping for split statuses | mitigate | Add explicit tests for each new status owner scope. |
| T-04-02-03 | Denial of Service | accidental mass quarantine from new statuses | mitigate | Require explicit policy rows plus mismatch telemetry-gap tests. |
| T-04-02-04 | Information Disclosure | compatibility deferral | accept | No new data fields or secrets are added; frontend/OpenAPI enum surfacing is intentionally deferred and documented. |
| T-04-02-05 | Spoofing | status/evidence mismatch | mitigate | New-status mismatches fail closed to telemetry gap rather than trusting status alone. |
</threat_model>

<verification>
Run:

```bash
safe-run -- uv run python -m pytest backend/pipeline/ingestion/tests/test_failure_policy.py backend/pipeline/ingestion/collectors/tests/test_failure_classification.py backend/pipeline/storage/tests/test_feed_store.py::TestFeedStatusReason::test_canonical_reason_values -q -n 0
```

Do not run or update frontend, OpenAPI generation, generated clients, broad API compatibility, DB migration, replay, breaker, Docker, or testcontainers suites for this plan.
</verification>

<success_criteria>
- STAT-11 through STAT-14 have backend enum, owner mapping, and policy coverage.
- TEST-11 and TEST-12 include the split statuses and their mismatch paths.
- `system_credential_access_failed` is intentionally non-budgeted and easy to change by editing one policy row.
- `system_runtime_configuration_invalid` and `system_source_payload_invalid` are budgeted only with matching evidence.
- Frontend/OpenAPI/generated compatibility remains untouched and explicitly deferred.
</success_criteria>

<output>
After execution, create `.planning/phases/04-strict-policy-table-and-status-vocabulary/04-02-SUMMARY.md`.
</output>
