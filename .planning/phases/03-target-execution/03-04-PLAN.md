---
phase: 03-target-execution
plan: "04"
type: execute
wave: 4
depends_on: ["03-02", "03-03"]
files_modified: [model/scripts/sft/score_gemini_sft_checkpoints_online.py, model/tests/gemini_sft/test_checkpoint_scorer.py, model/tests/common/tests/test_drift_guard.py, model/scripts/sft/run_config.example.toml, model/scripts/sft/README.md]
autonomous: true
requirements: [EXEC-01, EXEC-02, EXEC-04, EXEC-06]
user_setup: []
must_haves:
  truths:
    - "D-19/D-20/D-21: Maintained checkpoint scoring, batch eval, and docs all point to shared common.gemini prompt, request, generation, safety, and prior-context helpers."
    - "D-16/D-18: Operator-facing examples document only backend, limit, concurrency, and max_retries for [eval.execution], not retry_sleep_seconds, sync_every, or log_every."
    - "D-06/D-07/D-08: Checkpoint scorer may still discover checkpoint endpoints for the legacy sweep script, but packaged eval uses explicit target model strings and the docs make that boundary clear."
  artifacts:
    - path: "model/scripts/sft/score_gemini_sft_checkpoints_online.py"
      provides: "Legacy checkpoint sweep script delegating online inference to package code"
      contains: "run_online_target_inference"
    - path: "model/tests/common/tests/test_drift_guard.py"
      provides: "Import/use guards for shared Gemini helpers across maintained paths"
      contains: "target_execution"
    - path: "model/scripts/sft/run_config.example.toml"
      provides: "Lightweight [eval.execution] example"
      contains: "[eval.execution]"
    - path: "model/scripts/sft/README.md"
      provides: "Concise target execution and smoke-limit guidance"
      contains: "eval.execution"
  key_links:
    - from: "model/scripts/sft/score_gemini_sft_checkpoints_online.py"
      to: "model/src/gemini_sft/target_execution.py"
      via: "shared online target executor"
      pattern: "run_online_target_inference"
---

<objective>
Close the drift gap between the legacy checkpoint scorer, packaged eval, and
operator-facing examples.

Purpose: A maintainer can verify that online checkpoint scoring and
`gemini-sft eval` use the same packaged online execution and the same shared
Gemini helper APIs.

Output: Checkpoint scorer delegation, drift guards, and minimal docs/examples
for `[eval.execution]`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-target-execution/03-CONTEXT.md
@.planning/phases/03-target-execution/03-RESEARCH.md
@.planning/phases/03-target-execution/03-PATTERNS.md
@.planning/phases/03-target-execution/03-02-PLAN.md
@.planning/phases/03-target-execution/03-03-PLAN.md
@.planning/codebase/CONVENTIONS.md
@AGENTS.md
@.github/instructions/PYTHON_STYLE.instructions.md
@model/scripts/sft/score_gemini_sft_checkpoints_online.py
@model/src/gemini_sft/target_execution.py
@model/tests/gemini_sft/test_checkpoint_scorer.py
@model/tests/common/tests/test_drift_guard.py
@model/scripts/sft/run_config.example.toml
@model/scripts/sft/README.md
</context>

<threat_model>
Trust boundaries:
- The legacy checkpoint sweep script remains executable and can spend online
  Vertex calls.
- Operator docs and examples are copied into real run configs.
- Drift guards are the cheap preflight check before paid experiments.

STRIDE register:
- EXEC-T10 prompt/request drift: A script-local online request path can diverge
  from packaged eval. Mitigation: checkpoint scorer imports and calls
  `run_online_target_inference` and drift tests assert shared helper imports.
- EXEC-T11 config surface spoofing: Examples can imply hidden execution knobs.
  Mitigation: docs/examples show only `backend`, `limit`, `concurrency`, and
  `max_retries` under `[eval.execution]`.
- EXEC-T12 checkpoint metadata confusion: The legacy sweep script still has
  tuning-job checkpoint discovery, but packaged eval does not. Mitigation:
  README names the sweep script as legacy/discovery-specific and tells
  packaged eval users to list checkpoints explicitly in `[[eval.models]]`.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Delegate checkpoint online scoring to package code</name>
  <files>model/scripts/sft/score_gemini_sft_checkpoints_online.py, model/tests/gemini_sft/test_checkpoint_scorer.py</files>
  <read_first>model/scripts/sft/score_gemini_sft_checkpoints_online.py, model/src/gemini_sft/target_execution.py, model/tests/gemini_sft/test_checkpoint_scorer.py, .planning/phases/03-target-execution/03-CONTEXT.md</read_first>
  <action>Update `score_gemini_sft_checkpoints_online.py` so checkpoint endpoint rows are produced by `gemini_sft.target_execution.run_online_target_inference` instead of maintaining a script-local generate-content loop. Keep tuning-job checkpoint discovery, checkpoint ranking, and summary rendering in the script. Preserve checkpoint metadata by adding `checkpoint_id`, `epoch`, and `step` to the target metadata or ranking rows after package inference returns; do not add those fields to the package request identity. Remove or stop using script-local `generate_with_retries`, `append_prediction`, and `upload_predictions` if they become dead code. Update tests so the scorer patches the package executor and verifies the script passes histories, prompts, prior context mode, concurrency, max_retries, and endpoint model string through.</action>
  <verify>safe-run -- bash -lc 'cd model && PYTHONPATH=src:tests:scripts/sft python3 -m pytest tests/gemini_sft/test_checkpoint_scorer.py -q'</verify>
  <acceptance_criteria>
    - `model/scripts/sft/score_gemini_sft_checkpoints_online.py` imports `run_online_target_inference`
    - `model/scripts/sft/score_gemini_sft_checkpoints_online.py` does not define `generate_with_retries`
    - `model/tests/gemini_sft/test_checkpoint_scorer.py` contains `run_online_target_inference`
    - `model/tests/gemini_sft/test_checkpoint_scorer.py` contains `max_retries`
    - The targeted pytest command exits 0.
  </acceptance_criteria>
  <done>The checkpoint sweep script shares the packaged online executor and no longer owns request construction.</done>
</task>

<task type="auto">
  <name>Task 2: Extend drift guards for maintained Gemini paths</name>
  <files>model/tests/common/tests/test_drift_guard.py</files>
  <read_first>model/tests/common/tests/test_drift_guard.py, model/src/gemini_sft/evaluate.py, model/src/gemini_sft/target_execution.py, model/src/common/gemini/tuning_data.py, model/scripts/sft/score_gemini_sft_checkpoints_online.py, model/colabs/gemini_transcribe_audio.ipynb</read_first>
  <action>Extend `model/tests/common/tests/test_drift_guard.py` with AST-based checks that `gemini_sft.evaluate` imports `build_context_histories`, `gemini_sft.target_execution` imports `build_request`, `GEMINI_GENERATION_CONFIG`, and `GEMINI_SAFETY_SETTINGS`, `common.gemini.tuning_data` imports or calls the shared prompt/request helpers it already uses for SFT JSONL, and `score_gemini_sft_checkpoints_online.py` imports `run_online_target_inference` from package code. Keep the existing notebook prompt/request import tests. These tests must be pure file reads and must not import google-genai or contact GCP.</action>
  <verify>safe-run -- bash -lc 'cd model && PYTHONPATH=src:tests python3 -m pytest tests/common/tests/test_drift_guard.py -q'</verify>
  <acceptance_criteria>
    - `model/tests/common/tests/test_drift_guard.py` contains `target_execution`
    - `model/tests/common/tests/test_drift_guard.py` contains `run_online_target_inference`
    - `model/tests/common/tests/test_drift_guard.py` contains `GEMINI_SAFETY_SETTINGS`
    - The targeted pytest command exits 0.
  </acceptance_criteria>
  <done>Maintained paths have cheap tests preventing prompt/request/helper drift.</done>
</task>

<task type="auto">
  <name>Task 3: Document execution config briefly</name>
  <files>model/scripts/sft/run_config.example.toml, model/scripts/sft/README.md</files>
  <read_first>model/scripts/sft/run_config.example.toml, model/scripts/sft/README.md, .planning/phases/03-target-execution/03-CONTEXT.md, .planning/phases/03-target-execution/03-PATTERNS.md</read_first>
  <action>Update `model/scripts/sft/run_config.example.toml` with a concise commented `[eval.execution]` block showing `backend = "online"` as an optional forced backend, `limit = 100` as an optional smoke row cap, `concurrency = 16`, and `max_retries = 3`. Add comments that omitted backend means default routing, publisher/model IDs default to batch, full endpoint resources default to online, and separate configs are the Phase 3 way to force different backends. Do not mention or add `retry_sleep_seconds`, `sync_every`, or `log_every`. Update `model/scripts/sft/README.md` with a short `gemini-sft eval` target execution section explaining the same field surface, smoke-limit semantics, and that the checkpoint sweep script is only for tuning-job checkpoint discovery while packaged eval uses explicit `[[eval.models]]` entries.</action>
  <verify>rg -n "\\[eval\\.execution\\]|backend = \"online\"|limit = 100|retry_sleep_seconds|sync_every|log_every|run_online_target_inference" model/scripts/sft/run_config.example.toml model/scripts/sft/README.md model/scripts/sft/score_gemini_sft_checkpoints_online.py</verify>
  <acceptance_criteria>
    - `model/scripts/sft/run_config.example.toml` contains `[eval.execution]`
    - `model/scripts/sft/run_config.example.toml` contains `backend = "online"`
    - `model/scripts/sft/run_config.example.toml` contains `limit = 100`
    - `model/scripts/sft/run_config.example.toml` does not contain `retry_sleep_seconds`
    - `model/scripts/sft/run_config.example.toml` does not contain `sync_every`
    - `model/scripts/sft/run_config.example.toml` does not contain `log_every`
    - `model/scripts/sft/README.md` contains `eval.execution`
  </acceptance_criteria>
  <done>Operators can see the Phase 3 execution config surface without full Phase 5 docs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m py_compile model/scripts/sft/score_gemini_sft_checkpoints_online.py`
- [ ] `safe-run -- bash -lc 'cd model && PYTHONPATH=src:tests:scripts/sft python3 -m pytest tests/gemini_sft/test_checkpoint_scorer.py tests/common/tests/test_drift_guard.py -q'`
- [ ] `rg -n "run_online_target_inference|eval.execution|retry_sleep_seconds|sync_every|log_every" model/scripts/sft/score_gemini_sft_checkpoints_online.py model/tests/common/tests/test_drift_guard.py model/scripts/sft/run_config.example.toml model/scripts/sft/README.md`
</verification>

<success_criteria>
- Legacy checkpoint scoring delegates online inference to package code.
- Drift guards cover package eval, online execution, checkpoint scorer, SFT data generation, and maintained notebook imports.
- Examples expose only the approved `[eval.execution]` fields.
- README clearly separates explicit packaged eval targets from legacy checkpoint discovery.
</success_criteria>

<output>
After completion, create `.planning/phases/03-target-execution/03-04-SUMMARY.md`.
</output>

