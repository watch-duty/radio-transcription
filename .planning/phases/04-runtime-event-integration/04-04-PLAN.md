---
phase: 04-runtime-event-integration
plan: "04"
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - documentation/feed-audit-events.md
  - backend/pipeline/storage/tests/test_feed_store.py
  - backend/pipeline/storage/tests/test_sync_feed_store.py
  - backend/pipeline/storage/tests/test_feed_query_contracts.py
  - backend/pipeline/ingestion/tests/test_collector_runtime.py
  - backend/pipeline/ingestion/collectors/echo/tests/test_main.py
autonomous: true
requirements: [AUD-01, EVT-06, EVT-07, EVT-08, EVT-09, DIAG-02, DIAG-03, ACT-03, COMP-04]
must_haves:
  truths:
    - "Repository documentation describes the implemented Phase 4 runtime audit semantics, not only the Phase 1 contract boundary."
    - "Focused tests cover the cross-path contract: failure, quarantine, recovery, and no-noise behavior."
    - "No runtime or Echo code inserts feed_audit_events directly."
    - "No new system: actor prefix appears in runtime audit implementation or docs."
    - "Phase 4 does not add retention jobs, admin timeline APIs, Watch Duty delivery, or event sourcing."
    - "D-01: Failure audit rows are written only for meaningful status/status_reason combo changes."
    - "D-02: Diagnostic-detail churn alone remains no-event maintenance."
    - "D-03: Non-terminal abnormal combo changes emit feed.failure_reported."
    - "D-04: Threshold crossing emits exactly one feed.quarantined event."
    - "D-05: Repeated attempts with the same persisted combo do not emit new events."
    - "D-06: Runtime success emits feed.recovered only when it clears prior abnormal state."
    - "D-07: Recovery abnormal statuses are failing and quarantined; admin reset remains feed.reset."
    - "D-08: Claiming or leasing a failing feed is not recovery."
    - "D-09: Detail-only clearing while status was normal is not recovery."
    - "D-10: Later lease release after recovery remains unaudited lease churn."
    - "D-11: Echo/sync ingestion gets full v1 audit parity now."
    - "D-12: Echo sync mechanics may differ but contract, snapshots, actors, and diagnostics match async storage."
    - "D-13: Echo skipped quarantined or deactivated feeds emit no audit event when no mutation occurs."
    - "D-14: Echo success for a failing feed emits feed.recovered when it returns to normal state."
    - "D-15: Runtime audit events use service:collector-runtime and service:echo-ingestion actor IDs."
    - "D-16: Phase 4 does not use the removed system: actor prefix."
    - "D-17: Source type is not encoded into actor_id."
    - "D-18: gcp-sa actor IDs remain fallback-only, not normal runtime actors."
    - "D-19: GCP service-account provenance does not replace semantic service actors."
    - "D-20: Admin caller service claims remain separate from human causal actor context."
    - "D-21: Runtime abnormal writes set bounded status_reason_detail."
    - "D-22: status_reason_detail follows status_reason lifecycle."
    - "D-23: Persisted diagnostic detail is bounded and sanitized."
    - "D-24: quarantine_reason remains legacy compatibility-only."
    - "D-25: Lease acquisition, release, heartbeat renewal, clean progress, and clean observations do not emit default audit events."
    - "D-26: Clean success can update current state without audit."
    - "D-27: Ambiguous scheduler mechanics default to no event."
  artifacts:
    - path: "documentation/feed-audit-events.md"
      provides: "Updated runtime event semantics and compatibility boundaries"
      contains: "feed.failure_reported"
    - path: "backend/pipeline/storage/tests/test_feed_store.py"
      provides: "Async storage contract coverage"
    - path: "backend/pipeline/storage/tests/test_sync_feed_store.py"
      provides: "Sync parity contract coverage"
    - path: "backend/pipeline/ingestion/tests/test_collector_runtime.py"
      provides: "Async runtime wiring/no-noise coverage"
    - path: "backend/pipeline/ingestion/collectors/echo/tests/test_main.py"
      provides: "Echo wiring/no-noise coverage"
  key_links:
    - from: "documentation/feed-audit-events.md"
      to: "backend/pipeline/storage/feed_store.py"
      via: "documented runtime action vocabulary"
      pattern: "feed.recovered"
    - from: "documentation/feed-audit-events.md"
      to: "backend/pipeline/storage/sync_feed_store.py"
      via: "Echo parity note"
      pattern: "service:echo-ingestion"
---

<objective>
Harden the Phase 4 contract with documentation and end-to-end focused checks.

Purpose: Plans 04-01 through 04-03 implement the mechanics. This plan closes
the phase by making the documented contract match the code and by running a
focused verification suite that proves the meaningful-event and no-noise
requirements across async and sync paths.

Output: updated audit documentation, cross-path contract assertions, and a
focused verification record suitable for execution summary.
</objective>

<execution_context>
@/home/shuojing/.codex/get-shit-done/workflows/execute-plan.md
@/home/shuojing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-runtime-event-integration/04-CONTEXT.md
@.planning/phases/04-runtime-event-integration/04-RESEARCH.md
@.planning/phases/04-runtime-event-integration/04-PATTERNS.md
@.planning/phases/04-runtime-event-integration/04-01-PLAN.md
@.planning/phases/04-runtime-event-integration/04-02-PLAN.md
@.planning/phases/04-runtime-event-integration/04-03-PLAN.md
@documentation/feed-audit-events.md
@backend/pipeline/storage/feed_store.py
@backend/pipeline/storage/sync_feed_store.py
@backend/pipeline/ingestion/collector_runtime.py
@backend/pipeline/ingestion/collectors/echo/main.py
</context>

<decision_coverage>
- D-01: Contract tests cover failure events only for meaningful combo changes.
- D-02: Contract tests cover diagnostic-detail churn as no-event maintenance.
- D-03: Contract tests cover `feed.failure_reported`.
- D-04: Contract tests cover one `feed.quarantined` threshold event.
- D-05: Contract tests cover same-combo retry suppression.
- D-06: Contract tests cover `feed.recovered` after successful clearing.
- D-07: Documentation distinguishes recovery from reset.
- D-08: Tests and docs keep claim-time recovery out of scope.
- D-09: Tests cover detail-only clear as not recovery.
- D-10: Documentation and tests keep later lease release unaudited.
- D-11: Documentation and tests cover Echo parity.
- D-12: Documentation notes sync mechanics with the same contract.
- D-13: Tests cover skipped terminal Echo feeds as no mutation/no audit.
- D-14: Tests cover Echo success recovery from failing state.
- D-15: Documentation and tests cover runtime semantic actors.
- D-16: Static checks cover no new `system:` actor prefix.
- D-17: Documentation keeps source type out of actor IDs.
- D-18: Documentation preserves GCP service-account fallback semantics.
- D-19: Documentation keeps GCP provenance separate from semantic actors.
- D-20: Documentation keeps admin caller claims separate from runtime actors.
- D-21: Tests cover canonical detail on abnormal writes.
- D-22: Tests cover detail set/clear lifecycle.
- D-23: Tests cover bounded sanitized detail.
- D-24: Documentation keeps `quarantine_reason` compatibility-only.
- D-25: Tests cover no lease/heartbeat/clean progress noise.
- D-26: Tests cover clean success current-state updates without audit.
- D-27: Documentation keeps ambiguous scheduler mechanics no-event by default.
- AUD-01, EVT-06, EVT-07, EVT-08, EVT-09, DIAG-02, DIAG-03, ACT-03, COMP-04:
  verification must show all phase requirements are implemented or explicitly
  deferred outside Phase 4 scope.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Update Feed Audit Events documentation for runtime implementation</name>
  <read_first>
@documentation/feed-audit-events.md
@.planning/phases/04-runtime-event-integration/04-CONTEXT.md
@backend/pipeline/storage/feed_store.py
@backend/pipeline/storage/sync_feed_store.py
  </read_first>
  <files>documentation/feed-audit-events.md</files>
  <action>
    Update the phase-boundary and diagnostic-detail sections so they no longer
    describe runtime event emission as entirely future work. Document the
    implemented Phase 4 semantics:
    `feed.failure_reported`, `feed.quarantined`, `feed.recovered`, no routine
    lease/heartbeat/clean-progress events, semantic runtime actors, Echo parity,
    and `quarantine_reason` compatibility-only status.

    Keep the documentation clear that retention, admin timeline read APIs,
    Watch Duty delivery, and event sourcing remain out of Phase 4.
  </action>
  <verify>
    <automated>git diff --check documentation/feed-audit-events.md</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "service:collector-runtime|service:echo-ingestion|feed.failure_reported|feed.quarantined|feed.recovered|quarantine_reason" documentation/feed-audit-events.md` finds the updated runtime contract.
    `rg -n "Phase 1 does not implement runtime event emission" documentation/feed-audit-events.md` returns no stale boundary statement.
  </acceptance_criteria>
  <done>Documentation matches the implemented Phase 4 runtime audit contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add cross-path invariant tests for meaningful events and noise suppression</name>
  <read_first>
@backend/pipeline/storage/tests/test_feed_store.py
@backend/pipeline/storage/tests/test_sync_feed_store.py
@backend/pipeline/storage/tests/test_feed_query_contracts.py
@backend/pipeline/ingestion/tests/test_collector_runtime.py
@backend/pipeline/ingestion/collectors/echo/tests/test_main.py
  </read_first>
  <files>backend/pipeline/storage/tests/test_feed_store.py, backend/pipeline/storage/tests/test_sync_feed_store.py, backend/pipeline/storage/tests/test_feed_query_contracts.py, backend/pipeline/ingestion/tests/test_collector_runtime.py, backend/pipeline/ingestion/collectors/echo/tests/test_main.py</files>
  <action>
    Review the tests added by 04-01 through 04-03 and add any missing invariant
    coverage. Required invariants: failure events only for new logical
    `(status, status_reason)` combinations, quarantine emits exactly one
    `feed.quarantined` event, recovery requires prior abnormal status plus
    successful clearing, detail-only clearing while prior status was normal is
    not recovery, clean heartbeat/progress/source-observation paths are
    unaudited, Echo skipped terminal feeds are unaudited, and both runtime actor
    constants are covered.
  </action>
  <verify>
    <automated>safe-run -- python3 -m pytest backend/pipeline/storage/tests/test_feed_store.py backend/pipeline/storage/tests/test_sync_feed_store.py backend/pipeline/storage/tests/test_feed_query_contracts.py backend/pipeline/ingestion/tests/test_collector_runtime.py backend/pipeline/ingestion/collectors/echo/tests/test_main.py -q</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "same.*status_reason|feed.quarantined|feed.recovered|service:collector-runtime|service:echo-ingestion|status_reason_detail" backend/pipeline/storage/tests backend/pipeline/ingestion/tests backend/pipeline/ingestion/collectors/echo/tests` finds coverage for phase-critical invariants.
    No test asserts audit rows are inserted by runtime or Echo handler code directly.
  </acceptance_criteria>
  <done>Cross-path tests prove Phase 4's meaningful-event and no-noise contract.</done>
</task>

<task type="auto">
  <name>Task 3: Run final focused verification and static sanity checks</name>
  <read_first>
@AGENTS.md
@.agents/instructions.md
@.planning/phases/04-runtime-event-integration/04-CONTEXT.md
  </read_first>
  <files>documentation/feed-audit-events.md, backend/pipeline/storage/tests/test_feed_store.py, backend/pipeline/storage/tests/test_sync_feed_store.py, backend/pipeline/storage/tests/test_feed_query_contracts.py, backend/pipeline/ingestion/tests/test_collector_runtime.py, backend/pipeline/ingestion/collectors/echo/tests/test_main.py</files>
  <action>
    Run the focused Phase 4 verification suite. Use `safe-run --` for pytest per
    repository host-stability guidance. Do not run broad Docker/testcontainer or
    end-to-end suites unless the user explicitly asks; the repo instructions
    warn those are expensive.

    Also run `git diff --check` and targeted grep checks for direct runtime
    audit insertion and banned `system:` actor usage in modified runtime/sync
    implementation files.
  </action>
  <verify>
    <automated>safe-run -- python3 -m pytest backend/pipeline/storage/tests/test_feed_lifecycle.py backend/pipeline/storage/tests/test_feed_query_contracts.py backend/pipeline/storage/tests/test_feed_store.py backend/pipeline/storage/tests/test_sync_feed_store.py backend/pipeline/ingestion/tests/test_collector_runtime.py backend/pipeline/ingestion/tests/test_chunk_ingested.py backend/pipeline/ingestion/collectors/echo/tests/test_main.py backend/pipeline/ingestion/collectors/echo/tests/test_echo_collector_integration.py -q</automated>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
    Focused pytest command passes, or any failure is documented in the phase summary with exact failing tests and reason.
    `rg -n "INSERT INTO feed_audit_events|feed_audit_events" backend/pipeline/ingestion backend/pipeline/ingestion/collectors/echo` finds no direct runtime/Echo audit insertion.
    `rg -n "system:" backend/pipeline/storage/feed_store.py backend/pipeline/storage/sync_feed_store.py backend/pipeline/ingestion/collector_runtime.py backend/pipeline/ingestion/collectors/echo/main.py documentation/feed-audit-events.md` finds no newly introduced `system:` actor prefix.
  </acceptance_criteria>
  <done>Phase 4 has a focused verification result and no obvious direct-insert or actor-prefix regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation -> future implementers | Documentation must not preserve stale phase-boundary claims that contradict code. |
| Tests -> runtime contract | Tests encode the intended no-noise and event-selection boundaries. |
| Runtime code -> audit storage | Static sanity checks ensure runtime handlers do not bypass storage ownership. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Repudiation | Documentation drift | mitigate | Update docs to state runtime actor and event semantics after implementation. |
| T-04-04-02 | Denial of Service | Event volume regression | mitigate | Add invariant tests and grep checks for no-noise boundaries. |
| T-04-04-03 | Tampering | Audit ownership | mitigate | Verify runtime and Echo code do not insert `feed_audit_events` directly. |
| T-04-04-04 | Spoofing | Actor vocabulary | mitigate | Verify no new `system:` actor prefix appears in modified implementation/docs. |
</threat_model>

<risks_dependencies>
- Depends on 04-01 through 04-03 being implemented first.
- Phase 5 still owns retention and broader verification requirements; do not
  expand Phase 4 into retention jobs or admin timeline APIs.
- Some exact grep checks may need adjustment if documentation quotes historical
  decisions; prefer preserving the actor rule while not deleting useful history.
</risks_dependencies>

<verification>
Run final focused Phase 4 checks:

```bash
safe-run -- python3 -m pytest \
  backend/pipeline/storage/tests/test_feed_lifecycle.py \
  backend/pipeline/storage/tests/test_feed_query_contracts.py \
  backend/pipeline/storage/tests/test_feed_store.py \
  backend/pipeline/storage/tests/test_sync_feed_store.py \
  backend/pipeline/ingestion/tests/test_collector_runtime.py \
  backend/pipeline/ingestion/tests/test_chunk_ingested.py \
  backend/pipeline/ingestion/collectors/echo/tests/test_main.py \
  backend/pipeline/ingestion/collectors/echo/tests/test_echo_collector_integration.py \
  -q
git diff --check
```
</verification>

<success_criteria>
- Documentation and tests reflect the implemented runtime audit contract.
- Focused Phase 4 verification passes or any failure is explicitly recorded.
- There are no direct runtime/Echo audit inserts and no new `system:` actors.
- Scope remains limited to runtime event integration and Echo parity.
</success_criteria>
