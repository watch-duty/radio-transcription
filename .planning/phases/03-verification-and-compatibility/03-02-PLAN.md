---
phase: 03-verification-and-compatibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/api/openapi.yaml
  - frontend/common/src/types/feeds.ts
  - frontend/common/src/utils/statusUtils.ts
  - frontend/transcription-ui/src/components/common/FeedStatusIndicator.tsx
autonomous: true
requirements: [STAT-02]
user_setup: []
must_haves:
  truths:
    - "D-01: `pipeline_publish_after_bookmark_failed` is tolerated as a backend status reason, not a lifecycle redesign trigger."
    - "D-02: Backend/OpenAPI parity and shared TypeScript compatibility include `pipeline_publish_after_bookmark_failed`."
    - "D-03: No frontend test is added solely for the pipeline publish-after-bookmark reason."
    - "D-04: `failing` and `quarantined` continue to map to UI `error`."
  artifacts:
    - path: frontend/api/openapi.yaml
      provides: "OpenAPI BackendFeedStatusReason enum parity."
    - path: frontend/common/src/types/feeds.ts
      provides: "Shared BackendFeedStatusReason literal compatibility."
    - path: frontend/common/src/utils/statusUtils.ts
      provides: "Runtime conversion allowlist and lifecycle status mapping."
    - path: frontend/transcription-ui/src/components/common/FeedStatusIndicator.tsx
      provides: "Operator-facing display text for the tolerated status reason."
  key_links:
    - from: backend/pipeline/storage/feed_store.py
      to: frontend/api/openapi.yaml
      via: "TestFeedStatusReason.test_matches_openapi_spec"
      pattern: "pipeline_publish_after_bookmark_failed"
    - from: frontend/common/src/types/feeds.ts
      to: frontend/common/src/utils/statusUtils.ts
      via: "BackendFeedStatusReason allowlist"
      pattern: "BACKEND_FEED_STATUS_REASONS"
    - from: frontend/common/src/utils/statusUtils.ts
      to: frontend/transcription-ui/src/components/common/FeedStatusIndicator.tsx
      via: "failing/quarantined -> error lifecycle mapping with reason display"
      pattern: "case 'quarantined':|case 'failing':"
---

<objective>
Update status compatibility surfaces only where required.

Purpose: Shared API/UI/status surfaces must tolerate the new backend status
reason without changing feed lifecycle semantics or adding a frontend-only test
for this reason.
Output: OpenAPI and shared/frontend status surfaces aligned with
`pipeline_publish_after_bookmark_failed`.
</objective>

<execution_context>
@/home/shuojing/.codex/get-shit-done/workflows/execute-plan.md
@/home/shuojing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-verification-and-compatibility/03-CONTEXT.md
@.planning/phases/03-verification-and-compatibility/03-PATTERNS.md
@.planning/phases/01-policy-and-storage-foundation/01-CONTEXT.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@backend/pipeline/storage/feed_store.py
@backend/pipeline/storage/tests/test_feed_store.py
@frontend/api/openapi.yaml
@frontend/common/src/types/feeds.ts
@frontend/common/src/utils/statusUtils.ts
@frontend/transcription-ui/src/components/common/FeedStatusIndicator.tsx
@frontend/transcription-ui/src/components/common/FeedStatusIndicator.test.tsx

<interfaces>
The compatibility target is this exact backend reason value:

```text
pipeline_publish_after_bookmark_failed
```

Lifecycle mapping must remain:

```typescript
case 'quarantined':
case 'failing':
  return 'error';
```

The UI display text, when the reason is present, must be:

```typescript
'Pipeline Publish Failed After Bookmark'
```
</interfaces>
</context>

<must_haves>
<truth>D-01/D-02: Compatibility means backend/OpenAPI parity and TypeScript compile compatibility, not a new operator workflow.</truth>
<truth>D-03: Leave `FeedStatusIndicator.test.tsx` untouched unless an existing test fails because of this compatibility update.</truth>
<truth>D-04: Preserve `failing` and `quarantined` as UI `error` lifecycle states.</truth>
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Align OpenAPI status reason parity</name>
  <files>frontend/api/openapi.yaml</files>
  <read_first>
    - .planning/phases/03-verification-and-compatibility/03-CONTEXT.md
    - .planning/phases/03-verification-and-compatibility/03-PATTERNS.md
    - backend/pipeline/storage/feed_store.py
    - backend/pipeline/storage/tests/test_feed_store.py
    - frontend/api/openapi.yaml
  </read_first>
  <behavior>
    - STAT-02: `components.schemas.BackendFeedStatusReason.enum` includes `pipeline_publish_after_bookmark_failed`.
    - STAT-02: `components.schemas.FeedStatus.enum` is not expanded with a pipeline-specific lifecycle value.
    - STAT-02: Backend parity remains enforced by `TestFeedStatusReason.test_matches_openapi_spec`.
  </behavior>
  <action>
    Make `frontend/api/openapi.yaml` match the backend `FeedStatusReason` enum plus the existing OpenAPI `unknown` compatibility value. Ensure `BackendFeedStatusReason` contains `pipeline_publish_after_bookmark_failed`. Do not add this value to `FeedStatus` or any lifecycle-status schema. Do not add a DB migration, new API route, or operator workflow.
  </action>
  <verify>
    <automated>safe-run -- uv run python -m pytest backend/pipeline/storage/tests/test_feed_store.py::TestFeedStatusReason -q -n 0</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pipeline_publish_after_bookmark_failed" frontend/api/openapi.yaml backend/pipeline/storage/feed_store.py` finds the exact value in both backend and OpenAPI surfaces.
    - `rg -n "BackendFeedStatusReason" frontend/api/openapi.yaml` finds the OpenAPI status-reason schema.
    - The targeted `TestFeedStatusReason` pytest command passes.
  </acceptance_criteria>
  <done>STAT-02 backend/OpenAPI parity holds for the new status reason.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve shared TypeScript and UI lifecycle compatibility</name>
  <files>frontend/common/src/types/feeds.ts, frontend/common/src/utils/statusUtils.ts, frontend/transcription-ui/src/components/common/FeedStatusIndicator.tsx</files>
  <read_first>
    - .planning/phases/03-verification-and-compatibility/03-CONTEXT.md
    - .planning/phases/03-verification-and-compatibility/03-PATTERNS.md
    - frontend/common/src/types/feeds.ts
    - frontend/common/src/utils/statusUtils.ts
    - frontend/transcription-ui/src/components/common/FeedStatusIndicator.tsx
    - frontend/transcription-ui/src/components/common/FeedStatusIndicator.test.tsx
  </read_first>
  <behavior>
    - STAT-02: `BackendFeedStatusReason` includes the literal `'pipeline_publish_after_bookmark_failed'`.
    - STAT-02: `BACKEND_FEED_STATUS_REASONS` includes `'pipeline_publish_after_bookmark_failed'`.
    - STAT-02: `FEED_STATUS_REASON_UI_TEXT_DISPLAY` maps `pipeline_publish_after_bookmark_failed` to `Pipeline Publish Failed After Bookmark`.
    - D-04: `convertFeedStatusBackend('failing')` and `convertFeedStatusBackend('quarantined')` continue to return `error`.
    - D-03: `FeedStatusIndicator.test.tsx` remains unchanged unless an existing test fails after the compatibility edits.
  </behavior>
  <action>
    Make shared TypeScript and UI status handling tolerate `pipeline_publish_after_bookmark_failed` with the exact display text `Pipeline Publish Failed After Bookmark`. Preserve the existing `BackendFeedStatus` union, `FeedStatus` union, and `convertFeedStatusBackend(...)` lifecycle mapping. Preserve `mapFeedStatusToBackendStatuses('error')` returning both `failing` and `quarantined`. Do not add frontend tests solely for this reason; if an existing frontend test fails, repair that existing expectation without adding a pipeline-specific test case.
  </action>
  <verify>
    <automated>safe-run -- yarn --cwd frontend/common build</automated>
    <automated>safe-run -- yarn --cwd frontend/api typecheck</automated>
    <automated>safe-run -- yarn --cwd frontend/transcription-ui typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "'pipeline_publish_after_bookmark_failed'" frontend/common/src/types/feeds.ts frontend/common/src/utils/statusUtils.ts` finds the exact literal in the shared type and allowlist.
    - `rg -n "pipeline_publish_after_bookmark_failed: 'Pipeline Publish Failed After Bookmark'" frontend/transcription-ui/src/components/common/FeedStatusIndicator.tsx` finds the UI display mapping.
    - `rg -n "case 'quarantined':|case 'failing':|return 'error'" frontend/common/src/utils/statusUtils.ts` confirms lifecycle mapping is still present.
    - `git diff -- frontend/transcription-ui/src/components/common/FeedStatusIndicator.test.tsx` is empty unless the executor records an existing-test repair in the summary.
    - The three frontend type/compile commands pass.
  </acceptance_criteria>
  <done>STAT-02 shared status surfaces compile and continue to map `failing` and `quarantined` to UI `error`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend enum -> OpenAPI schema | Backend status reason values cross into generated/API documentation. |
| OpenAPI/shared TS -> UI display | API response status reason values cross into operator-facing UI text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | `frontend/api/openapi.yaml` | mitigate | Run backend enum parity test and require the exact `pipeline_publish_after_bookmark_failed` value in OpenAPI. |
| T-03-02-02 | Information Disclosure | `FeedStatusIndicator.tsx` display text | accept | Display text is a bounded status label and does not expose raw `quarantine_reason` or secrets. |
| T-03-02-03 | Denial of Service | frontend lifecycle mapping | mitigate | Preserve `failing` and `quarantined` mapping to `error`; do not introduce a new lifecycle value that breaks filters. |
| T-03-02-04 | Spoofing | unknown status reason conversion | mitigate | Keep `convertFeedStatusReason(...)` allowlist fallback to `unknown` for unrecognized strings. |
</threat_model>

<verification>
Run the targeted backend parity and frontend type/compile commands from the task `<verify>` blocks. Do not run frontend E2E, component suites, API local stack, Docker, or broad test commands.
</verification>

<success_criteria>
- STAT-02 is satisfied by OpenAPI, shared TypeScript, status conversion, and UI display compatibility.
- No new feed lifecycle status is introduced.
- No frontend test is added solely for `pipeline_publish_after_bookmark_failed`.
- Existing status filters continue to treat `failing` and `quarantined` as UI `error`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-verification-and-compatibility/03-02-SUMMARY.md`.
</output>
