---
phase: 03-verification-and-compatibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pipeline/storage/tests/test_feed_store.py
  - backend/pipeline/ingestion/tests/test_collector_runtime.py
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06, TEST-07, TEST-08]
user_setup: []
must_haves:
  truths:
    - "Storage tests prove non-budgeted release writes status='failing', failure_count=0, retry_after, and status_reason."
    - "Storage tests prove non-budgeted release cannot write quarantine_reason or failure_count + 1."
    - "Runtime tests prove post-bookmark publish failures do not call report_feed_failure(...)."
    - "Runtime tests prove post-bookmark publish failures emit feed_failure_policy_decision and post_bookmark_publish_failure telemetry."
    - "Runtime tests prove source-offline, auth, rate-limit, unknown, and source-class cases use release_non_budgeted_failure(...)."
    - "Runtime tests prove feed-config quarantine still uses report_feed_failure(...)."
    - "Runtime tests prove non-budgeted paths never emit feed_quarantined telemetry."
  artifacts:
    - path: backend/pipeline/storage/tests/test_feed_store.py
      provides: "TEST-01 and TEST-02 storage proof."
    - path: backend/pipeline/ingestion/tests/test_collector_runtime.py
      provides: "TEST-03 through TEST-08 runtime routing and telemetry proof."
  key_links:
    - from: backend/pipeline/storage/tests/test_feed_store.py
      to: backend/pipeline/storage/feed_queries.py
      via: "RELEASE_NON_BUDGETED_FAILURE_SQL assertions"
      pattern: "RELEASE_NON_BUDGETED_FAILURE_SQL"
    - from: backend/pipeline/ingestion/tests/test_collector_runtime.py
      to: backend/pipeline/ingestion/collector_runtime.py
      via: "store-call and json_fields assertions"
      pattern: "release_non_budgeted_failure|report_feed_failure|feed_failure_policy_decision"
---

<objective>
Complete focused storage and runtime tests for the quarantine policy behavior.

Purpose: Phase 3 must prove that suppressed retry paths cannot consume feed
quarantine budget, while feed-owned configuration failures still can.
Output: Hardened backend tests in storage and ingestion runtime test files.
</objective>

<execution_context>
@/home/shuojing/.codex/get-shit-done/workflows/execute-plan.md
@/home/shuojing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-verification-and-compatibility/03-CONTEXT.md
@.planning/phases/03-verification-and-compatibility/03-PATTERNS.md
@.planning/phases/01-policy-and-storage-foundation/01-CONTEXT.md
@.planning/phases/02-runtime-routing-and-telemetry/02-CONTEXT.md
@.planning/phases/02-runtime-routing-and-telemetry/02-VERIFICATION.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@backend/pipeline/storage/feed_store.py
@backend/pipeline/storage/feed_queries.py
@backend/pipeline/storage/tests/test_feed_store.py
@backend/pipeline/ingestion/failure_policy.py
@backend/pipeline/ingestion/collector_runtime.py
@backend/pipeline/ingestion/models.py
@backend/pipeline/ingestion/tests/test_collector_runtime.py

<interfaces>
Storage proof must use the existing budgeted and non-budgeted storage split:

```python
FeedStore.report_feed_failure(...)
FeedStore.release_non_budgeted_failure(...)
feed_queries.RELEASE_NON_BUDGETED_FAILURE_SQL
feed_queries.REPORT_FAILURE_SQL
```

Runtime proof must use store calls as routing evidence before telemetry:

```python
rt._store.report_feed_failure.assert_not_awaited()
rt._store.release_non_budgeted_failure.assert_awaited_once()
mock_telemetry.emit_quarantine_event.assert_not_awaited()
```
</interfaces>
</context>

<must_haves>
<truth>D-05/D-06: Requirement evidence must be traceable from TEST-01 through TEST-08 to exact tests.</truth>
<truth>D-07: Tests must cover non-budgeted release, post-bookmark publish gap, feed-config quarantine, unknown telemetry gap, source/source-class non-quarantine routing, and non-quarantine telemetry suppression.</truth>
<truth>D-08: Do not add one bespoke test per historic incident label; cover shared policy scenarios.</truth>
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove non-budgeted storage cannot consume quarantine budget</name>
  <files>backend/pipeline/storage/tests/test_feed_store.py</files>
  <read_first>
    - .planning/phases/03-verification-and-compatibility/03-CONTEXT.md
    - .planning/phases/03-verification-and-compatibility/03-PATTERNS.md
    - .planning/phases/01-policy-and-storage-foundation/01-CONTEXT.md
    - backend/pipeline/storage/feed_queries.py
    - backend/pipeline/storage/feed_store.py
    - backend/pipeline/storage/tests/test_feed_store.py
  </read_first>
  <behavior>
    - TEST-01: `TestNonBudgetedFailureSql` asserts `RELEASE_NON_BUDGETED_FAILURE_SQL` contains `status = 'failing'::feed_status`, `failure_count = 0`, `retry_after = $4`, `status_reason = $5`, and `worker_id = NULL`.
    - TEST-01: `TestReleaseNonBudgetedFailure` asserts the store method passes `(feed_id, worker_id, fencing_token, retry_after, status_reason.value)` to SQL and returns `"failing"` when the fenced update succeeds.
    - TEST-02: SQL tests assert `RELEASE_NON_BUDGETED_FAILURE_SQL` does not contain `quarantine_reason` or `failure_count + 1`.
  </behavior>
  <action>
    Make the final storage test state prove TEST-01 and TEST-02 exactly. Preserve the existing `TestNonBudgetedFailureSql` and `TestReleaseNonBudgetedFailure` class pattern from 03-PATTERNS. Add assertions only to `backend/pipeline/storage/tests/test_feed_store.py`; do not alter `FeedStore.release_non_budgeted_failure(...)` or `RELEASE_NON_BUDGETED_FAILURE_SQL` unless a test exposes a real mismatch with the Phase 1 decisions. Keep `report_feed_failure(...)` as the only path whose SQL contains `failure_count + 1`.
  </action>
  <verify>
    <automated>safe-run -- uv run python -m pytest backend/pipeline/storage/tests/test_feed_store.py::TestNonBudgetedFailureSql backend/pipeline/storage/tests/test_feed_store.py::TestReleaseNonBudgetedFailure -q -n 0</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "status = 'failing'::feed_status|failure_count = 0|retry_after = \\$4|status_reason = \\$5|worker_id = NULL" backend/pipeline/storage/tests/test_feed_store.py` finds assertions in the non-budgeted SQL tests.
    - `rg -n "assertNotIn\\(\"quarantine_reason\"|assertNotIn\\(\"failure_count \\+ 1\"" backend/pipeline/storage/tests/test_feed_store.py` finds the budget-isolation assertions.
    - The targeted pytest command for `TestNonBudgetedFailureSql` and `TestReleaseNonBudgetedFailure` passes.
  </acceptance_criteria>
  <done>TEST-01 and TEST-02 are backed by narrow storage tests that fail if non-budgeted release writes quarantine budget state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove runtime routing and telemetry scenarios</name>
  <files>backend/pipeline/ingestion/tests/test_collector_runtime.py</files>
  <read_first>
    - .planning/phases/03-verification-and-compatibility/03-CONTEXT.md
    - .planning/phases/03-verification-and-compatibility/03-PATTERNS.md
    - .planning/phases/02-runtime-routing-and-telemetry/02-CONTEXT.md
    - .planning/phases/02-runtime-routing-and-telemetry/02-VERIFICATION.md
    - backend/pipeline/ingestion/failure_policy.py
    - backend/pipeline/ingestion/collector_runtime.py
    - backend/pipeline/ingestion/models.py
    - backend/pipeline/storage/feed_store.py
    - backend/pipeline/ingestion/tests/test_collector_runtime.py
  </read_first>
  <behavior>
    - TEST-03: Pub/Sub post-bookmark publish failure asserts `report_feed_failure.assert_not_awaited()` and `release_non_budgeted_failure.assert_awaited_once()`.
    - TEST-04: The same publish-gap scenario asserts both `feed_failure_policy_decision` and `post_bookmark_publish_failure`, with `policy_intent=hold_for_replay`, `executed_action=suppress_feed_quarantine_record_publish_gap`, `replay_missing=True`, and `data_gap_known=True`.
    - TEST-05: Source-offline, shared-auth, rate-limit, capture-timeout, and source-class failures assert the non-budgeted path.
    - TEST-06: Untyped runtime exceptions assert UNKNOWN telemetry-gap evidence and non-budgeted release.
    - TEST-07: Feed configuration failures assert `report_feed_failure.assert_awaited_once()` and `release_non_budgeted_failure.assert_not_awaited()`.
    - TEST-08: Representative non-budgeted cases assert `quarantine_telemetry.emit_quarantine_event.assert_not_awaited()`.
  </behavior>
  <action>
    Make the final runtime test state prove TEST-03 through TEST-08 exactly. Use existing `TestProcessFeedRetry` and `TestProcessFeedQuarantine` helpers. Store-call assertions are the routing proof; structured `json_fields` assertions are used only to prove telemetry contracts. Filter logs by `event_type` and assert concrete values listed in the behavior block. Per D-08, do not create separate tests for every historic incident label when an existing policy scenario covers that lane.
  </action>
  <verify>
    <automated>safe-run -- uv run python -m pytest backend/pipeline/ingestion/tests/test_collector_runtime.py::TestProcessFeedRetry backend/pipeline/ingestion/tests/test_collector_runtime.py::TestProcessFeedQuarantine -q -n 0</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "report_feed_failure\\.assert_not_awaited\\(\\)|release_non_budgeted_failure\\.assert_awaited_once\\(\\)" backend/pipeline/ingestion/tests/test_collector_runtime.py` finds runtime routing assertions.
    - `rg -n "feed_failure_policy_decision|post_bookmark_publish_failure|hold_for_replay|suppress_feed_quarantine_record_publish_gap|replay_missing|data_gap_known" backend/pipeline/ingestion/tests/test_collector_runtime.py` finds publish-gap telemetry assertions.
    - `rg -n "emit_quarantine_event\\.assert_not_awaited\\(\\)|report_feed_failure\\.assert_awaited_once\\(\\)|release_non_budgeted_failure\\.assert_not_awaited\\(\\)" backend/pipeline/ingestion/tests/test_collector_runtime.py` finds no-quarantine and positive quarantine assertions.
    - The targeted pytest command for `TestProcessFeedRetry` and `TestProcessFeedQuarantine` passes.
  </acceptance_criteria>
  <done>TEST-03 through TEST-08 are backed by narrow runtime tests that prove both routing and telemetry behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| collector/runtime -> storage mock | Tests model untrusted failure evidence crossing into store actions. |
| runtime -> structured logs | Tests model operator-facing telemetry emitted from failure handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | `RELEASE_NON_BUDGETED_FAILURE_SQL` tests | mitigate | Assert non-budgeted SQL contains `failure_count = 0` and does not contain `failure_count + 1` or `quarantine_reason`. |
| T-03-01-02 | Repudiation | `feed_failure_policy_decision` logs | mitigate | Assert stable `event_type`, policy intent, executed action, and evidence fields in runtime tests. |
| T-03-01-03 | Denial of Service | accidental feed quarantine/alert fanout | mitigate | Assert non-budgeted cases do not call `report_feed_failure(...)` and do not emit quarantine telemetry. |
| T-03-01-04 | Information Disclosure | test log assertions | accept | Tests inspect bounded structured fields only; they do not introduce secrets or external log sinks. |
</threat_model>

<verification>
Run the two targeted commands from the task `<verify>` blocks. Do not run broad local E2E, API, Docker, testcontainers, or full integration-stack commands.
</verification>

<success_criteria>
- TEST-01 and TEST-02 pass through storage tests.
- TEST-03 through TEST-08 pass through runtime tests.
- No test uses raw `quarantine_reason` as routing authority.
- No Phase 3 test adds per-incident-label duplication outside the shared policy scenarios.
</success_criteria>

<output>
After completion, create `.planning/phases/03-verification-and-compatibility/03-01-SUMMARY.md`.
</output>
