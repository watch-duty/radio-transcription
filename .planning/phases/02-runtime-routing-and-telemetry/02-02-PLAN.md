---
phase: 02-runtime-routing-and-telemetry
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - backend/pipeline/ingestion/collector_runtime.py
  - backend/pipeline/ingestion/tests/test_collector_runtime.py
autonomous: true
requirements: [RUN-02, RUN-03]
must_haves:
  truths:
    - "Pipeline-owned failures are post-capture system/data movement failures and never feed-health evidence."
    - "Source-class, credential-scope, rate-limit, capture-timeout, source-offline, and unknown decisions use non-budgeted release in v1."
    - "D-08/D-09: retry timing must be present, but exact 5-15 minute bounds are not a policy contract."
    - "D-10: Do not add reason-specific or status-specific backoff in this phase; prove routing and budget isolation only."
  artifacts:
    - path: backend/pipeline/ingestion/collector_runtime.py
      provides: "_PipelineFailure handling and non-budgeted release routing."
    - path: backend/pipeline/ingestion/tests/test_collector_runtime.py
      provides: "Runtime non-budgeted path tests."
  key_links:
    - from: backend/pipeline/ingestion/collector_runtime.py
      to: backend/pipeline/storage/feed_store.py
      via: "release_non_budgeted_failure(...)"
      pattern: "release_non_budgeted_failure"
---

<objective>
Route pipeline and non-actionable source/system failures through suppressed
retry.

All non-feed-actionable failures must use
`FeedStore.release_non_budgeted_failure(...)` with retry timing and must not
consume the feed quarantine budget.
</objective>

<execution_context>
@/home/shuojing/.codex/get-shit-done/workflows/execute-plan.md
@/home/shuojing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-runtime-routing-and-telemetry/02-CONTEXT.md
@.planning/phases/02-runtime-routing-and-telemetry/02-RESEARCH.md
@.planning/phases/02-runtime-routing-and-telemetry/02-PATTERNS.md
@.planning/phases/02-runtime-routing-and-telemetry/02-VALIDATION.md
@backend/pipeline/ingestion/collector_runtime.py
@backend/pipeline/ingestion/tests/test_collector_runtime.py
@backend/pipeline/storage/feed_store.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden pipeline-owned suppressed retry tests</name>
  <files>backend/pipeline/ingestion/collector_runtime.py, backend/pipeline/ingestion/tests/test_collector_runtime.py</files>
  <steps>
  - Keep existing `_PipelineFailure` evidence for GCS upload, bookmark write, and Pub/Sub publish after bookmark if it already matches the policy.
  - Prove GCS upload and bookmark write failures call `release_non_budgeted_failure(...)`, never `report_feed_failure(...)`, with `FeedStatusReason.SYSTEM_PIPELINE_ERROR`.
  - Prove Pub/Sub publish failure after bookmark calls `release_non_budgeted_failure(...)`, never `report_feed_failure(...)`, with `FeedStatusReason.PIPELINE_PUBLISH_AFTER_BOOKMARK_FAILED`.
  - Ensure the post-bookmark publish case does not call `release_feed(...)` as if it were a normal completion.
  </steps>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover non-actionable collector decisions</name>
  <files>backend/pipeline/ingestion/tests/test_collector_runtime.py</files>
  <steps>
  - Add table-driven runtime coverage for source-offline, rate-limit, capture-timeout, shared auth, source-class, and unknown decisions.
  - For each case, construct typed `FeedFailure` with valid policy evidence except the unknown runtime exception case, which should use the generic `Exception` path.
  - Assert `report_feed_failure(...)` is not awaited.
  - Assert `release_non_budgeted_failure(...)` is awaited once with the expected `status_reason`.
  - Assert quarantine telemetry is not emitted for these cases where practical.
  </steps>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verify retry_after without over-specifying timing</name>
  <files>backend/pipeline/ingestion/collector_runtime.py, backend/pipeline/ingestion/tests/test_collector_runtime.py</files>
  <steps>
  - Patch `_non_budgeted_retry_after()` to return a sentinel datetime in routing tests that need exact propagation.
  - Assert that sentinel is passed to `release_non_budgeted_failure(...)`.
  - Do not assert the 5-15 minute jitter bounds in Phase 2 tests.
  - Keep the current coarse runtime default unless a requirement is failing.
  </steps>
</task>

</tasks>

<threat_model>
- Regression: pipeline failures after capture burn feed budget.
  Mitigation: negative `report_feed_failure(...)` assertions for all pipeline stages.
- Regression: post-bookmark publish failure is treated as normal lease release.
  Mitigation: assert `release_feed(...)` is not called in the publish-gap test.
- Maintenance risk: exact retry windows become brittle policy.
  Mitigation: patch retry helper to a sentinel and assert propagation only.
</threat_model>

<verification>
Run:

```bash
safe-run -- uv run python -m pytest \
  backend/pipeline/ingestion/tests/test_collector_runtime.py::TestProcessFeedRetry \
  backend/pipeline/ingestion/tests/test_collector_runtime.py::TestProcessFeedQuarantine \
  -q -n 0
```
</verification>

<success_criteria>
- Pipeline-owned failures use non-budgeted release.
- Post-bookmark Pub/Sub publish failure uses
  `pipeline_publish_after_bookmark_failed`.
- Source-offline, rate-limit, capture-timeout, shared-auth/source-class, and
  unknown paths use non-budgeted release.
- Retry timing is supplied without encoding exact backoff parameters.
</success_criteria>

<output>
Create `.planning/phases/02-runtime-routing-and-telemetry/02-02-SUMMARY.md`
with routing cases covered, tests run, and any intentionally deferred behavior.
</output>
