---
phase: 02-runtime-routing-and-telemetry
plan: 01
type: execute
wave: 1
depends_on:
  - phase-01
files_modified:
  - backend/pipeline/ingestion/failure_policy.py
  - backend/pipeline/ingestion/models.py
  - backend/pipeline/ingestion/collector_runtime.py
  - backend/pipeline/ingestion/tests/test_failure_policy.py
  - backend/pipeline/ingestion/tests/test_collector_runtime.py
autonomous: true
requirements: [POL-04, RUN-01, RUN-07]
must_haves:
  truths:
    - "D-01: Evidence-less FeedFailure remains invalid."
    - "D-02/D-03: Untyped runtime exceptions, not typed evidence-less FeedFailure, route to UNKNOWN telemetry gap."
    - "D-04/D-05: OPEN_BREAKER is an intent only in v1 and executes release_non_budgeted_failure."
    - "D-06: Do not add breaker persistence, canary state, source-class pause mechanics, or alert routing in this phase."
    - "D-07: Tests must assert the OPEN_BREAKER intent/action split so it is not mistaken for executed breaker state."
    - "D-15: Routing proof comes from store method calls, not telemetry."
  artifacts:
    - path: backend/pipeline/ingestion/failure_policy.py
      provides: "Pure policy classifier and predicates."
    - path: backend/pipeline/ingestion/collector_runtime.py
      provides: "Runtime branch that chooses budgeted or non-budgeted store action."
    - path: backend/pipeline/ingestion/tests/test_failure_policy.py
      provides: "Pure classifier guard tests."
    - path: backend/pipeline/ingestion/tests/test_collector_runtime.py
      provides: "Runtime store-call routing tests."
  key_links:
    - from: backend/pipeline/ingestion/collector_runtime.py
      to: backend/pipeline/ingestion/failure_policy.py
      via: "classify_failure_policy(...)"
      pattern: "classify_failure_policy"
---

<objective>
Guard the budgeted quarantine path.

Only feed-owned, feed-actionable `quarantine_feed` decisions may reach
`report_feed_failure(...)`. Feed-owned configuration failures must still be
able to quarantine, and untyped runtime exceptions must use UNKNOWN telemetry
gap evidence instead of legacy quarantine.
</objective>

<execution_context>
@/home/shuojing/.codex/get-shit-done/workflows/execute-plan.md
@/home/shuojing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-runtime-routing-and-telemetry/02-CONTEXT.md
@.planning/phases/02-runtime-routing-and-telemetry/02-RESEARCH.md
@.planning/phases/02-runtime-routing-and-telemetry/02-PATTERNS.md
@.planning/phases/02-runtime-routing-and-telemetry/02-VALIDATION.md
@.planning/phases/01-policy-and-storage-foundation/01-01-SUMMARY.md
@.planning/phases/01-policy-and-storage-foundation/01-02-SUMMARY.md
@.planning/phases/01-policy-and-storage-foundation/01-03-SUMMARY.md
@.planning/phases/01-policy-and-storage-foundation/01-04-SUMMARY.md
@backend/pipeline/ingestion/failure_policy.py
@backend/pipeline/ingestion/models.py
@backend/pipeline/ingestion/collector_runtime.py
@backend/pipeline/ingestion/tests/test_failure_policy.py
@backend/pipeline/ingestion/tests/test_collector_runtime.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden pure policy guard tests</name>
  <files>backend/pipeline/ingestion/failure_policy.py, backend/pipeline/ingestion/tests/test_failure_policy.py</files>
  <steps>
  - Read the existing classifier before editing; Phase 1 may already satisfy most of this.
  - Add or strengthen tests that prove only feed-owned feed-configuration evidence is `feed_budget_eligible=True` and `quarantine_feed=True`.
  - Add or strengthen tests that credential-scope and source-class evidence produce `PolicyIntent.OPEN_BREAKER` plus `ExecutedAction.RELEASE_NON_BUDGETED_FAILURE`.
  - Add or strengthen tests that UNKNOWN evidence produces `PolicyIntent.TELEMETRY_GAP` plus `ExecutedAction.SUPPRESS_FEED_QUARANTINE_TELEMETRY_GAP`.
  - Do not add `reason_family`; Phase 1 intentionally avoided it.
  </steps>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove runtime budgeted path is guarded by decision</name>
  <files>backend/pipeline/ingestion/collector_runtime.py, backend/pipeline/ingestion/tests/test_collector_runtime.py</files>
  <steps>
  - Add a runtime test that raises a `FeedFailure` with feed-owned configuration evidence and proves `report_feed_failure(...)` is called.
  - Add a runtime test that raises a `FeedFailure` with source-class or credential-scope evidence and proves `report_feed_failure(...)` is not called while `release_non_budgeted_failure(...)` is called.
  - If the current `_process_feed` branch already implements this routing, keep the code unchanged and add only missing tests.
  - If code changes are required, keep them local to the existing `FeedFailure` catch arm and existing `_is_feed_quarantine_decision(...)` helper.
  </steps>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reconcile POL-04 wording with the strict FeedFailure contract</name>
  <files>backend/pipeline/ingestion/models.py, backend/pipeline/ingestion/tests/test_collector_runtime.py</files>
  <steps>
  - Preserve the existing `TestFeedFailureContract.test_rejects_missing_policy_evidence` behavior.
  - Rename or adjust any misleading test/docstring that suggests evidence-less typed `FeedFailure` should be accepted.
  - Ensure the telemetry-gap runtime test uses an untyped runtime `Exception` path for UNKNOWN fallback, not an evidence-less `FeedFailure`.
  - Keep status reason normalization tests for typed `FeedFailure` with evidence.
  </steps>
</task>

</tasks>

<threat_model>
- Regression: a shared or unknown failure can still call `report_feed_failure(...)`.
  Mitigation: store-call assertions for each representative owner scope.
- Regression: a real feed configuration problem can no longer quarantine.
  Mitigation: keep a positive feed-config quarantine test.
- Drift: tests imply old evidence-less `FeedFailure` behavior.
  Mitigation: keep strict model-boundary rejection and use untyped exception for telemetry gap.
</threat_model>

<verification>
Run:

```bash
safe-run -- uv run python -m pytest \
  backend/pipeline/ingestion/tests/test_failure_policy.py \
  backend/pipeline/ingestion/tests/test_collector_runtime.py::TestFeedFailureContract \
  backend/pipeline/ingestion/tests/test_collector_runtime.py::TestProcessFeedQuarantine \
  -q -n 0
```
</verification>

<success_criteria>
- `report_feed_failure(...)` is tested as reachable only for feed-owned quarantine decisions.
- Source-class, credential-scope, and UNKNOWN decisions are non-budgeted in tests.
- `FeedFailure` without evidence remains invalid.
- Feed configuration quarantine remains available.
</success_criteria>

<output>
Create `.planning/phases/02-runtime-routing-and-telemetry/02-01-SUMMARY.md`
with files changed, tests run, and any already-satisfied work called out.
</output>
