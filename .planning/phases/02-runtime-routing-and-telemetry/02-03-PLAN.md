---
phase: 02-runtime-routing-and-telemetry
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - backend/pipeline/ingestion/collector_runtime.py
  - backend/pipeline/ingestion/tests/test_collector_runtime.py
autonomous: true
requirements: [RUN-04, RUN-05, RUN-06, TEL-01, TEL-02, TEL-03, TEL-04, TEL-05]
must_haves:
  truths:
    - "D-11: Telemetry is never routing authority."
    - "D-12/D-13: Keep telemetry stable and minimal; keep richer existing fields only if already present."
    - "D-14: post_bookmark_publish_failure must record replay_missing=true and data_gap_known=true."
    - "D-15: Telemetry tests mirror decisions; routing proof still comes from store calls."
  artifacts:
    - path: backend/pipeline/ingestion/collector_runtime.py
      provides: "_emit_policy_decision(...) and _emit_post_bookmark_publish_failure(...)."
    - path: backend/pipeline/ingestion/tests/test_collector_runtime.py
      provides: "Structured log contract tests and no-quarantine telemetry assertions."
  key_links:
    - from: backend/pipeline/ingestion/collector_runtime.py
      to: backend/pipeline/ingestion/failure_policy.py
      via: "decision fields emitted in json_fields"
      pattern: "feed_failure_policy_decision"
---

<objective>
Emit policy decision and post-bookmark publish-gap telemetry.

Every routed failure should produce a stable operational decision log, and
post-bookmark Pub/Sub publish gaps should additionally record that v1 has a
known data gap with no replay available.
</objective>

<execution_context>
@/home/shuojing/.codex/get-shit-done/workflows/execute-plan.md
@/home/shuojing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-runtime-routing-and-telemetry/02-CONTEXT.md
@.planning/phases/02-runtime-routing-and-telemetry/02-RESEARCH.md
@.planning/phases/02-runtime-routing-and-telemetry/02-PATTERNS.md
@.planning/phases/02-runtime-routing-and-telemetry/02-VALIDATION.md
@backend/pipeline/ingestion/collector_runtime.py
@backend/pipeline/ingestion/tests/test_collector_runtime.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stabilize policy decision telemetry</name>
  <files>backend/pipeline/ingestion/collector_runtime.py, backend/pipeline/ingestion/tests/test_collector_runtime.py</files>
  <steps>
  - Ensure `_emit_policy_decision(...)` logs `event_type=feed_failure_policy_decision`.
  - Assert representative budgeted and non-budgeted failures include `feed_id`, `source_type`, `status_reason`, `owner_scope`, `failure_scope`, `endpoint_kind`, `policy_intent`, and `executed_action`.
  - Assert non-budgeted decision telemetry includes `retry_after`.
  - Keep existing extra fields such as `reason`, `feed_budget_eligible`, `quarantine_feed`, `replay_missing`, and `data_gap_known` if already present; do not add new fields only for completeness.
  </steps>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock post-bookmark publish-gap telemetry</name>
  <files>backend/pipeline/ingestion/collector_runtime.py, backend/pipeline/ingestion/tests/test_collector_runtime.py</files>
  <steps>
  - Assert Pub/Sub publish failure after bookmark emits `post_bookmark_publish_failure`.
  - Assert the event has `status_reason=pipeline_publish_after_bookmark_failed`.
  - Assert the event has `policy_intent=hold_for_replay`.
  - Assert the event has `executed_action=suppress_feed_quarantine_record_publish_gap`.
  - Assert the event has `replay_missing=true` and `data_gap_known=true`.
  - Add negative coverage that GCS upload and bookmark write failures do not emit `post_bookmark_publish_failure`.
  </steps>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove non-budgeted paths do not emit feed quarantine telemetry</name>
  <files>backend/pipeline/ingestion/tests/test_collector_runtime.py</files>
  <steps>
  - Patch `quarantine_telemetry.emit_quarantine_event` in representative non-budgeted tests.
  - Cover at least one pipeline-owned case and one source-class or unknown case.
  - Assert `emit_quarantine_event` is not awaited.
  - Keep the positive feed-config quarantine telemetry test from 02-01.
  </steps>
</task>

</tasks>

<threat_model>
- Regression: logs become the hidden routing source.
  Mitigation: tests route by store calls and only inspect logs after routing.
- Regression: post-bookmark data gaps are hidden as generic pipeline errors.
  Mitigation: dedicated event and replay flags are required for the publish stage.
- Regression: suppressed failures still emit quarantine telemetry.
  Mitigation: explicit no-quarantine telemetry assertions on non-budgeted paths.
</threat_model>

<verification>
Run:

```bash
safe-run -- uv run python -m pytest \
  backend/pipeline/ingestion/tests/test_failure_policy.py \
  backend/pipeline/ingestion/tests/test_collector_runtime.py::TestFeedFailureContract \
  backend/pipeline/ingestion/tests/test_collector_runtime.py::TestProcessFeedRetry \
  backend/pipeline/ingestion/tests/test_collector_runtime.py::TestProcessFeedQuarantine \
  -q -n 0
```
</verification>

<success_criteria>
- Every routed failure emits `feed_failure_policy_decision`.
- Post-bookmark Pub/Sub publish gaps emit the dedicated publish-gap event with
  hold-for-replay intent, suppress-publish-gap action, and replay flags.
- Non-budgeted decisions never emit feed quarantine telemetry.
- Telemetry remains an audit mirror, not routing authority.
</success_criteria>

<output>
Create `.planning/phases/02-runtime-routing-and-telemetry/02-03-SUMMARY.md`
with telemetry fields verified, tests run, and any deferred observability work.
</output>
