---
phase: 05-cli-reports-docs-and-verification
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - model/scripts/sft/README.md
  - model/scripts/sft/tests/test_readme_docs.py
  - model/scripts/sft/tests/test_split_dataset_cli.py
autonomous: true
requirements: [CLI-01, CLI-02, CLI-03, CLI-04, CLI-05]
user_setup: []
must_haves:
  truths:
    - "D-21: split_dataset.py is a runbook script, not a production service."
    - "D-22: tests stay minimal and cover CLI dispatch, successful dry-run bundle shape, one hard failure short error, and generate smoke error handling."
    - "D-23: no live GCS automated tests are added."
    - "D-24: no new docs; update existing docs/comments only where code/help does not reveal behavior."
    - "D-25: final verification runs targeted SFT script tests plus py_compile for split_dataset.py."
    - "CLI-05: existing docs or help must explain Source Group, Labeled Segment, SFT Example, SFT Eval Split, and Dataset Version without adding a new document."
  artifacts:
    - path: "model/scripts/sft/README.md"
      provides: "Existing docs updated with concise split_dataset runbook terms if needed"
      contains: "split_dataset.py"
    - path: "model/scripts/sft/tests/test_readme_docs.py"
      provides: "Doc drift guard for concise runbook terms"
      contains: "split_dataset.py"
---

<objective>
Close Phase 05 documentation and verification.

Purpose: the runbook CLI should be discoverable without creating new drift-prone
documentation, and the final verification should be targeted to the SFT script
area.
Output: minimal existing README updates, doc drift guard if needed, and final
targeted verification.
</objective>

<execution_context>
@/home/shuojing/.codex/get-shit-done/workflows/execute-plan.md
@/home/shuojing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-cli-reports-docs-and-verification/05-CONTEXT.md
@.planning/phases/05-cli-reports-docs-and-verification/05-RESEARCH.md
@.planning/phases/05-cli-reports-docs-and-verification/05-VALIDATION.md
@.planning/phases/05-cli-reports-docs-and-verification/05-PATTERNS.md
@model/scripts/sft/README.md
@model/scripts/sft/split_dataset.py
@model/scripts/sft/tests/test_readme_docs.py
@model/scripts/sft/tests/test_split_dataset_cli.py

<interfaces>
No new public implementation interfaces. This plan may update:

```text
model/scripts/sft/README.md
```

Only include behavior not obvious from `split_dataset.py --help`, such as the relationship between Source Group, Labeled Segment, SFT Example, SFT Eval Split, and Dataset Version.
</interfaces>
</context>

<threat_model>
- **T05-07 documentation drift:** detailed artifact trees in docs can become stale. Mitigation: keep README changes concise and point users to CLI output/report inventory for exact paths.
- **T05-08 over-testing runbook code:** broad/live tests make the runbook harder to maintain. Mitigation: no live GCS tests; final verification is targeted SFT script tests.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add minimal existing-doc runbook wording</name>
  <files>model/scripts/sft/README.md</files>
  <read_first>model/scripts/sft/README.md, model/scripts/sft/split_dataset.py, .planning/phases/05-cli-reports-docs-and-verification/05-CONTEXT.md</read_first>
  <action>Update `model/scripts/sft/README.md` only if `split_dataset.py --help` does not already explain the required terms. Add a concise section headed `## Dataset Split Runbook` containing exactly the public command examples `python split_dataset.py dry-run --config-uri gs://... --output-dir ./dry-run-out` and `python split_dataset.py generate --config-uri gs://...`. Add a short terminology list that defines `Source Group`, `Labeled Segment`, `SFT Example`, `SFT Eval Split`, and `Dataset Version` in one sentence each. Do not add a full artifact tree; say that exact generated paths are printed by the CLI and recorded in `dataset_version_report.json`.</action>
  <verify>
    <automated>rg -n "Dataset Split Runbook|Source Group|Labeled Segment|SFT Example|SFT Eval Split|Dataset Version" model/scripts/sft/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `model/scripts/sft/README.md` contains `## Dataset Split Runbook`
    - `model/scripts/sft/README.md` contains `python split_dataset.py dry-run --config-uri`
    - `model/scripts/sft/README.md` contains `python split_dataset.py generate --config-uri`
    - `model/scripts/sft/README.md` contains `Source Group`
    - `model/scripts/sft/README.md` contains `Labeled Segment`
    - `model/scripts/sft/README.md` contains `SFT Example`
    - `model/scripts/sft/README.md` contains `SFT Eval Split`
    - `model/scripts/sft/README.md` contains `Dataset Version`
    - `model/scripts/sft/README.md` contains `dataset_version_report.json`
  </acceptance_criteria>
  <done>Existing docs explain only the non-obvious runbook terms and command examples.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add minimal doc/help drift guards</name>
  <files>model/scripts/sft/tests/test_readme_docs.py, model/scripts/sft/tests/test_split_dataset_cli.py</files>
  <read_first>model/scripts/sft/tests/test_readme_docs.py, model/scripts/sft/tests/test_split_dataset_cli.py, model/scripts/sft/README.md, model/scripts/sft/split_dataset.py</read_first>
  <action>If `model/scripts/sft/tests/test_readme_docs.py` exists, extend it; otherwise create it. Add tests that read `model/scripts/sft/README.md` and assert it contains `Dataset Split Runbook`, the two `split_dataset.py` command examples, and the five required terms. Extend `test_split_dataset_cli.py` help tests to call `main(["dry-run", "--help"])` and `main(["generate", "--help"])` through `pytest.raises(SystemExit)` or an equivalent argparse-safe pattern, asserting dry-run help contains `--config-uri` and `--output-dir`, generate help contains `--config-uri`, and neither help text contains `--scratch-dir` or `validate`.</action>
  <verify>
    <automated>python3 -m pytest model/scripts/sft/tests/test_readme_docs.py model/scripts/sft/tests/test_split_dataset_cli.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `model/scripts/sft/tests/test_readme_docs.py` contains `Dataset Split Runbook`
    - `model/scripts/sft/tests/test_readme_docs.py` contains `Source Group`
    - `model/scripts/sft/tests/test_split_dataset_cli.py` contains `--scratch-dir`
    - `model/scripts/sft/tests/test_split_dataset_cli.py` asserts `--scratch-dir` is absent from help output
  </acceptance_criteria>
  <done>Docs and help have low-cost regression coverage without live cloud dependencies.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run final targeted verification and fix local fallout</name>
  <files>model/scripts/sft/split_dataset.py, model/scripts/sft/dataset_split/*.py, model/scripts/sft/tests/*.py, model/scripts/sft/README.md</files>
  <read_first>.planning/phases/05-cli-reports-docs-and-verification/05-VALIDATION.md, model/scripts/sft/split_dataset.py, model/scripts/sft/tests/test_split_dataset_cli.py, model/scripts/sft/tests/test_dataset_reports.py, model/scripts/sft/tests/test_dataset_publisher.py, model/scripts/sft/tests/test_readme_docs.py</read_first>
  <action>Run `python3 -m py_compile model/scripts/sft/split_dataset.py` and `python3 -m pytest model/scripts/sft/tests -q`. Fix only failures caused by Phase 05 changes. Do not add live GCS tests, broad unrelated model tests, new docs, force/resume/cleanup flags, or a public validate command.</action>
  <verify>
    <automated>python3 -m py_compile model/scripts/sft/split_dataset.py</automated>
    <automated>python3 -m pytest model/scripts/sft/tests -q</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m py_compile model/scripts/sft/split_dataset.py` exits 0
    - `python3 -m pytest model/scripts/sft/tests -q` exits 0
    - `rg -n "add_parser\\(\"validate\"" model/scripts/sft` returns no matches
    - `rg -n "scratch-dir|force|resume|cleanup" model/scripts/sft/split_dataset.py` returns no matches
  </acceptance_criteria>
  <done>Phase 05 is verified with targeted SFT script tests and no out-of-scope CLI surface.</done>
</task>

</tasks>

<verification>
Run after all tasks:

```bash
python3 -m py_compile model/scripts/sft/split_dataset.py
python3 -m pytest model/scripts/sft/tests -q
```
</verification>

<success_criteria>
- Existing docs, not new docs, explain the required terms and command examples.
- Help output exposes only the intended dry-run/generate surface.
- No live GCS tests are introduced.
- Targeted SFT script tests pass.
</success_criteria>

