---
phase: 05-cli-reports-docs-and-verification
plan: 02
type: execute
wave: 1
depends_on: [05-01]
files_modified:
  - model/scripts/sft/dataset_split/reports.py
  - model/scripts/sft/dataset_split/publisher.py
  - model/scripts/sft/dataset_split/dry_run.py
  - model/scripts/sft/tests/test_dataset_reports.py
  - model/scripts/sft/tests/test_dataset_publisher.py
  - model/scripts/sft/tests/test_split_dataset_cli.py
autonomous: true
requirements: [CLI-03, CLI-04]
user_setup: []
must_haves:
  truths:
    - "D-11/D-12/D-13/D-15: report work must not add failure report files; source-key failures remain short fail-fast CLI errors."
    - "D-16: keep dataset_version_report.json and dataset_version_report.md as the summary report pair."
    - "D-17/D-18: add only useful row-level sidecars; successful runs produce reports/excluded_rows.jsonl for non-fatal exclusions."
    - "D-19: do not add a transformations sidecar; transformation row details stay in canonical manifests."
    - "D-20: Markdown report shows summaries and artifact paths only, not row-level tables or transcript/audio examples."
  artifacts:
    - path: "model/scripts/sft/dataset_split/reports.py"
      provides: "Summary report fields for sidecar inventory, source-key failure count, and dry-run audio state"
      contains: "excluded_rows"
    - path: "model/scripts/sft/dataset_split/publisher.py"
      provides: "GCS upload of excluded_rows.jsonl sidecar on successful generate"
      contains: "reports/excluded_rows"
    - path: "model/scripts/sft/dataset_split/dry_run.py"
      provides: "Local excluded_rows.jsonl sidecar in dry-run bundle"
      contains: "excluded_rows.jsonl"
---

<objective>
Complete the Phase 05 report bundle and failure UX.

Purpose: dry-run and generate should produce inspectable reports without
duplicating canonical manifests or creating failure artifacts.
Output: `reports/excluded_rows.jsonl`, report inventory/count updates, and tests
that failure behavior stays short and non-artifact-producing.
</objective>

<execution_context>
@/home/shuojing/.codex/get-shit-done/workflows/execute-plan.md
@/home/shuojing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-cli-reports-docs-and-verification/05-CONTEXT.md
@.planning/phases/05-cli-reports-docs-and-verification/05-RESEARCH.md
@.planning/phases/05-cli-reports-docs-and-verification/05-VALIDATION.md
@.planning/phases/05-cli-reports-docs-and-verification/05-PATTERNS.md
@model/scripts/sft/dataset_split/reports.py
@model/scripts/sft/dataset_split/publisher.py
@model/scripts/sft/dataset_split/dry_run.py
@model/scripts/sft/dataset_split/types.py
@model/scripts/sft/dataset_split/canonical.py
@model/scripts/sft/tests/test_dataset_reports.py
@model/scripts/sft/tests/test_dataset_publisher.py
@model/scripts/sft/tests/test_split_dataset_cli.py

<interfaces>
Create or update these contracts:

```python
def serialize_excluded_rows(rows: Sequence[ExcludedRow]) -> str: ...

def build_dataset_version_report(
    ...,
    excluded_rows: Sequence[ExcludedRow] = (),
    audio_materialized: bool = True,
    source_key_failures: int = 0,
) -> DatasetVersionReport: ...
```

`artifact_inventory["reports"]` must include:

```json
{
  "json": ".../reports/dataset_version_report.json",
  "markdown": ".../reports/dataset_version_report.md",
  "excluded_rows": ".../reports/excluded_rows.jsonl"
}
```
</interfaces>
</context>

<threat_model>
- **T05-04 report bloat/data exposure:** embedding row-level transcripts in Markdown or one large JSON can expose more proprietary content than needed. Mitigation: Markdown includes counts and paths only; row-level skipped rows live in JSONL sidecar.
- **T05-05 duplicated transformation truth:** a transformations sidecar can drift from canonical manifests. Mitigation: do not create it; canonical manifests remain the row-level transformation source.
- **T05-06 failure artifacts:** adding source-key diagnostics files would contradict Phase 05 failure decisions. Mitigation: source-key failures remain fail-fast short errors; successful reports state zero failures.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add report and publisher tests for excluded-row sidecar</name>
  <files>model/scripts/sft/tests/test_dataset_reports.py, model/scripts/sft/tests/test_dataset_publisher.py, model/scripts/sft/tests/test_split_dataset_cli.py</files>
  <read_first>model/scripts/sft/tests/test_dataset_reports.py, model/scripts/sft/tests/test_dataset_publisher.py, model/scripts/sft/tests/test_split_dataset_cli.py, model/scripts/sft/dataset_split/reports.py, model/scripts/sft/dataset_split/publisher.py, .planning/phases/05-cli-reports-docs-and-verification/05-CONTEXT.md</read_first>
  <action>Extend report tests to assert `dataset_version_report.json` contains `excluded_rows` summary with `count`, `reasons.empty_text`, and `artifact_uri`; contains `source_key_failures: 0`; contains `audio_materialized` boolean; and Markdown contains `excluded_rows.jsonl` plus counts but does not include row-level transcript text. Extend publisher tests so expected upload inventory includes `gs://wd-transcription-data/sft/dv-001/reports/excluded_rows.jsonl`, content type is JSONL, and the sidecar serializes `dataset_name`, `row_index`, `audio_uri`, and `reason`. Extend split CLI tests so dry-run writes local `reports/excluded_rows.jsonl` and no file named `failure` exists after a hard failure.</action>
  <verify>
    <automated>python3 -m pytest model/scripts/sft/tests/test_dataset_reports.py model/scripts/sft/tests/test_dataset_publisher.py model/scripts/sft/tests/test_split_dataset_cli.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `model/scripts/sft/tests/test_dataset_reports.py` contains `excluded_rows`
    - `model/scripts/sft/tests/test_dataset_reports.py` contains `source_key_failures`
    - `model/scripts/sft/tests/test_dataset_reports.py` contains `audio_materialized`
    - `model/scripts/sft/tests/test_dataset_publisher.py` contains `reports/excluded_rows.jsonl`
    - `model/scripts/sft/tests/test_split_dataset_cli.py` contains `failure`
  </acceptance_criteria>
  <done>Tests define the successful report sidecar and no-failure-artifact behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement excluded-row sidecar serialization and report fields</name>
  <files>model/scripts/sft/dataset_split/reports.py</files>
  <read_first>model/scripts/sft/dataset_split/reports.py, model/scripts/sft/dataset_split/types.py, model/scripts/sft/dataset_split/canonical.py, .planning/phases/05-cli-reports-docs-and-verification/05-CONTEXT.md</read_first>
  <action>In `reports.py`, import `ExcludedRow`. Add `serialize_excluded_rows(rows)` that returns JSONL sorted by input order, one object per excluded row with exactly `dataset_name`, `row_index`, `audio_uri`, and `reason`, and returns an empty string for no rows. Extend `DatasetVersionReport` with `excluded_rows`, `source_key_failures`, and `audio_materialized`. `excluded_rows` must be a summary mapping with `count`, `reasons`, and `artifact_uri` when available. Extend `build_dataset_version_report()` parameters with `excluded_rows=()`, `excluded_rows_artifact_uri=None`, `source_key_failures=0`, and `audio_materialized=True`. Extend `render_dataset_version_markdown()` with a compact `## Excluded Rows` section that lists count, reasons, and artifact path only. Do not render transcript text or full row tables.</action>
  <verify>
    <automated>python3 -m py_compile model/scripts/sft/dataset_split/reports.py</automated>
    <automated>python3 -m pytest model/scripts/sft/tests/test_dataset_reports.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `model/scripts/sft/dataset_split/reports.py` contains `def serialize_excluded_rows(`
    - `model/scripts/sft/dataset_split/reports.py` contains `source_key_failures`
    - `model/scripts/sft/dataset_split/reports.py` contains `audio_materialized`
    - `model/scripts/sft/dataset_split/reports.py` contains `## Excluded Rows`
    - `model/scripts/sft/dataset_split/reports.py` does not contain `row-level transcript`
  </acceptance_criteria>
  <done>Summary reports know about non-fatal exclusions and dry-run materialization state without embedding row tables.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Publish and dry-run the excluded-row sidecar</name>
  <files>model/scripts/sft/dataset_split/publisher.py, model/scripts/sft/dataset_split/dry_run.py</files>
  <read_first>model/scripts/sft/dataset_split/publisher.py, model/scripts/sft/dataset_split/dry_run.py, model/scripts/sft/dataset_split/artifacts.py, model/scripts/sft/dataset_split/reports.py, model/scripts/sft/tests/test_dataset_publisher.py, model/scripts/sft/tests/test_split_dataset_cli.py</read_first>
  <action>Update `publish_dataset_version_artifacts()` to accept `excluded_rows: tuple[ExcludedRow, ...] = ()`. Add `reports/excluded_rows.jsonl` to artifact inventory and planned artifacts using `serialize_excluded_rows(excluded_rows)` with content type `application/x-ndjson`. Pass `excluded_rows`, `excluded_rows_artifact_uri`, `source_key_failures=0`, and `audio_materialized=True` into `build_dataset_version_report()`. Update `dry_run.py` to write local `reports/excluded_rows.jsonl`, include it in local artifact inventory, and pass `audio_materialized=False`. Keep failure handling in `split_dataset.py` short: do not create any file or directory containing `failure` on expected hard failures.</action>
  <verify>
    <automated>python3 -m py_compile model/scripts/sft/dataset_split/publisher.py model/scripts/sft/dataset_split/dry_run.py</automated>
    <automated>python3 -m pytest model/scripts/sft/tests/test_dataset_publisher.py model/scripts/sft/tests/test_split_dataset_cli.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `model/scripts/sft/dataset_split/publisher.py` contains `excluded_rows:`
    - `model/scripts/sft/dataset_split/publisher.py` contains `reports/excluded_rows`
    - `model/scripts/sft/dataset_split/publisher.py` contains `serialize_excluded_rows`
    - `model/scripts/sft/dataset_split/dry_run.py` contains `excluded_rows.jsonl`
    - `python3 -m pytest model/scripts/sft/tests/test_dataset_publisher.py model/scripts/sft/tests/test_split_dataset_cli.py -q` exits 0
  </acceptance_criteria>
  <done>Generate and dry-run both emit the non-fatal exclusion sidecar, and failure paths remain artifact-free.</done>
</task>

</tasks>

<verification>
Run after all tasks:

```bash
python3 -m pytest model/scripts/sft/tests/test_dataset_reports.py model/scripts/sft/tests/test_dataset_publisher.py model/scripts/sft/tests/test_split_dataset_cli.py -q
```
</verification>

<success_criteria>
- Successful dry-run and generate bundles include `reports/excluded_rows.jsonl`.
- Summary JSON and Markdown mention excluded-row counts and sidecar path.
- Source-key failures remain short nonzero CLI errors with no failure artifacts.
- Transformation details are not duplicated outside canonical manifests.
</success_criteria>

