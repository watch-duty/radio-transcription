---
phase: 05-cli-reports-docs-and-verification
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - model/scripts/sft/split_dataset.py
  - model/scripts/sft/validate_dataset.py
  - model/scripts/sft/dataset_split/validate.py
  - model/scripts/sft/dataset_split/dry_run.py
  - model/scripts/sft/tests/test_split_dataset_cli.py
  - model/scripts/sft/tests/test_dataset_split_validate.py
autonomous: true
requirements: [CLI-01, CLI-02, CLI-03]
user_setup: []
must_haves:
  truths:
    - "D-01/D-03: dry-run writes a local plan bundle with canonical and model-input previews that use planned final gs:// model-ready URIs."
    - "D-02/D-04: dry-run must not call audio materialization and must mark audio_materialized=false."
    - "D-05: dry-run fails when --output-dir already exists."
    - "D-06/D-07: split_dataset.py is the public script and exposes only dry-run and generate."
    - "D-08/D-09: dry-run requires --config-uri and --output-dir; generate requires --config-uri; manifests/source maps/ratios/prefix/model settings come from TOML."
    - "D-10: generate uses temporary scratch space by default and exposes no scratch-dir flag."
    - "D-11/D-12/D-13/D-15: hard failures write no failure files and print a short nonzero error with first failing dataset/context plus cause."
    - "D-14: generate does not add cleanup/resume/force/partial-state report handling."
  artifacts:
    - path: "model/scripts/sft/split_dataset.py"
      provides: "Runbook CLI for dry-run and generate"
      contains: "def main("
    - path: "model/scripts/sft/dataset_split/validate.py"
      provides: "Dataset loading result with segments, excluded rows, and summaries"
      contains: "class DatasetValidationResult"
    - path: "model/scripts/sft/dataset_split/dry_run.py"
      provides: "Plan-only dry-run enrichment and local text artifact writer"
      contains: "audio_materialized"
    - path: "model/scripts/sft/tests/test_split_dataset_cli.py"
      provides: "CLI contract tests for dry-run, generate, and short errors"
      contains: "class TestSplitDatasetCli"
---

<objective>
Create the Phase 05 public CLI entrypoint and the shared split preparation path.

Purpose: users need one runbook script that can either preview a dataset split locally
or publish it to GCS without reopening the older Gemini SFT `pipeline.py`.
Output: `split_dataset.py`, a validation result that preserves valid/excluded rows,
plan-only dry-run artifact generation, and minimal CLI tests.
</objective>

<execution_context>
@/home/shuojing/.codex/get-shit-done/workflows/execute-plan.md
@/home/shuojing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-cli-reports-docs-and-verification/05-CONTEXT.md
@.planning/phases/05-cli-reports-docs-and-verification/05-RESEARCH.md
@.planning/phases/05-cli-reports-docs-and-verification/05-VALIDATION.md
@.planning/phases/05-cli-reports-docs-and-verification/05-PATTERNS.md
@model/scripts/sft/validate_dataset.py
@model/scripts/sft/dataset_split/config.py
@model/scripts/sft/dataset_split/validate.py
@model/scripts/sft/dataset_split/split.py
@model/scripts/sft/dataset_split/leakage.py
@model/scripts/sft/dataset_split/artifacts.py
@model/scripts/sft/dataset_split/model_writers.py
@model/scripts/sft/dataset_split/publisher.py
@model/scripts/sft/tests/test_dataset_split_validate.py
@model/scripts/sft/tests/test_dataset_publisher.py

<interfaces>
Create or update these public contracts:

```python
@dataclass(frozen=True)
class DatasetValidationResult:
    segments: tuple[LabeledSegment, ...]
    excluded: tuple[ExcludedRow, ...]
    summaries: tuple[DatasetValidationSummary, ...]

def load_and_validate_datasets(
    config: DatasetVersionConfig,
    *,
    reader: TextReader,
    echo_registry: Mapping[str, set[str]] | None = None,
) -> DatasetValidationResult: ...

def build_dry_run_artifacts(
    *,
    config: DatasetVersionConfig,
    segments: tuple[LabeledSegment, ...],
    excluded_rows: tuple[ExcludedRow, ...],
    leakage_validation: dict[str, object],
    balance_report: dict[str, object],
    output_dir: Path,
) -> dict[str, object]: ...
```

`split_dataset.py` must expose:

```bash
python split_dataset.py dry-run --config-uri gs://... --output-dir ./dry-run-out
python split_dataset.py generate --config-uri gs://...
```
</interfaces>
</context>

<threat_model>
- **T05-01 dry-run side effects:** dry-run could accidentally download, probe, derive, or upload audio. Mitigation: dry-run uses `dataset_split.dry_run`, not `prepare_audio_for_publication`; tests patch the audio preparer/materializer and assert it is not called.
- **T05-02 misleading public validate entry:** keeping a public validate command would conflict with D-07. Mitigation: `split_dataset.py --help` lists only `dry-run` and `generate`; old `validate_dataset.py` is removed or left unadvertised with tests moved off it.
- **T05-03 failure artifact leakage:** failure report files could leak proprietary rows or transcripts. Mitigation: expected failures return code 1 and print one short error only.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Wave 0 CLI contract tests</name>
  <files>model/scripts/sft/tests/test_split_dataset_cli.py, model/scripts/sft/tests/test_dataset_split_validate.py</files>
  <read_first>.planning/phases/05-cli-reports-docs-and-verification/05-CONTEXT.md, .planning/phases/05-cli-reports-docs-and-verification/05-RESEARCH.md, model/scripts/sft/tests/test_dataset_split_validate.py, model/scripts/sft/tests/test_dataset_publisher.py, model/scripts/sft/validate_dataset.py, model/scripts/sft/dataset_split/validate.py</read_first>
  <action>Create `model/scripts/sft/tests/test_split_dataset_cli.py` using `unittest`, `redirect_stdout`, and `unittest.mock.patch`. Include `FakeTextReader` with `read_text(uri)`. Tests must cover: `main(["dry-run", "--config-uri", config_uri, "--output-dir", out_dir])` returns `0` and creates `reports/dataset_version_report.json`, `reports/dataset_version_report.md`, `manifests/canonical/train.jsonl`, `model_inputs/nemo/train.jsonl`, `model_inputs/whisper/train.jsonl`, and `model_inputs/gemini/train.jsonl`; dry-run report JSON contains `"audio_materialized": false`; dry-run fails with return `1` and text containing `output_dir already exists` when the directory pre-exists; `main(["generate", "--config-uri", config_uri])` dispatches to a patched generation function and returns `0`; patched generation failure returns `1` and prints the exception message; `main(["--help"])` help output contains `dry-run` and `generate` and does not contain a public `validate` subcommand. Update `test_dataset_split_validate.py` so library validation tests import validation helpers directly and no longer assert public `validate_dataset.py` CLI behavior.</action>
  <verify>
    <automated>python3 -m py_compile model/scripts/sft/tests/test_split_dataset_cli.py</automated>
    <automated>python3 -m pytest model/scripts/sft/tests/test_split_dataset_cli.py model/scripts/sft/tests/test_dataset_split_validate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `model/scripts/sft/tests/test_split_dataset_cli.py` contains `class TestSplitDatasetCli`
    - `model/scripts/sft/tests/test_split_dataset_cli.py` contains `test_dry_run_writes_plan_bundle`
    - `model/scripts/sft/tests/test_split_dataset_cli.py` contains `audio_materialized`
    - `model/scripts/sft/tests/test_split_dataset_cli.py` contains `test_dry_run_rejects_existing_output_dir`
    - `model/scripts/sft/tests/test_split_dataset_cli.py` contains `test_generate_dispatches_to_publication_path`
    - `model/scripts/sft/tests/test_split_dataset_cli.py` contains `test_generate_failure_prints_short_error`
    - `model/scripts/sft/tests/test_split_dataset_cli.py` asserts help output includes `dry-run` and `generate`
    - `model/scripts/sft/tests/test_dataset_split_validate.py` no longer imports `validate_dataset as cli`
  </acceptance_criteria>
  <done>Focused failing tests define the runbook CLI behavior before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add validation result and dry-run artifact helpers</name>
  <files>model/scripts/sft/dataset_split/validate.py, model/scripts/sft/dataset_split/dry_run.py</files>
  <read_first>model/scripts/sft/dataset_split/validate.py, model/scripts/sft/dataset_split/normalize.py, model/scripts/sft/dataset_split/types.py, model/scripts/sft/dataset_split/split.py, model/scripts/sft/dataset_split/artifacts.py, model/scripts/sft/dataset_split/model_writers.py, model/scripts/sft/dataset_split/canonical.py, model/scripts/sft/dataset_split/reports.py, .planning/phases/05-cli-reports-docs-and-verification/05-CONTEXT.md</read_first>
  <action>In `dataset_split/validate.py`, add frozen dataclass `DatasetValidationResult` with `segments`, `excluded`, and `summaries`. Add `load_and_validate_datasets(config, reader, echo_registry=None)` that contains the current validation loop, returns all valid `LabeledSegment` rows and non-fatal `ExcludedRow` rows, and preserves the existing fail-fast `DatasetValidationError` wrapping with `dataset=... manifest=... source_strategy=... error=...`. Keep `validate_dataset()` as a compatibility wrapper returning only `result.summaries` if the module still needs it for tests. Create `dataset_split/dry_run.py` with `build_dry_run_artifacts(...)`. This helper must: require `output_dir` not to exist; create a local tree mirroring the text artifact layout; enrich split rows with non-materialized planned `model_ready_audio_uri` values under `DatasetArtifactLayout.for_dataset_version(config.dataset_version_id, root_prefix=config.output_gcs_prefix)`; mark every dry-run row/report with `audio_materialized=False`, `padded=False`, `resampled=False`, and matching `split`/`source_group`; use only existing audio action vocabulary (`reused`, `copied`, `derived`, `transcoded`) if a preview row needs `transformation_metadata.action`; write canonical preview JSONL, NeMo/Whisper/Gemini preview JSONL/configs, `reports/dataset_version_report.json`, and `reports/dataset_version_report.md`. Do not import or call `prepare_audio_for_publication`, `stage_source_audio`, `probe_audio`, `ffmpeg`, or `ffprobe` from this helper.</action>
  <verify>
    <automated>python3 -m py_compile model/scripts/sft/dataset_split/validate.py model/scripts/sft/dataset_split/dry_run.py</automated>
    <automated>python3 -m pytest model/scripts/sft/tests/test_split_dataset_cli.py model/scripts/sft/tests/test_dataset_split_validate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `model/scripts/sft/dataset_split/validate.py` contains `class DatasetValidationResult`
    - `model/scripts/sft/dataset_split/validate.py` contains `def load_and_validate_datasets(`
    - `model/scripts/sft/dataset_split/validate.py` still contains `def validate_dataset(`
    - `model/scripts/sft/dataset_split/dry_run.py` contains `def build_dry_run_artifacts(`
    - `model/scripts/sft/dataset_split/dry_run.py` contains `audio_materialized`
    - `model/scripts/sft/dataset_split/dry_run.py` contains `output_dir already exists`
    - `model/scripts/sft/dataset_split/dry_run.py` does not contain `prepare_audio_for_publication`
    - `model/scripts/sft/dataset_split/dry_run.py` does not contain `probe_audio`
  </acceptance_criteria>
  <done>Shared loading and dry-run artifact generation exist without audio materialization side effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement split_dataset.py and retire public validate CLI</name>
  <files>model/scripts/sft/split_dataset.py, model/scripts/sft/validate_dataset.py</files>
  <read_first>model/scripts/sft/validate_dataset.py, model/scripts/sft/pipeline.py, model/scripts/sft/dataset_split/config.py, model/scripts/sft/dataset_split/gcs_io.py, model/scripts/sft/dataset_split/validate.py, model/scripts/sft/dataset_split/split.py, model/scripts/sft/dataset_split/leakage.py, model/scripts/sft/dataset_split/publisher.py, model/scripts/sft/dataset_split/dry_run.py, model/scripts/sft/prompts.py</read_first>
  <action>Create `model/scripts/sft/split_dataset.py`. Use `argparse` subparsers with only `dry-run` and `generate`. Add `_make_text_reader()` using `google.cloud.storage.Client()` and `GoogleCloudTextReader`, `_load_config(config_uri, reader)`, `_prepare_split(config, reader)` that calls `load_and_validate_datasets()`, `assign_train_eval_split()`, and `validate_split_integrity()`, `_dry_run(args) -> int`, `_generate(args) -> int`, and `main(argv=None) -> int`. `dry-run` must reject non-`gs://` config URIs before making a reader, reject existing output dirs through the dry-run helper, and print `Dry run complete: {output_dir}` plus report path on success. `generate` must call `publish_dataset_version_artifacts()` with temporary scratch defaults and prompts from `model/scripts/sft/prompts.py`, then print `Dataset generated: {root_uri}` and report/model input paths from the publication result. Catch `ConfigValidationError`, `GcsInputError`, `DatasetValidationError`, `SplitAssignmentError`, `SplitLeakageError`, `DatasetPublicationError`, and `DatasetArtifactError`, print `str(exc)`, and return `1`. Delete `validate_dataset.py` or reduce it to a non-public compatibility shim that is not referenced by tests or docs; no public validate command should remain.</action>
  <verify>
    <automated>python3 -m py_compile model/scripts/sft/split_dataset.py</automated>
    <automated>python3 -m pytest model/scripts/sft/tests/test_split_dataset_cli.py model/scripts/sft/tests/test_dataset_split_validate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `model/scripts/sft/split_dataset.py` contains `def main(`
    - `model/scripts/sft/split_dataset.py` contains `add_parser("dry-run"`
    - `model/scripts/sft/split_dataset.py` contains `add_parser("generate"`
    - `model/scripts/sft/split_dataset.py` does not contain `add_parser("validate"`
    - `model/scripts/sft/split_dataset.py` contains `config_uri must be a gs:// URI`
    - `model/scripts/sft/split_dataset.py` contains `publish_dataset_version_artifacts(`
    - `model/scripts/sft/split_dataset.py` contains `build_dry_run_artifacts(`
    - `python3 -m pytest model/scripts/sft/tests/test_split_dataset_cli.py model/scripts/sft/tests/test_dataset_split_validate.py -q` exits 0
  </acceptance_criteria>
  <done>`split_dataset.py` is the only public dataset splitting CLI and supports the required dry-run/generate flows.</done>
</task>

</tasks>

<verification>
Run after all tasks:

```bash
python3 -m py_compile model/scripts/sft/split_dataset.py
python3 -m pytest model/scripts/sft/tests/test_split_dataset_cli.py model/scripts/sft/tests/test_dataset_split_validate.py -q
```
</verification>

<success_criteria>
- `split_dataset.py --help` exposes `dry-run` and `generate`, not `validate`.
- `dry-run` validates inputs, computes split assignment, writes local preview artifacts, and does not materialize audio.
- `generate` dispatches to the existing publisher and prints generated artifact locations.
- Expected hard failures return nonzero and print one short actionable error.
</success_criteria>
